# Prime-patching parity index supplement: docs 693--700

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `693` | `PP3del--PP3den` | Correct canonical eighteenth state, complete minimum-four nineteenth obstruction through budget six, sharp budget-seven correction, and raw twentieth spectrum | Finite nineteenth transition; no recurrence |
| `694` | `PP3deo--PP3deq` | Alternative nineteenth repair selected by next-spectrum signature; exact twentieth and twenty-first corrections; canonical 168-point, twenty-one-block endpoint | Finite chain selected by exact spectra; no periodic invariant |
| `695` | `PP3der--PP3det` | Raw twenty-second minimum six; 26 attempts and 178 cores; all 68,580 preserving budget-six replacements rejected | Minimum-budget obstruction; budget seven open |
| `696` | `PP3deu--PP3dew` | All-width identity separator; exact cyclic optimum `max(0,5K-w+1)`; uniqueness of nontrivial optimizers in small exact censuses | Forced minimum alphabet only; no hidden non-rolling operation |
| `697` | `PP3dex--PP3dez` | Integer realization of finite degree-two defect graphs; explicit 30-centre synthetic packet retaining 28; exact chain formula `27K+1`; exhaustive two-motif incidence transfer census | Resources and labels are synthetic, not extracted from the prime-patching host |
| `698` | `PP3dfa--PP3dfc` | Exact route deduplication and all-optimal audit for every composition with at most four runs; 5,271,552 physical pairs covered | Fixed thirteen-pair source; 848 longer compositions and source-size recurrence open |
| `699` | `PP3dfd--PP3dff` | Vector-valued connector-tour minimax, exact uncertainty census, and joint integer repayment objective | Requires an actual coordinate macro graph with burden and gain vectors |
| `700` | `PP3dfg--PP3dfi` | Candidate/evidence gate and unchanged fixture fixed point | Zero promoted rows; all-`n` open |

## Conservation and legality checks

- Every boundary correction preserves the deleted row and column multisets. The
  canonical states contain 144, 152, 160, and 168 distinct points, and every
  reconstructed state passes the complete no-three audit.
- The twenty-second budget-six census permutes exactly the deleted column multiset
  among the deleted rows. Its matching-count histogram accounts for all
  `68,580` candidates.
- The threshold schedule conserves `8K` cyclic starts as
  `max(0,5K-w+1)` legal starts plus the complementary illegal starts. The forced
  score is `-2` times the identity count.
- Hall source/host labels have load at most two. Coordinate row/column sharing is
  exactly label sharing, and the explicit packet and checked finite chains contain
  no collinear triple.
- Prefix route deduplication preserves geometry because the embedding depends only
  on deletion class, route, and composition. Every inserted point is checked
  against every existing pair.
- Shell connector burdens are added coordinatewise before worst-state selection.
  The repayment formula enforces strict positivity in every uncertainty state.

## Promotion rule

The tranche adds a longer finite boundary chain, one synthetic coordinate Hall
packet, an all-width threshold obstruction, wider finite prefix coverage, and the
exact robust connector objective. It still supplies no recurrent or asymptotic
coordinate source path derived from the prime-patching host. Candidate completion
remains `25/30`, every actual row remains `fixture_derived`, and the all-`n`
theorem remains open.
