# Prime-patching parity index supplement: docs 701--706

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `701` | `PP3dfj--PP3dfl` | All 178 minimum-six twenty-second cores reject every preserving budget-seven repair; 80,525,340 new permutations and 80,593,920 combined budget-six/seven rejections | Minimum-six layer only; 205 raw minimum-seven attempts at budget seven and budget eight remain open |
| `702` | `PP3dfm--PP3dfo` | Complete classification of thirty-centre path/even-cycle signatures retaining at least 28; nine signatures total and at least 24 isolated centres | Structural host filter only; no host-derived motif packet |
| `703` | `PP3dfp--PP3dfr` | Dual nonnegative separators force equality scale `8K`, hidden count `3K`, the same five common-zero types, and multiplicities `4K` each | Current four-by-four primitive alphabet only; enlarged or unexposed operations remain open |
| `704` | `PP3dfs--PP3dfu` | All 7,248 distinct optimal deletion/routes pass all 1,024 compositions; 7,421,952 embeddings cover 30,670,848 physical pairs | Complete fixed thirteen-pair source only; no source-size recurrence |
| `705` | `PP3dfv--PP3dfx` | Exhaustive three-state vector-valued connector-tour and repetition census over 262,144 burden tables | Requires an actual coordinate macro graph with burden and gain vectors |
| `706` | `PP3dfy--PP3dga` | Candidate/evidence gate and unchanged fixture fixed point | Zero promoted rows; all-`n` open |

## Conservation and legality checks

- The boundary census reconstructs the certified 168-point state and the exact 26
  minimum-six attempts before enumerating all seven-point preserving assignments.
  The histogram accounts for all `80,525,340` new candidates.
- The Hall classification enumerates all 18,170 multisets of path and even-cycle
  component orders summing to thirty. Componentwise independence numbers agree
  with `(30+o)/2` in every case.
- The threshold separators are nonnegative on all eighteen legal primitive layers.
  Their two equality equations force both separator sums to vanish. A unimodular
  minor then fixes all five multiplicities.
- Prefix route deduplication preserves geometry because the embedding depends only
  on deletion class, ordered route, and composition. Sixteen deterministic shards
  account for all `7,421,952` distinct audits.
- Shell connector burdens are summed coordinatewise before worst-state selection.
  Every gain/setup formula value is checked against direct strict-positivity
  repetition search.

## Promotion rule

The tranche closes the minimum-six boundary layer through budget seven, gives a
sharp Hall extraction signature, proves all-multiple threshold rigidity, completes
the fixed-source prefix family, and extends robust shell optimization to three
states. It still supplies no recurrent or asymptotic coordinate source path
derived from the actual prime-patching host. Candidate completion remains `25/30`,
every actual row remains `fixture_derived`, and the all-`n` theorem remains open.
