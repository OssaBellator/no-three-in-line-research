# Frontier pass: AC restricted physical kernel manifest

## Active branch

`agent/ac-restricted-kernel-manifest`

Parent: `agent/ac-frontier-manifest-compiler` at `b3f202b278d944d2c85d3b4eaf37cb62d71bceab`.

Only AC is active. Historical AC, RI, BDA, GC, OP, SRR, SAS and all-n branches are immutable source libraries.

## Source mechanisms consolidated

The pass compiles the following historical AC mechanisms into one coherent restricted kernel:

- closed-form retained-partner blocker inventory and scalar slack;
- fixed-order sequential installation states;
- reverse-load threshold transportation and Hall cores;
- weighted layer shortfall partition;
- exact menu-obstruction capacity banks;
- low-buffer threshold-lineage tickets;
- finite occurrence-faithful recreation tickets;
- absolute-threshold and residue-phase tail routing.

No side theorem number is continued and no model-specific constant is imported without its complete restricted-kernel record.

## New theorem block

- **AC5id:** exact restricted-menu compatibility from five enumerated physical exclusion sets.
- **AC5ie:** finite partial-injection graph with ambient state count `sum_j (p_*)_j` and canonical dead-prefix/Hall output.
- **AC5if:** exact threshold layer-cake minimum cost, weighted shortfall partition and classwise paid-failure bound.
- **AC5ig:** explicit low-buffer, recreation, finite-strip and residue-phase stocks.
- **AC5ih:** restricted-kernel episode envelope with the abstract payment/ticket terms expanded into historical AC capacities.

## Logical gain

The previous frontier manifest still treated the physical compatibility graph, ticket stocks and recurrence router as anonymous fields. The restricted kernel now specifies their exact form.

For a fixed target batch and retained pool, compatibility is total once the support, secant, dangerous-line, residual and declared-new-blocker exclusion sets are enumerated. The compatible installation state graph is finite and has the explicit ambient size

\[
N_{\rm inj}=\sum_{j=0}^{t}(p_*)_j.
\]

The previously abstract finite capacities become

\[
T_{\rm low}=\sum_{i,h}U_{(i,h)},
\qquad
T_{\rm rec}=|A||E_{\rm rec}|,
\]

and

\[
N_{\rm paid}
\le
\sum_\lambda
\left\lfloor
\frac{C_\lambda+D_\lambda^{\rm dep}}{\tau_\lambda}
\right\rfloor.
\]

Together with the exact strip and phase counts, these give the AC5ih finite episode envelope. Every failure keeps a concrete target, partner, threshold, Hall set, obstruction class, lineage, recreation edge or phase-cycle address.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_restricted_kernel_manifest.py` checks 2,500 coherent restricted kernels:

- 30,698 exact target-partner compatibility pairs;
- 129 positive-slack systems;
- 2,371 nonpositive-slack overload routes;
- 1,735 full target matchings;
- 765 Hall failures;
- exact aggregate minimum cost 5,209 from threshold layer cakes;
- 1,990 fully paid shortfall systems;
- 510 exact class overloads;
- 381,054 ambient partial-injection states;
- 18,682 low-buffer coordinate-level slots;
- 37,532 low-buffer lineage tickets;
- 35,310 recreation tickets;
- 22,167 exceptional-strip states;
- 25,407 residue-control phase states;
- tail routes: 881 positive drift, 735 zero drift, 884 negative drift;
- aggregate paid-failure bound 62,684;
- aggregate compiled restricted-kernel bound 36,546,268.

All assertions pass.

## Remaining AC frontier

1. Populate the five exact exclusion sets for actual physical menus.
2. Determine the physical macro states covered by the restricted kernel.
3. Enumerate the corresponding compatible prefix and repair graphs.
4. Compute endpoint costs, threshold Hall layers, forward degrees and reverse loads.
5. Certify exact obstruction classes, capacities and deposits.
6. Certify low-buffer lineages, recreation edges and tail transition tables.
7. Run AC5ih on the physical restricted kernel.
8. Enlarge the kernel until every AC macro state is covered or return the first exact out-of-kernel state.

AC6 and the global no-three-in-line conjecture remain open.
