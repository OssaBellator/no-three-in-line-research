# Frontier pass: AC physical edge stratification

## Active branch

`agent/ac-physical-edge-stratification`

Parent: `agent/ac-cycle-discharge-compiler` at `c592f2fc09110b39a8ac00aa732bd1fd2073614a`.

Only AC is active. Historical side branches remain immutable source libraries.

## New theorem block

- **AC5md:** first-exposure and support-descent rank components.
- **AC5me:** acyclic-signature and cycle-bank rank components.
- **AC5mf:** local repair component with immutable earlier components.
- **AC5mg:** complete physical internal-edge stratification.
- **AC5mh:** strict ordinal descent on every physical progress edge.
- **AC5mi:** physical rank/stratification manifest compiler.

## Logical gain

The concrete AC mechanisms now supply one physical ordinal rank. Deficit and credited restart are the first two components. The finite repair component is the mixed-radix combination of:

1. remaining unexposed signatures;
2. current labelled-recursion support size;
3. old-signature DAG rank;
4. remaining cycle-bank rank;
5. local canonical repair rank.

First exposure, support shrinkage, acyclic old-signature motion, cycle-bank descent and local repair each lower the first component they are allowed to change. Payments, tickets, disturbances, recreation and funded resets remain exceptional and require one live occurrence.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_physical_edge_stratification.py` checks 2,500 edge records:

- strict physical progress edges: `1,750`;
- supply edges: `250`;
- credited-restart edges: `250`;
- first-signature-exposure edges: `250`;
- labelled-support descents: `250`;
- acyclic old-signature edges: `250`;
- cycle-bank descents: `250`;
- local repair edges: `250`;
- exceptional occurrence edges: `250`;
- strict mixed-radix checks: `1,750`;
- earlier-component/misclassification failures: `250`;
- missing exceptional-occurrence failures: `250`;
- exception-alias witnesses: `250`.

All assertions pass.

## Remaining AC frontier

1. Compute the rank components on actual operations from the manifested AN seed.
2. Repair every returned nondecreasing or misclassified physical edge.
3. Assign every exceptional edge through the integrated source network.
4. Run fair reachable shell expansion with overlap-preserving rank extensions.
5. Complete the initial-state manifest or retain its first finite witness.

AC6 and the global no-three-in-line conjecture remain open.
