# Frontier pass: AC physical schema registry

## Active branch

`agent/ac-physical-schema-registry`

Parent: `agent/ac-initial-state-termination-manifest` at `4c186c720019fc6807bfc87f2ab7c15de1348b86`.

Only AC is active. Historical AC, RI, BDA, GC, OP, SRR, SAS and all-n branches remain immutable source libraries.

## New theorem block

- **AC5kn:** canonical prime-minus-one initial physical schema.
- **AC5ko:** deterministic reconstruction of derived fields and reduced schema digest.
- **AC5kp:** exact live-field guardrail and least changed live cause.
- **AC5kq:** transition-relative target-menu and owner-status schema rows.
- **AC5kr:** cap-only refinement preserves the schema digest.
- **AC5ks:** completed `schema` section of the seven-section initial-state manifest.

## Logical gain

The historical chain contains several fields that were once retained as independent state data but are actually deterministic outputs. Under their declared contracts, the reduced schema deletes:

1. canonical active hosts;
2. canonical minimal repair envelopes;
3. determinant-derived BDA quotient labels;
4. prime-field RI subgroup, root, image, companion and scale labels;
5. canonical private selected subbanks and owner assignments.

Their generators remain live. Arbitrary filters, nonmaximal protected subbanks, global contracts, shared owners, non-unit capacities, target-completion states, owner-status rows, source dictionaries and the operation dictionary are not removed.

The canonical manifest digest is

\[
D_{\rm red}=H(\text{immutable schema},\text{live generators}).
\]

A cached derived field is accepted only when it equals its deterministic reconstruction and is excluded from `D_red`. Cap-only box enlargement leaves this digest unchanged; a new physical dictionary entry is a schema boundary.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_physical_schema_registry.py` checks 2,500 generated schemas:

- valid complete schemas: `1,000`;
- derived reconstruction checks: `12,500`;
- derived-cache mutation witnesses: `2,500`;
- genuine live-field digest changes: `1,000`;
- prime-minus-one failures: `250`;
- undersized-bank failures: `250`;
- malformed permutation failures: `250`;
- determinant/BDA failures: `250`;
- physical RI identity failures: `250`;
- immutable schema-digest failures: `250`.

All assertions pass.

## Remaining AC frontier

1. Instantiate the reduced schema with the actual initial red/blue permutations, neutralization bank and current star.
2. Extract the concrete historical operation-family registry.
3. Enumerate complete target-menu states and deterministic successor records.
4. Populate the six historical recognizer row families.
5. Run the remaining manifest sections and discharge the first finite witness.

AC6 and the global no-three-in-line conjecture remain open.
