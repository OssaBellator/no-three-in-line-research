# Prime-patching parity index supplement: docs 651--656

This supplement indexes theorem identifiers and evidence boundaries for the
uniform-mechanism tranche.

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `651` | `PP3czp--PP3czr` | Fourteenth spectrum, all 125 best-core repairs through budget seven, minimum three-point transition, raw fifteenth obstruction | Finite corrected chain only |
| `652` | `PP3czs--PP3czu` | Bounded-overlap motif packing and sharp candidate threshold | Conditional conflict graph; geometric host absent |
| `653` | `PP3czv--PP3czx` | Cellwise sign coherence and additive threshold endpoint drift | Matrix endpoint obstruction; no geometric inverse |
| `654` | `PP3czy--PP3daa` | Two-pair component anchor obstruction and minimum four cross-component anchors | Finite thirteen-pair source |
| `655` | `PP3dab--PP3dad` | Heterogeneous-period saving and mean-burden criterion | No measured geometric macro repertoire |
| `656` | `PP3dae--PP3dag` | Candidate/evidence gate and unchanged fixed point | Zero promoted rows; all-`n` open |

## Parity and conservation checks

- The canonical boundary correction deletes and inserts three points with identical
  row and column multisets.
- Threshold target differences preserve every row and column margin individually,
  but their cellwise signs are coherent, so batching cannot cancel endpoint drift.
- Prefix anchor pairings remain perfect matchings between the `P` and `Q` cell
  sets; the obstruction concerns row/column compatibility inside incidence
  components rather than cardinality.
- Shell cost comparisons use exact rational arithmetic and compare every schedule
  to the same fifteen-control-per-period baseline.

## Promotion rule

None of these exact statements supplies a complete coordinate-level source path
for an integration row. Candidate completion therefore remains `25/30`, all
actual rows remain `fixture_derived`, and the all-`n` theorem remains open.
