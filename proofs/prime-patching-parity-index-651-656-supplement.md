# Prime-patching parity index supplement: docs 651--656

This supplement indexes theorem identifiers and evidence boundaries for the
consolidated uniform-mechanism tranche.

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `651` | `PP3czp--PP3czr` | Fourteenth spectrum, all 125 best-core repairs through budget seven, minimum three-point transition, raw fifteenth obstruction | Finite corrected chain only |
| `652` | `PP3czs--PP3czu` | Two-stage motif-overlap and centre-conflict packing, vertex-cover/matching certificate, sharp candidate threshold | Conditional graphs; coordinate host absent |
| `653` | `PP3czv--PP3czx` | Forty-four three-round factorizations, invariant full-grid exposure with load five, sign-coherent additive endpoint drift | Matrix scheduling/obstruction; no geometric inverse |
| `654` | `PP3czy--PP3daa` | Explicit fourteen-pair saturated source, fourteen anchors, all 8,192 compositions, single incidence cycle | Finite global source; no recurrence or nesting |
| `655` | `PP3dab--PP3dad` | Exact heterogeneous prefix envelope and complete periodic gain classification | No measured geometric macro burden sequence |
| `656` | `PP3dae--PP3dag` | Candidate/evidence gate and unchanged fixed point | Zero promoted rows; all-`n` open |

## Parity and conservation checks

- The canonical boundary correction deletes and inserts three points with identical
  row and column multisets.
- Hall selection is separated into resource-disjoint motif packing and centre-level
  conflict deletion; no centre is counted twice across the two stages.
- Each threshold target preserves row and column margins, every factorization uses
  each incidence exactly once, and transient choices cancel from the endpoint.
  The remaining target displacements are cellwise sign-coherent.
- The fourteen-pair source has degree two in every row and column. Its fourteen
  anchor pairs partition all twenty-eight source cells and use distinct endpoint
  rows and columns.
- Shell cost comparisons use exact rational arithmetic and compare every prefix to
  the same fifteen-control-per-period baseline.

## Supplementary finite comparison

The older thirteen-pair source has incidence-component sizes `2,2,4,5`; its two
size-two components are internally anchor-unpairable, forcing at least four
cross-component anchors. This is checked by
`scripts/check_prefix_component_anchor_obstruction.py`, but it is not assigned a
second competing `PP3czy--PP3daa` theorem sequence.

## Promotion rule

None of these exact statements supplies a complete coordinate-level source path
for an integration row. Candidate completion therefore remains `25/30`, all
actual rows remain `fixture_derived`, and the all-`n` theorem remains open.
