# Prime-patching parity index supplement: docs 675--680

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `675` | `PP3dcj--PP3dcl` | Exact correction census for seven eighteenth cores through budget six; one six-point correction reaches eighteen blocks; complete raw nineteenth spectrum | Finite corrected chain only; no recurrence |
| `676` | `PP3dcm--PP3dco` | Path/even-cycle classification and exact independent-set formula for bipartite maximum-degree-two centre-conflict graphs | Conditional on coordinate-certified bipartiteness, degree two, and sufficient first-stage motifs |
| `677` | `PP3dcp--PP3dcr` | Explicit minimal eight-state mixture attaining hidden weight `3/8` with three copies of `4I_4` and five legal facet states | Algebraic equality witness; hidden state is not an exposed legal primitive |
| `678` | `PP3dcs--PP3dcu` | Forty selected first optimal routes pass all 1,024 compositions; 40,960 coordinate audits and exact coordinate bounds | Selected finite representatives only; no full coordinate orbit or recurrence |
| `679` | `PP3dcv--PP3dcx` | Rational mixed-cycle weights clear to deterministic composite walks; exact connector and setup repayment | Requires coordinate macro cycles, connectors, and a certified burden polytope |
| `680` | `PP3dcy--PP3dda` | Candidate/evidence gate and unchanged fixture fixed point | Zero promoted rows; all-`n` open |

## Conservation checks

- The canonical eighteenth correction deletes and inserts six points with identical
  row and column multisets.
- Hall centre retention is computed componentwise; path and even-cycle formulas
  agree exactly with total centres minus bipartite matching loss.
- The threshold identity conserves every row and column total:
  `3*(4I_4)+sum_i L_i=8S`.
- Every prefix insertion preserves distinct occupied rows and columns, and all
  collinear triples in the 40,960 audited embeddings remain within one labelled
  run.
- Mixed shell weights are cleared to integer cycle counts before execution;
  connector loss is charged once per composite tour and not hidden in an
  independent-edge bound.

## Correction to the preceding shell interpretation

The two-loop example in `docs/673` separates primitive loops from composite
closed-walk execution. Each primitive loop has robust gain `-1`, but concatenating
one copy of each gives robust gain one. The prior document and checker were updated
accordingly before `docs/679` was added.

## Promotion rule

The tranche supplies exact finite transitions, a sharp algebraic threshold mixture,
and stronger conditional execution interfaces, but no complete recurrent or
asymptotic coordinate source path. Candidate completion remains `25/30`, every
actual row remains `fixture_derived`, and the all-`n` theorem remains open.
