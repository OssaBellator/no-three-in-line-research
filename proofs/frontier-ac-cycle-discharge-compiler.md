# Frontier pass: AC physical cycle discharge compiler

## Active branch

`agent/ac-cycle-discharge-compiler`

Parent: `agent/ac-ac3-signature-recurrence` at `35d8cd97d8cb2d357af9d09daa993719ca335a66`.

Only AC is active. Historical side branches remain immutable source libraries.

## New theorem block

- **AC5lx:** canonical simple decorated cycle with retained deletion ledger.
- **AC5ly:** least changed field and first-return prefix.
- **AC5lz:** complete physical cycle-route normal form.
- **AC5ma:** integrated source/recreation cycle-ticket assignment.
- **AC5mb:** finite recurrent-cycle bound under accepted routes.
- **AC5mc:** exact unresolved cycle core and manifest continuation.

## Logical gain

Every old-signature SCC cycle now retains a simple decorated profile cycle together with all deleted physical subwalk occurrences. The least changed outer field and its first return identify the exact prefix on which payment, descent, ticket or impossibility must occur.

Accepted cycle routes are current payment, strict bounded-bank descent, source ticket, recreation ticket, physical impossibility or funded reset. Cycle tickets share one physical source assignment and cannot be independently counted by type.

An unresolved cycle is not returned as a generic recurrence. It carries its simple word, deletion ledger, first-return prefix and first missing discharge certificate.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_cycle_discharge_compiler.py` checks 2,500 episodes:

- return episodes: `2,500`;
- simple decorated cycles: `2,500`;
- deleted subwalk vertices retained in audit ledgers: `13,164`;
- canonical first-field returns: `2,500`;
- current paid cycles: `500`;
- strict bank-descent cycles: `500`;
- source-ticket cycles: `250`;
- recreation-ticket cycles: `250`;
- physical-impossibility cycles: `500`;
- exact unresolved cycle cores: `500`;
- successful integrated ticket assignments: `500`;
- deficient ticket flows: `500`;
- duplicate cycle-ticket alias witnesses: `500`.

All assertions pass.

## Remaining AC frontier

1. Populate the actual cycles returned by the signature graph.
2. Prove one accepted route for each cycle or retain its unresolved core.
3. Construct the shared cycle-ticket source assignment.
4. Append accepted routes to the ordinal manifest.
5. Continue reachable expansion from the manifested AN seed.

AC6 and the global no-three-in-line conjecture remain open.
