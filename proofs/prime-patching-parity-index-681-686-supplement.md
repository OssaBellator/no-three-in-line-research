# Prime-patching parity index supplement: docs 681--686

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `681` | `PP3ddb--PP3ddd` | Corrected nineteenth frontier: unique `P2/-64` minimum-four attempt, five cores, and no preserving correction through budget six | Finite obstruction; budget seven and recurrence open |
| `682` | `PP3dde--PP3ddg` | Exact odd-path surplus formula `alpha(H)=(|V|+o)/2` and sharp Hall condition `3q+o>=56` | Conditional on a coordinate-certified selected family and complete degree-two centre graph |
| `683` | `PP3ddh--PP3ddj` | Unique minimum-illegal decomposition of `8S`: twelve identity layers and four copies of each of five legal facet permutations | Algebraic/layer obstruction; no hidden geometric execution |
| `684` | `PP3ddk--PP3ddm` | All 208 physical matching/deletion cases pass the deterministic all-unit route lift | One composition only; arbitrary compositions and recurrence open |
| `685` | `PP3ddn--PP3ddp` | Balanced connected rational circulations clear exactly to Eulerian composite shell walks; disconnected supports require connectors | Requires a coordinate macro graph and certified robust burdens |
| `686` | `PP3ddq--PP3dds` | Candidate/evidence gate and unchanged fixture fixed point | Zero promoted rows; all-`n` open |

## Conservation checks

- The corrected 144-point eighteenth state remains row/column balanced and legal;
  `docs/681` adds no transition and therefore introduces no new coordinate state.
- For a bipartite maximum-degree-two centre graph, every odd path contributes one
  parity-surplus unit, giving `2*alpha=|V|+o` exactly.
- The unique threshold decomposition conserves all 16 matrix entries:
  twelve identity layers plus twenty legal layers equal `8S`.
- Every prefix lift preserves distinct occupied rows and columns; all collinear
  triples in the 208 audited all-unit embeddings remain within one labelled run.
- A shell circulation conserves flow at every macro state. Denominator clearing
  preserves balance, and the Euler tour uses every edge with exactly its certified
  multiplicity.

## Correction carried into this tranche

The committed 144-point state has the nineteenth histogram

```text
4:1,5:13,6:46,7:176,8:280,9:1,
10:17,11:61,12:124,13:176,14:137.
```

`P1/-33` has minimum five, not four. The unique minimum-four attempt is `P2/-64`
with five cores. `docs/675` and its transition wrapper were corrected before
`docs/681` was added.

## Promotion rule

The tranche supplies sharper exact obstructions and finite execution interfaces,
but no complete recurrent or asymptotic coordinate source path. Candidate
completion remains `25/30`, every actual row remains `fixture_derived`, and the
all-`n` theorem remains open.
