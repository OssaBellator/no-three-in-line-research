# OP frontier pass: zero-vector physical decorations

- **OP4aw:** a deterministic decoration map on `K` states repeats within `K` transitions.
- **OP4ax:** at most `K-1` first-visit decoration tickets are consumed.
- **OP4ay:** physical decoration potentials telescope on the eventual cycle.
- **OP4az:** zero and nonzero invariant profiles both have finite recurrence routers.

## Remaining frontier

Build the actual finite decoration dictionaries and score decompositions; pay positive traversals, concentrate classified debt or justify balanced-cycle tickets.

Verifier: `scripts/verify_phase_zero_vector_decorations.py`.