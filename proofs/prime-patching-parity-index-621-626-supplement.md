# Prime-patching parity index supplement: `docs/621--626`

This supplement continues the cumulative parity index after `docs/620`.

| ID | Statement | Status | Location |
|---|---|---|---|
| PP3cwd--PP3cwf | The corrected seventh boundary state has an exact eighth transversal spectrum, one seven-point corrected eighth transition, and no raw ninth extension | PROVED | `docs/621-repeated-corrected-boundary-transition.md` |
| PP3cwg--PP3cwi | Bad-vertex deletion extracts a robust residual `K_4,4` exactly when the resource reserve exceeds the bad-vertex counts by four per side | PROVED / CONDITIONAL INTERFACE | `docs/622-bad-vertex-reserve-hall-extraction.md` |
| PP3cwj--PP3cwl | Every nearest legal degree-four threshold target is generated by exactly two conservative `2 x 2` pivots, with six shortest paths per target | PROVED | `docs/623-two-pivot-generation-of-threshold-trades.md` |
| PP3cwm--PP3cwo | Maximal unary-run counts give an exact two-anchor retained-source reservoir price and conditional source conversion | PROVED / CONDITIONAL INTERFACE | `docs/624-retained-anchor-reservoir-price.md` |
| PP3cwp--PP3cwr | The unique optimal odd shell column of `l_1` norm at most three is `(1,1,1)`, giving twelve active controls and a zero-buffer period | PROVED / CONDITIONAL SOURCE COLUMN | `docs/625-optimal-all-cycle-odd-shell-column.md` |
| PP3cws--PP3cwu | Candidate coverage is `25/30`, fixture arithmetic slack remains positive, and no geometric row is promoted | PROVED | `docs/626-repeated-transition-source-generation-evidence-gate.md` |

## Frontier update

### Boundary recleaning

The corrected-state path now reaches eight blocks.  Of 520 eighth-block attempts,
one has minimum transversal four, six have minimum five, twenty-one have minimum
six, and 492 require more than six.  Across the twenty-one minimum transversals
of sizes four and five, exactly one supports a degree-preserving correction of
size seven.  The resulting sixty-four-point state has no raw ninth extension.

### Localized Hall transport

The six-resource completion lemma now has an exact reserve interpretation.  If
`b_L,b_R` count resources on which either real forbidden family branches, a
robust residual `K_4,4` can be extracted whenever

```text
total_resources >= 6 + max(b_L,b_R).
```

A six-resource host requires no bad vertices; eight resources tolerate two per
side.

### Fractional direct-clean layers

The eight source-preserving three-cycle replacements now have explicit source
matrix paths.  Each is exactly two nonnegative conservative `2 x 2` residual
pivots from the stored matrix, and each has six shortest pivot paths.  This closes
the candidate matrix-generation field without supplying geometric pivot edits.

### Support-chord repair words

The selected profile has aggregate `1212286655580` maximal unary runs, mean
`209/29`.  Two retained anchors per run require aggregate
`2424573311160`, mean `418/29`, and at most twenty-two cells per encoding.  The
source reservoir remains unidentified.

### Clean-macro shells

Among nonnegative odd columns of norm at most three, `(1,1,1)` is uniquely best.
The identity

```text
2A+4B+6D=(12,10,8)
```

uses twelve active controls and eight idle slots.  The word
`DABABBBDDDDDIIIIIIII` is feasible at every prefix with zero startup buffer.

### Integration

Candidate coverage rises to `25/30` because the threshold matrix-level source
path is complete.  All actual rows remain fixture-derived, so geometric closure
is false and the all-`n` theorem remains open.

## Exact diagnostics

Run the complete group with

```bash
python scripts/check_frontier_621_626.py
```

or individually with

```bash
python scripts/check_boundary_eighth_corrected_transition.py
python scripts/check_hall_bad_vertex_reserve.py
python scripts/check_threshold_two_pivot_generation.py
python scripts/check_prefix_retained_anchor_reservoir.py
python scripts/check_shell_all_cycle_odd_column.py
python scripts/check_repeated_transition_source_generation_gate.py
```

The next available theorem identifier is `PP3cwv`.
