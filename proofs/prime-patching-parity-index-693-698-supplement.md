# Prime-patching parity index supplement: docs 693--698

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `693` | `PP3del--PP3den` | Reconstructs the canonical eighteenth state, proves all six minimum-four nineteenth cores fail through budget six, gives a row/column-preserving budget-seven correction to 152 points, and computes the exact raw twentieth spectrum with nine minimum-four cores | Exact finite coordinate transition only; no recurrence or promoted row |
| `694` | `PP3deo--PP3deq` | Classifies all 1,636 two-motif, six-centre degree-two defect-incidence packet graphs and audits 6,544 exact packet chains | Complete combinatorial packet target; no coordinate motif realization or periodic geometric interface |
| `695` | `PP3der--PP3det` | The separator proves forced-alphabet legality iff identity-free at every width; sharp cyclic optimum `max(0,5K-w+1)` | Obstruction only inside the minimum forced alphabet and rolling exposure model |
| `696` | `PP3deu--PP3dew` | Deduplicates optimal routes to 3,624 per deletion and verifies all 176 compositions with at most four runs, covering 5,271,552 physical pairs | Fixed thirteen-pair source; 848 longer compositions and all-size recurrence remain open |
| `697` | `PP3dex--PP3dez` | Vector-valued minimax connector tours, exact uncertainty incompatibility census, and joint repetition objective | Requires a coordinate macro graph with path-burden and gain vectors |
| `698` | `PP3dfa--PP3dfc` | Candidate/evidence gate and unchanged fixture fixed point | Zero promoted rows; all-`n` open |

## Conservation checks

- The boundary transition preserves the deleted row and column multisets exactly.
  The reconstructed 144-point predecessor and corrected 152-point successor both
  pass full exact no-three audits.
- Every Hall source and host label class has load at most two. The internal conflict
  graph is exactly the union of the two label matchings, and packet interfaces are
  enforced by exact boundary-state compatibility.
- The threshold separator score is additive. Every identity contributes `-2`,
  every forced exposed layer contributes zero, and every legal permutation layer
  has nonnegative score.
- Prefix route deduplication changes no geometry: the embedding kernel depends only
  on deletion class, route, and composition. Every physical route occurrence maps
  to one audited distinct route state.
- Shell path burdens are summed componentwise before taking a worst uncertainty
  state. The repetition objective enforces strict positivity at every burden
  vertex separately.

## Promotion rule

The tranche advances the finite boundary chain and substantially narrows the
combinatorial Hall, rolling-threshold, bounded-prefix, and robust-shell interfaces.
It does not supply a complete recurrent or asymptotic coordinate source path.
Candidate completion remains `25/30`, every actual row remains `fixture_derived`,
and the all-`n` theorem remains open.
