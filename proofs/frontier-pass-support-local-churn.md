# AC frontier pass: synchronized support-local churn

- AC5gd: every changed record in the seven typed tracks has a least typed support cause, or returns a nonlocal dependency/type witness.
- AC5ge: synchronized audit work is at most `sum_s H_s(alpha_R,s L_T,s+alpha_+,s L_R,s)` and total matching deficit at most `sum_s H_s(alpha_R,s+alpha_-,s)`.
- AC5gf: every residual unmatched root remains support-caused; one typed primitive `(track,resource)` anchors at least `ceil(delta/sum_s |P_s|)` roots.
- AC5gg: the support-local work and augmentation bounds add across macro epochs without cross-ledger mass creation or cancellation.

Validation: 1,500 seven-track macro epochs, 177,940 key-local pairs versus 732,501 full-product pairs, 57,504 disturbed roots and 39,268 residual deficits.

Remaining: construct concrete dependencies and prove numerical support, primitive-incidence and key-fibre bounds; pay the resulting support-anchored track-key Hall roots and close recurrent AC6 replenishment.