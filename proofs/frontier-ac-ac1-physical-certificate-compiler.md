# Frontier pass: AC1 physical certificate compiler

## Active branch

`agent/ac-ac1-physical-certificate-compiler`

Parent: `agent/ac-an-bank-seed-manifest` at `571cea8c727ec8cb7c7231999b53df3b103fe74b`.

Only AC is active. Historical side branches remain immutable source libraries.

## New theorem block

- **AC5lf:** occurrence-faithful AC1 certificate serialization.
- **AC5lg:** canonical normalized heavy rank and heavy physical label.
- **AC5lh:** exact anchor/pair incidence identities and anchor-link router.
- **AC5li:** sound typed continuation normal form for anchor, quotient, carry and BDA outputs.
- **AC5lj:** complete no-improvement AC1 ledger compiler.
- **AC5lk:** AC1 manifest continuation and corrected finite frontier.

## Logical gain

The AN no-improvement output now retains every physical rank-one through rank-three triple occurrence. Distinct triples supported by the same rich line remain distinct occurrences.

A heavy normalized family may continue only with one complete semantic certificate:

1. an endpoint-disjoint star and complete AN seed fields;
2. a physical ratio set, proved small quotient and current paid weight;
3. a complete carry signature, exact new/repeated status and owner route;
4. a determinant-realized denominator occurrence and physical BDA chamber.

A label alone is not a continuation. The compiler returns the heavy occurrence family together with the first missing structural certificate.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_ac1_physical_certificate_compiler.py` checks 2,500 physical AC1 ledgers:

- certificate occurrences: `220,072`;
- normalized heavy-rank checks: `2,500`;
- heavy-label checks: `2,500`;
- exact incidence-identity checks: `2,500`;
- rank-three pair concentrations: `87`;
- rank-three endpoint-disjoint stars: `746`;
- rich-line multiplicity occurrences retained separately: `217,699`;
- certified continuations: `1,000`;
- anchor continuations: `250`;
- quotient continuations: `250`;
- carry continuations: `250`;
- BDA continuations: `250`;
- exact missing semantic certificates: `1,500`.

All assertions pass.

## Remaining AC frontier

1. Supply or refute each returned semantic certificate on the actual AN occurrence family.
2. Compile accepted structural outputs into physical AC2 operations.
3. Apply compatibility re-extraction and exact paid-overload routing.
4. Populate later repair, source, rank and exceptional records.
5. Continue the reachable initial-state termination manifest.

AC6 and the global no-three-in-line conjecture remain open.
