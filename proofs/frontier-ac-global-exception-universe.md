# Frontier pass: AC global exceptional occurrence universe

## Active branch

`agent/ac-global-exception-universe`

Parent: `agent/ac-ordinal-termination-router` at `99f43771ec6cb41f19713bbb4ef956f8c52d616d`.

Only AC is active. Historical AC, RI, BDA, GC, OP, SRR, SAS and all-n branches remain immutable source libraries.

## New theorem block

- **AC5jv:** finite fixed-schema exceptional claim registry.
- **AC5jw:** exact integral physical-source to exceptional-claim maximum flow.
- **AC5jx:** global cross-type source conservation and no double use.
- **AC5jy:** cap-invariance of the exceptional universe.
- **AC5jz:** complete globally deduplicated exceptional stock.
- **AC5ka:** fixed-schema reduction to ordinal termination.

## Logical gain

Payments, low-buffer tickets, recreation tickets, disturbances and funded resets now share one physical source ledger. A source compatible with several exception types is counted once.

The exact source-backed exceptional stock is the maximum flow in

\[
\text{source}\to S_{\rm phys}\to A_{\rm exc}\to\text{sink}.
\]

Under a fixed schema, numerical cap enlargement does not change the low-buffer thresholds, recreation atoms, restoration edges, obstruction classes, source classes, reset addresses or event atoms. Therefore the global exception network and its maximum flow are independent of the box level.

This discharges AC5jr once the actual fixed physical registry and compatibility arcs are supplied.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_global_exception_universe.py` checks 2,500 registries:

- valid fixed registries: `500`;
- physical source stock: `6,771`;
- exceptional claim stock: `6,225`;
- integrated maximum-flow stock: `4,544`;
- naive separately counted stock: `6,212`;
- cross-type overcount removed: `1,668`;
- low-buffer ticket addresses: `7,832`;
- recreation ticket addresses: `5,310`;
- reset stock: `1,549`;
- disturbance stock: `2,072`;
- complete direct-address registry: `16,763`;
- source-to-claim compatibility arcs: `26,555`;
- new-address-on-cap-expansion failures: `500`;
- relabel conflicts: `500`;
- duplicate debits: `500`;
- unrecorded deposits: `500`.

All assertions pass.

## Remaining AC frontier

1. Enumerate the actual fixed physical source and exceptional claim dictionaries.
2. Construct source-to-claim compatibility and run the integrated flow.
3. Discharge the first source/claim cut or address discrepancy.
4. Stratify physical internal edges and extend the repair rank.
5. Prove reachable cofinality of the cap tower.
6. Apply AC5ka--AC5js.

AC6 and the global no-three-in-line conjecture remain open.
