# Frontier pass: AC AN-bank physical seed manifest

## Active branch

`agent/ac-an-bank-seed-manifest`

Parent: `agent/ac-operation-family-registry` at `830d759349c64f2dfb3f71182672d2070e94d31f`.

Only AC is active. Historical side branches remain immutable source libraries.

## New theorem block

- **AC5kz:** canonical physical AN seed validation.
- **AC5la:** exact alternating installation bank and typed operation addresses.
- **AC5lb:** exact star destruction and physical collateral census.
- **AC5lc:** canonical improving selector or complete AC1 no-improvement ledger.
- **AC5ld:** schema, serializer and initial frontier population for an AN seed.
- **AC5le:** uniform compiler for the first physical AC initial-state class.

## Logical gain

The first physical initial-state class is no longer a placeholder. A valid seed retains the rectangle switch, inserted candidate, endpoint-disjoint star, selected opposite-layer endpoint type, row/column sets, other-layer occupancy and exact degree-two forbidden-position matrix.

For every allowed matching `pi in Omega(F)`, the compiler creates one physical `install` operation with successor

\[
S_\pi=Z\cup M_\pi.
\]

It computes exact rank-one through rank-three collateral certificate counts and returns either the lexicographically least improving state or the complete no-improvement ledger required by AC1.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_an_bank_seed_manifest.py` checks:

- degree-two forbidden-position banks: `2,500`;
- aggregate allowed matching states: `345,226,537`;
- exact AN1 bank-count checks: `2,500`;
- rank-one through rank-three partial-matching probability checks: `7,500`;
- complete physical seeds: `80`;
- concrete installation operations: `46,320`;
- designated star-pair destructions: `324,240`;
- exact physical collateral triples: `13,204`;
- canonical improving selectors: `80`;
- complete no-improvement ledger records: `80`;
- schema/serializer round trips: `80`.

All assertions pass.

## Remaining AC frontier

1. Populate AC1's physical certificate labels on the no-improvement ledger.
2. Compile the four AC1 structural outputs into typed physical operations.
3. Instantiate the later repair, arithmetic and source rows reached by those outputs.
4. Manifest every later restart class or certify its permanent boundary.
5. Run the reachable initial-state termination manifest from the seed.

AC6 and the global no-three-in-line conjecture remain open.
