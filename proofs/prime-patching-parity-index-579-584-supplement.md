# Prime-patching parity index supplement: `docs/579--584`

This supplement continues the cumulative parity index after `docs/578`.

| ID | Statement | Status | Location |
|---|---|---|---|
| PP3crh--PP3crj | Complete inherited-line states give the radius-32 boundary census `8,688,886,376,8,0` and a six-block obstruction | PROVED FOR THE BOUNDED CATALOGUE | `docs/579-larger-offset-inherited-line-horizon.md` |
| PP3crk--PP3crm | Cylinder choices decode to twelve coordinate grid pairs, every pair has a no-three-in-line matching extension, and both orientations lift it | PROVED FOR THE COORDINATE GRID CANDIDATE | `docs/580-coordinate-grid-matching-decoder-for-hall.md` |
| PP3crn--PP3crp | Eighteen permutation layers are legal, no decomposition of the aligned matrix is all-legal, and four geometric triple normals are hidden | PROVED / GEOMETRIC OBSTRUCTION | `docs/581-geometric-obstruction-for-aligned-threshold-layers.md` |
| PP3crq--PP3crs | The profile terminal multiset is fixed, does not determine nesting risk, and any exact decoder must retain ancestry incidence | PROVED | `docs/582-state-only-support-cell-decoder-obstruction.md` |
| PP3crt--PP3crv | The stored shell cycle system has a full-rank source-action incidence bridge, exact buffer transfer, and a cycle-coordinate phase table | PROVED FOR THE STORED SHELL SYSTEM | `docs/583-cycle-resource-incidence-bridge-for-shell-controls.md` |
| PP3crw--PP3cry | Coordinate progress leaves candidate completion at 23/30, preserves fixture arithmetic slack, and promotes no geometric row | PROVED | `docs/584-coordinate-identification-evidence-gate.md` |

## Frontier update

### Boundary recleaning

At offset radius thirty-two, exact globally legal path counts are

```text
8, 688, 886, 376, 8, 0
```

for one through six blocks. The eight five-block survivors are all-P words using
two variant patterns and four signed offset patterns. None admits a sixth block.

### Localized Hall transport

The twelve quotient choices are now realized as actual cells in the first two
rows of a four-by-four grid. Every pair extends to a no-three-in-line permutation:
six pairs have one extension and six have two, for eighteen total extensions.

### Fractional direct-clean layers

Only eighteen of the twenty-four four-cell permutation layers are geometrically
legal. None of the eighty-four decompositions of the aligned conservative matrix
uses four legal layers; every decomposition has at least two illegal layers.
Among fifteen positive-cell collinear triples, eleven quotient normals are
controlled and four are hidden.

### Support-chord repair words

Every object in the thirty-leaf, nine-binary profile has terminal inventory
`(10,11,9)`, yet positive subfamilies occur at risks zero and ten. A support-cell
decoder based only on terminal types cannot recover nesting risk; parent-child
incidence is necessary.

### Clean-macro shells

The three cycle inequalities in `docs/517` induce the full-rank incidence matrix

```text
((1,1,0),(0,1,1),(1,0,1)).
```

It maps the stored action buffer `(2/5,0,0)` to cycle reserve `(2/5,0,2/5)` and
connects the canonical unit-action model to the twenty-slot shell schedule.

### Integration

Candidate completion remains `23/30`. The exact fixture fixed point and positive
arithmetic slack are unchanged. All six actual rows remain fixture-derived, so
geometric closure remains false.

## Exact diagnostics

Run the complete group with

```bash
python scripts/check_frontier_579_584.py
```

or run the six new diagnostics individually. The bounded audits cover every
radius-32 boundary extension through length six, all coordinate Hall matching
extensions, all eighty-four threshold decompositions and fifteen geometric
triple normals, the complete prefix risk distribution endpoints, the stored
shell cycle/action bridge, and the exact six-row fixture fixed point.

The next available theorem identifier is `PP3crz`.
