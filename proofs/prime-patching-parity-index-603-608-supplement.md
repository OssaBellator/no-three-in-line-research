# Prime-patching parity index supplement: `docs/603--608`

This supplement continues the cumulative PP3 index after `docs/602`.

| ID | Statement | Status | Location |
|---|---|---|---|
| PP3cub--PP3cud | The sharp boundary repairs have `210092` exact five-point degree refills, all illegal | PROVED | `docs/603-five-point-boundary-refill-obstruction.md` |
| PP3cue--PP3cug | A one-free-resource lift of the stored conditional Hall-star fixture has 96 complete embeddings, 72 uniformly robust | PROVED / FINITE SOURCE-TYPED FIXTURE | `docs/604-source-typed-five-resource-hall-lift.md` |
| PP3cuh--PP3cuj | The unique legal threshold augmentation requires degree-five margins, four new source-cell units, and one extra slot | PROVED | `docs/605-threshold-augmentation-source-margin-obstruction.md` |
| PP3cuk--PP3cum | Distinct unary-run points give aggregate `396499770810` guaranteed collinear triples, mean `33/14` | PROVED / ENCODING GEOMETRY | `docs/606-distinct-unary-run-chord-triples.md` |
| PP3cun--PP3cup | The monoid generated by recorded shell primitives remains in the even-sum cycle lattice | PROVED | `docs/607-recorded-shell-source-monoid-obstruction.md` |
| PP3cuq--PP3cus | Candidate completion stays `23/30`; zero rows are promoted and closure remains false | PROVED / EVIDENCE GATE | `docs/608-source-identification-evidence-gate.md` |

## Frontier update

### Boundary recleaning

The correction class now includes the sharp three deletions plus every pair of
further deletions and every exact five-point row-column refill.  There are
`3960` deletion cases and `210092` unique refill candidates; none is legal.

### Localized Hall transport

The abstract `4 x 5` robustness survives a source-typed finite lift of the stored
binary-resource-star fixture.  Of 480 candidate embeddings, 96 decode every
choice and 72 have blocker number at least two throughout.  The extra free
resource is not yet supplied by the asymptotic host.

### Fractional direct-clean layers

The unique legal fifth layer is `(3,0,1,2)`.  It changes all row and column
margins from four to five, adds four source-cell units, and occupies one fifth of
the augmented schedule.  Existing fixed-margin threshold operations cannot
supply it.

### Support-chord repair words

Separating unary-run copies into distinct integer points retains an exact
ancestry statistic: the aggregate guaranteed within-run collinearity is
`396499770810`, mean `33/14`.  The vertical-run coordinates are not yet actual
support chords.

### Clean-macro shells

Idle slots, simultaneous compositions, repetitions, and the scaled startup
buffer do not cross the recorded action lattice.  An odd-sum action must be
genuinely new.

### Integration

The fixed-point arithmetic remains feasible, but every direct row remains
fixture-derived.  The all-`n` theorem remains open.

## Exact diagnostics

```bash
python scripts/check_frontier_603_608.py
```

or individually:

```bash
python scripts/check_boundary_five_point_refill_obstruction.py
python scripts/check_hall_lifted_conditional_star.py
python scripts/check_threshold_augmentation_source_margin.py
python scripts/check_prefix_distinct_run_chord_triples.py
python scripts/check_shell_recorded_closure_parity.py
python scripts/check_source_identification_evidence_gate.py
```

The next theorem identifier is `PP3cut`.
