# Prime-patching parity index supplement: `docs/549--554`

This supplement continues the cumulative parity index after `docs/548`.

| ID | Statement | Status | Location |
|---|---|---|---|
| PP3cnv--PP3cnx | Phase-locked boundary words lift through finite additive block signatures and yield block-derived ledger rows | PROVED / GEOMETRIC BLOCK TABLE STILL REQUIRED | `docs/549-finite-signature-adapter-for-boundary-markers.md` |
| PP3cny--PP3coa | Microscopic Hall gadgets project exactly to certified quotient kernels under finite strong-lumpability identities | PROVED / ACTUAL GADGET CENSUS STILL REQUIRED | `docs/550-exact-lumpability-adapter-for-hall-gadgets.md` |
| PP3cob--PP3cod | A finite facet-normal matrix converts common quotient boxes into simultaneous geometric threshold bounds | PROVED / ACTUAL NORMAL MATRIX STILL REQUIRED | `docs/551-facet-normal-adapter-for-threshold-schedules.md` |
| PP3coe--PP3cog | Exact profile counts and aggregate risk sums certify a legal prefix object satisfying all declared restrictions | PROVED / ACTUAL RISK CENSUS STILL REQUIRED | `docs/552-profile-sliced-existence-for-prefix-risk.md` |
| PP3coh--PP3coj | A physical shell-incidence matrix converts phase words into exact pooled reserve and ledger certificates | PROVED / ACTUAL INCIDENCE MATRIX STILL REQUIRED | `docs/553-incidence-projected-shell-reserve-adapter.md` |
| PP3cok--PP3com | Nonnegative cross-frontier couplings close under a rational supersolution below the global margin | PROVED / ACTUAL COUPLING MATRIX STILL REQUIRED | `docs/554-coupled-frontier-supersolution-closure.md` |

## Frontier update

### Boundary recleaning

A phase-locked marker word now has a complete finite geometric adapter: verify the
local four- and seven-step block signatures and the allowed seams, then every
global defect, controller, and action count follows by addition.  In the stored
signature fixture the exact phase thresholds remain 60, 67, and 74, while block
prices derive the boundary row `7/120` from length 120 onward.

### Localized Hall transport

The quotient Hall kernels no longer need to be postulated independently of the
local gadget state space.  Strong lumpability gives a finite integer audit.  The
stored six-state refinement commutes exactly with both quotient kernels for every
switch word through length ten and reproduces reverse load `11723/524288` at
degree sixteen.

### Fractional direct-clean layers

Every actual threshold inequality may be listed as one row of a finite normal
matrix in quotient coordinates.  The common box then controls all inequalities
by exact dual products.  The stored three-facet adapter has dual loss `1/32` and
derives the threshold row `1/24` from length 96 onward.

### Support-chord repair words

Profile abundance is now converted into simultaneous existence.  Exact aggregate
risk sums and thresholds leave a certified nonempty complement.  In the stored
30-leaf, nine-branch profile, at least `52566257495` objects meet all four risk
bounds, and their normalized risk row is `1/40`.

### Clean-macro shells

Shell phases may now be checked in physical incidence coordinates rather than an
abstract pooled space.  The stored three-resource incidence map has three aligned
zero-reserve phase pairs and derives the shell row `1/40` from length 40 onward.

### Integration

Hidden cross-frontier losses are represented by a nonnegative coupling matrix.
The exact supersolution

```text
(59,23,42,26,26,22)/1000
```

has total `99/500`, leaving slack `13/250`.  After the existing `6/N`
integerization overhead, the coupled adapter fixture is strictly feasible from
length 120 onward, improving the previous abstract threshold 361.

## Exact diagnostics

Run the complete group with

```bash
python scripts/check_frontier_549_554.py
```

or individually with

```bash
python scripts/check_boundary_signature_adapter.py
python scripts/check_hall_lumpability_adapter.py
python scripts/check_threshold_facet_adapter.py
python scripts/check_prefix_profile_risk_adapter.py
python scripts/check_shell_incidence_adapter.py
python scripts/check_coupled_frontier_supersolution.py
```

The local audits verify 5781 phase-locked boundary certificates, 2047 Hall switch
words with all six microscopic starts, 14706 threshold intervals, 2450 finite
profile ratios, all nine shell phase pairs with 300 repeated physical prefixes,
and every coupled length from 120 through 5000.

## Honest endpoint

This tranche advances all six realization frontiers by giving finite adapter
criteria.  It does not establish that the actual prime-patching geometry supplies
the stored block signatures, microscopic transition census, facet normals, risk
sums, shell incidence matrix, or coupling coefficients.  Those six data-extraction
problems remain the global closure target.

The next available theorem identifier is `PP3con`.
