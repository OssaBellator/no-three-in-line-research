# Frontier pass: AC canonical box and nested tower

## Active branch

`agent/ac-canonical-box-tower`

Parent: `agent/ac-bounded-epoch-serializer` at `39b665a47c86bfc003fbc178c7e62049bedc4b6f`.

Only AC is active. Historical AC, RI, BDA, GC, OP, SRR, SAS and all-n branches remain immutable source libraries.

## New theorem block

- **AC5ix:** canonical componentwise least historical cap profile from low-buffer thresholds, conserved physical stocks and a complete finite-phase observation window.
- **AC5iy:** exhaustive disjoint frontier partition into terminal, shortage, amplification, monotone escape, reset, zero-neighbourhood and overflow records.
- **AC5iz:** unique least cap vector resolving the current finite overflow queue and exact product-shell formula.
- **AC5ja:** shell-efficient coordinate queue, finite current-frontier drain and telescoping proof reuse.
- **AC5jb:** direct-limit physical graph and atlas for a cofinal nested box tower.
- **AC5jc:** uniform tower closure criterion and exact divergence witness when a uniform hypothesis fails.

## Logical gain

The first box is no longer chosen by arbitrary small constants. Historical AC contracts force

\[
K_i^{(0)}=\beta_i,
\qquad
C_a^{(0)}=S_a^{\rm init}+S_a^{\rm dep},
\qquad
H^{(0)}=H_{\rm abs}+G|Q|M.
\]

These are the least caps containing every low-buffer face and first rich level, every physically conserved balance, and one complete residue-control observation window.

For the current overflow queue `R`, the unique least enlargement is

\[
L_z^+=\max\bigl(L_z,\max_{r:z(r)=z}v(r)\bigr).
\]

The exact shell is

\[
N(L^+)-N(L),
\]

and sequential coordinate expansions telescope to this same one-shot difference. AC5iw preserves every old interior object.

A cofinal tower has a well-defined direct-limit graph. If all selected bounded atlases admit one uniform globally deduplicated bound

\[
E_*
=(C_{\rm reset}+1)\Psi_*
+UW_{\rm dist}
+C_{\rm pay}+C_{\rm ticket},
\]

then no nonterminal direct-limit trajectory has more than `E_*` steps. Failure returns the first noncofinal coordinate, overlap mismatch, divergent potential or divergent physical ledger.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_canonical_box_tower.py` checks 2,500 generated systems:

- aggregate canonical ambient state bound: `87,324,551,933,092`;
- residue-control phase states: `18,504`;
- sampled tail transient steps: `4,697`;
- finite-strip returns: `1,254`;
- phase cycles: `1,246`;
- positive/zero/negative drift cycles: `481 / 730 / 35`;
- typed boundary records: `22,588`;
- terminal routes: `4,504`;
- paid routes: `4,530`;
- certified escape routes: `2,302`;
- reset routes: `2,235`;
- unresolved overflows: `9,017`;
- coordinates enlarged: `6,632`;
- overflows resolved: `9,017`;
- exact aggregate enlargement shell: `232,616,018,137,130` states;
- zero-neighbourhood objects: `6,234`;
- uniform tower certificates: `1,805`;
- explicit divergent-tower witnesses: `695`;
- maximum sampled uniform episode bound: `346`.

All assertions pass.

## Remaining AC frontier

1. Instantiate the canonical cap profile with actual physical AC thresholds and deposits.
2. Complete historical recognizer records for every zero-neighbourhood interior object.
3. Compile permanent certificates for shortage, amplification, monotone escape and reset records.
4. Resolve each finite overflow queue by the least enlargement.
5. Construct a cofinal physical tower.
6. Prove uniform global potential and resource-ledger bounds, or retain the first divergent component as the exact AC6 obstruction.

AC6 and the global no-three-in-line conjecture remain open.
