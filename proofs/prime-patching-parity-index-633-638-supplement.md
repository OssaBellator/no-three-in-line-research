# Prime-patching parity index supplement: `docs/633--638`

This supplement continues the cumulative PP3 index after `docs/632`.

| ID | Statement | Status | Location |
|---|---|---|---|
| PP3cxn--PP3cxp | The radius-64 ten-block state has a complete eleventh transversal census, all twelve minimum-four attempts are correctable within budget seven, a five-point correction reaches eleven blocks, and no raw twelfth step exists | PROVED | `docs/633-corrected-eleventh-boundary-transition.md` |
| PP3cxq--PP3cxs | One matching-shaped plus two degree-two forbidden families have a sharp twenty-six-residual-resource extraction threshold | PROVED / CONDITIONAL INTERFACE | `docs/634-mixed-degree-hall-extraction.md` |
| PP3cxt--PP3cxv | Every threshold alternating `C6` is an indivisible transportation-kernel circuit and must be exposed atomically | PROVED / FINITE REDUCTION | `docs/635-threshold-c6-circuit-indivisibility.md` |
| PP3cxw--PP3cxy | An explicit `12 x 12` degree-two saturated source supplies twelve anchor pairs and all 2,048 unary-run compositions | PROVED / FINITE SOURCE MODEL | `docs/636-twelve-pair-saturated-anchor-reservoir.md` |
| PP3cxz--PP3cyb | Six symmetric shell-macro uses improve the baseline exactly inside `6*delta+C<3`, allowing at most two integer collateral controls at unit cost | PROVED / CONDITIONAL COST INTERFACE | `docs/637-shell-collateral-budget.md` |
| PP3cyc--PP3cye | Candidate coverage stays `25/30`, fixture arithmetic is unchanged, and zero geometric rows are promoted | PROVED | `docs/638-scalable-source-realization-evidence-gate.md` |

## Frontier update

### Boundary recleaning

The exact eleventh minimum-transversal histogram is

```text
4:12, 5:35, 6:76, 7:135, 8:261,
9:21, 10:41, 11:79, 12:99, 13:170, 14:103.
```

The twelve minimum-four attempts have ninety-four minimum cores.  Every attempt
has a degree-preserving correction within budget seven.  The canonical `P1`
offset-31 correction uses five deletions and reaches an eighty-eight-point
corrected eleven-block state.  All 1,032 raw twelfth attempts fail.

### Localized Hall transport

If the partner fibre is already matching-shaped and the source and host-defect
families have maximum degree two, each side's collision graph has maximum degree
four.  Twenty-six residual resources force an independent six-set; twenty-five
fail sharply through five disjoint `K5` components.  The corresponding
preselection threshold is twenty-eight resources per side.

### Fractional direct-clean layers

Each target `C6` has sixty-four signed-support subsets.  Only the empty subset and
the complete cycle preserve every row and column margin.  Thus no proper visible
sub-edit can implement part of the operation while retaining source margins.

### Support-chord repair words

The new permutations

```text
P=(6,7,0,4,9,2,11,10,1,5,8,3),
Q=(2,9,11,7,0,4,10,8,3,6,1,5)
```

form a twenty-four-cell, degree-two, no-three source on `12 x 12`.  A perfect
anchor pairing supports all 2,048 ordered compositions of twelve unary nodes,
with zero mixed-run triples and maximum coordinate magnitude 187.

### Clean-macro shells

For six `(1,1,1)` uses with per-use overhead `delta` and fixed period collateral
`C`, total active-equivalent cost is `12+6*delta+C`.  Strict improvement over the
recorded fifteen-control baseline is exactly `6*delta+C<3`.  At unit macro cost,
two integer repair controls are affordable and three tie.

### Integration

Candidate completion remains `25/30`.  The fixture fixed point and positive slack
are unchanged.  All direct rows remain fixture-derived and geometric closure
stays false.

## Exact diagnostics

```bash
python scripts/check_frontier_633_638.py
```

The new standalone audits are the compiled eleventh spectrum/correction kernels,
the mixed-degree Hall checker, the threshold circuit checker, the twelve-pair
reservoir checker, the shell collateral checker, and the strict evidence gate.

The next available theorem identifier is `PP3cyf`.
