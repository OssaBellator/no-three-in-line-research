# Prime-patching parity index supplement: docs 657--662

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `657` | `PP3dah--PP3daj` | Ten minimum-four fifteenth attempts, 156 cores all repaired through budget seven, four-point transition to fifteen blocks, raw sixteenth obstruction | Finite corrected chain only |
| `658` | `PP3dak--PP3dam` | Caro–Wei motif degree-sequence packing, average-degree corollary, two-stage centre-matching certificate | Conditional host degrees and conflicts absent |
| `659` | `PP3dan--PP3dap` | Three-round transient factorization, invariant full-grid load, unique balanced order per matching with all intermediates illegal | Matrix-level exposed-state obstruction |
| `660` | `PP3daq--PP3das` | Exact `(2,2)` deletion boundary flow, rerouting radius four, 144 optimal repairs per case | Anchor permutation layer only |
| `661` | `PP3dat--PP3dav` | Reachable positive-cycle criterion, optimal cycle-mean saving rate, exact entry-plus-cycle setup bound | No coordinate macro transition graph |
| `662` | `PP3daw--PP3day` | Candidate/evidence gate and unchanged fixed point | Zero promoted rows; all-`n` open |

## Conservation checks

- The canonical fifteenth correction deletes and inserts four points with identical
  row and column multisets.
- Hall motif selection uses an independent set in the resource-overlap graph;
  centre deletion is accounted for separately by a bipartite matching bound.
- Threshold perfect matchings use every transient cell once. The unique balanced
  order exposes every matrix cell twice, but transient cancellation does not make
  any intermediate legal.
- Prefix rerouting remains a bijection between the eleven surviving `P` and `Q`
  rows and changes exactly four assignments in every deletion case.
- Shell transition weights compare each period against the same three-control
  saving margin; positive cycle gain is invariant under cycle rotation.

## Promotion rule

The tranche supplies exact finite and combinatorial mechanisms but no complete
coordinate-level asymptotic source path. Candidate completion remains `25/30`,
all actual rows remain `fixture_derived`, and the all-`n` theorem remains open.
