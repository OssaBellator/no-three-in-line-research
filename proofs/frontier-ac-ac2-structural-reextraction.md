# Frontier pass: AC2 physical structural re-extraction

## Active branch

`agent/ac-ac2-structural-reextraction`

Parent: `agent/ac-ac1-physical-certificate-compiler` at `0f16a744e6c0ab5d871617f9321665c203e12e9c`.

Only AC is active. Historical side branches remain immutable source libraries.

## New theorem block

- **AC5ll:** canonical complete incompatibility graph.
- **AC5lm:** deterministic compatible-family extraction.
- **AC5ln:** paid closed-neighbourhood overload router.
- **AC5lo:** labelled overload concentration and strict support descent.
- **AC5lp:** occurrence-faithful source assignment or canonical Hall cut.
- **AC5lq:** complete AC2 physical continuation compiler.

## Logical gain

Every AC1 structural object now retains its complete physical footprint, paid occurrence set, collateral rows, protected contracts, cross-object certificate rows and finite conflict labels. Pairwise compatibility means independence in this full graph—not merely disjoint geometric support.

The compiler either extracts a compatible paid family or returns one object whose closed conflict neighbourhood carries more than `K` times its own current paid weight. A paid overload is narrowed to one exact arithmetic label and then gives a compatible same-label family or a strict recursive support descent.

Selected payment demands must also admit an occurrence-faithful matching to globally addressed physical source units. Shared source aliases are counted once; failure returns a canonical Hall deficiency.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_ac2_structural_reextraction.py` checks 2,500 systems:

- physical structural objects: `17,624`;
- exact conflict edges: `9,364`;
- direct compatible batches: `359`;
- paid closed-neighbourhood overloads: `1,141`;
- compatible same-label families: `778`;
- recursive overloads: `363`;
- strict support descents: `363`;
- feasible occurrence-source assignments: `996`;
- canonical source Hall failures: `504`;
- missing true conflict rows: `500`;
- duplicated paid occurrence aliases: `500`.

All assertions pass.

## Remaining AC frontier

1. Populate the true conflict rows of the actual quotient, carry, BDA and star objects.
2. Classify each returned paid overload label geometrically.
3. Supply each physical source assignment or discharge its Hall cut.
4. Turn successful batches into reachable progress/exceptional operations.
5. Route repeated signatures through the exact AC3 cycle and ticket interfaces.
6. Continue the initial-state termination manifest.

AC6 and the global no-three-in-line conjecture remain open.
