# Frontier pass: BDA footprint-costed repairs

## New closure

- BDA5hz bounds restoration-repair footprint conflicts by `h(beta-1)`.
- BDA5ia extracts and executes a commuting footprint-disjoint batch with equal matching-deficit descent.
- BDA5ib either fits the batch inside the typed potential reserve or returns the least exact shortage.
- BDA5ic bounds cumulative scalar repair cost by `kappa(Delta_0 + sum U_j)`.

## Verified finite record

Seed `733` checks 2,500 restoration systems and a 5,000-epoch churn-funded cost ledger.

## Remaining frontier

Construct the physical rational-gain repair maps and footprints; prove concrete overlap, cost, reserve, and replenishment bounds; preserve the occurrence-faithful potential ledger. The global conjecture remains open.