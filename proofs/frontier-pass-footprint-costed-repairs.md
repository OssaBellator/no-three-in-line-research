# Frontier pass: GC footprint-costed repairs

## New closure

- GC2mx bounds cleaning-repair footprint conflicts by `h(beta-1)`.
- GC2my extracts and executes a commuting footprint-disjoint batch with equal donor-deficit descent.
- GC2mz either fits the batch inside the typed geometric reserve or returns the least exact shortage.
- GC2na bounds cumulative scalar repair cost by `kappa(Delta_0 + sum U_j)`.

## Verified finite record

Seed `739` checks 2,500 cleaning systems and a 5,000-epoch churn-funded cost ledger.

## Remaining frontier

Construct chart/protected-event repair maps and footprints; prove concrete overlap, cost, reserve, and replenishment bounds; preserve donor and protected-height ledgers. The global conjecture remains open.