# Frontier pass: OP footprint-costed repairs

## New closure

- OP4gw bounds phase-repair footprint conflicts by `h(beta-1)`.
- OP4gx extracts and executes a commuting footprint-disjoint batch with equal source-deficit descent.
- OP4gy either fits the batch inside the typed phase reserve or returns the least exact shortage.
- OP4gz bounds cumulative scalar repair cost by `kappa(Delta_0 + sum U_j)`.

## Verified finite record

Seed `743` checks 2,500 phase systems and a 5,000-epoch churn-funded cost ledger.

## Remaining frontier

Construct quotient/action/unit/valuation/holonomy repair maps and footprints; prove concrete overlap, cost, reserve, and replenishment bounds; preserve all typed phase ledgers. The global conjecture remains open.