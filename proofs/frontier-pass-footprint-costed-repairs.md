# Frontier pass: SAS footprint-costed repairs

## New closure

- SAS5nw bounds sparse-repair footprint conflicts by `h(beta-1)`.
- SAS5nx extracts and executes a commuting footprint-disjoint batch with equal neutral-deficit descent.
- SAS5ny either fits the batch inside the typed sparse reserve or returns the least exact shortage.
- SAS5nz bounds cumulative scalar repair cost by `kappa(Delta_0 + sum U_j)`.

## Verified finite record

Seed `757` checks 2,500 sparse systems and a 5,000-epoch churn-funded cost ledger.

## Remaining frontier

Construct move/legality/orientation/lineage repair maps and footprints; prove concrete overlap, cost, reserve, and replenishment bounds; preserve neutral and orientation ledgers. The global conjecture remains open.