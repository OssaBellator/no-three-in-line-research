# Frontier pass: AC footprint-costed repairs

## New closure

- AC5gt bounds global within-track and cross-track repair-footprint conflicts by `h(beta-1)`.
- AC5gu extracts and executes a globally commuting footprint-disjoint batch with equal synchronized-deficit descent.
- AC5gv either fits the batch inside the synchronized typed reserve or returns the least exact track-resource shortage.
- AC5gw gives trackwise expenditure bounds and the global envelope `sum E_s <= kappa_*(Delta_0 + sum U_j)`.

## Verified finite record

Seed `761` checks 1,500 seven-track typed systems and a 5,000-epoch trackwise churn-funded cost ledger.

## Remaining frontier

Construct all physical repair maps and shared-state footprints; prove concrete overlap and cost constants; establish typed reserve and replenishment inequalities; combine the cost envelope with finite macro-state recurrence. The global conjecture remains open.