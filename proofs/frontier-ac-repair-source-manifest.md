# Frontier pass: AC repair-source physical manifest

## Active branch

`agent/ac-repair-source-manifest`

Parent: `agent/ac-physical-edge-stratification` at `d193cee2361b99991cc72b280e9011bfb4bef606`.

Only AC is active. Historical side branches remain immutable source libraries.

## New theorem block

- **AC5mj:** global layer success or canonical bad-layer witness.
- **AC5mk:** deficit-sized occurrence-slot repair transversal.
- **AC5ml:** exact repair-source issuance and classwise conservation.
- **AC5mm:** source compatibility assignment or canonical Hall core.
- **AC5mn:** missing compatibility rectangle and failed-predicate concentration.
- **AC5mo:** complete repair/source manifest compiler.

## Logical gain

A global incidence failure now localizes to one quantitatively bad layer. A Hall-deficient occurrence-slot graph returns exactly `delta` distinct outside-slot repair incidences. Those incidences must consume class-compatible physical repair-source occurrences under one global and classwise conservation law.

If numerical source stock is sufficient but compatibility fails, a canonical Hall core exposes a complete missing source-incidence rectangle. One retained compatibility predicate carries a guaranteed share of that rectangle.

## Deterministic audit

Equivalent execution of `scripts/verify_ac_repair_source_manifest.py` checks 2,500 systems:

- retained incidence layers: `9,982`;
- canonical bad-layer witnesses: `1,968`;
- globally adequate layer systems: `532`;
- physical repair uses: `10,077`;
- old slots: `5,037`;
- deficit-sized transversal incidences: `5,495`;
- repair-source tokens: `15,663`;
- actual source-backed issuances: `5,047`;
- exact conservation checks: `2,500`;
- complete source assignments: `2,188`;
- canonical source Hall cores: `312`;
- Hall deficit units: `325`;
- missing compatibility rectangle pairs: `873`;
- failed-predicate concentration checks: `312`;
- source-less issuance witnesses: `63`.

All assertions pass.

## Remaining AC frontier

1. Populate the actual incidence-layer values.
2. Construct the returned outside repair slots.
3. Enumerate the physical repair-source tokens and event histories.
4. Validate the concrete source-incidence predicates.
5. Discharge every bad layer, missing rectangle or conservation witness.
6. Append accepted repair operations to the reachable ordinal manifest.

AC6 and the global no-three-in-line conjecture remain open.
