# Prime-patching parity index supplement: `docs/555--560`

This supplement continues the cumulative parity index after `docs/554`.

| ID | Statement | Status | Location |
|---|---|---|---|
| PP3con--PP3cop | Boundary marker catalogues require signature-preserving provenance; the current four/seven row is fixture-backed pending an exact geometric block-and-seam extraction | PROVED / PROVENANCE AUDIT | `docs/555-provenance-typed-boundary-catalogue-extraction.md` |
| PP3coq--PP3cos | Quotient Hall kernels have nonunique strongly lumpable microscopic lifts; geometric realization requires an independently enumerated microcensus and partition test | PROVED / PROVENANCE AUDIT | `docs/556-independent-microcensus-before-hall-lumpability.md` |
| PP3cot--PP3cov | Physical threshold normals are quotient-controlled exactly when `G=Lambda Q`; nonfactorable rows are hidden resources, and the stored facets lack geometric source IDs | PROVED / PROVENANCE AUDIT | `docs/557-threshold-normal-factorization-and-hidden-residuals.md` |
| PP3cow--PP3coy | Aggregate profile risks suffice for existence but require a traceable enumerator for geometric evidence; the stored totals certify `52566257495` objects but remain fixture-backed | PROVED / PROVENANCE AUDIT | `docs/558-traceable-prefix-risk-census-extraction.md` |
| PP3coz--PP3cpb | Slotwise shell precancellation makes every incidence matrix indistinguishable; a nonzero residual and geometric incidence extraction are required | PROVED / PROVENANCE AUDIT | `docs/559-shell-incidence-nullspace-and-precancellation.md` |
| PP3cpc--PP3cpe | Coupled arithmetic closure carries the minimum evidence level of its rows; the stored cyclic system has exact fixed point but zero geometrically verified inputs | PROVED / CONDITIONAL CLOSURE AUDIT | `docs/560-evidence-closed-coupled-ledger-audit.md` |

## Frontier update

### Boundary recleaning

Six successive marker fixtures use five distinct length alphabets.  The
phase-locked four/seven catalogue first appears as a fixture, not as an extracted
boundary-controller block table.  The scheduler and `7/120` arithmetic remain
valid conditionally, while the required local point, port, seam, and signature
fields are now explicit.

### Localized Hall transport

The six-state transition table in the current adapter is generated from the
three-state quotient.  Balanced and diagonal two-state lifts are distinct,
regular, and strongly lumpable to the same kernels.  Exact quotient evolution
therefore does not identify microscopic geometry; an independent gadget census
is now the precise target.

### Fractional direct-clean layers

Actual threshold normals must factor through the scheduled quotient map as
`G=Lambda Q`.  A residual row outside the quotient row space is invisible to
every common-box certificate.  The stored facets achieve dual loss `1/32`, but
no physical quotient map or source inequality IDs are recorded.

### Support-chord repair words

The central profile count is exact.  The aggregate totals certify at least
`52566257495` objects below all four thresholds, but the totals are not linked to
an object-level risk enumerator.  The extraction contract now requires either a
canonical enumeration or an exact sufficient-statistic dynamic program.

### Clean-macro shells

The selected component phases cancel before applying the incidence matrix.
Consequently matrices of different dimensions and ranks all yield the same zero
physical trajectory.  A nonzero residual outside the common kernel is required
to identify the real physical incidence table.

### Integration

The stored coupling cycle has product `1/9216000000000`.  Its exact minimal fixed
point has total approximately `0.194671739175`, below the simple supersolution
`99/500`.  Arithmetic closure is therefore robust in the fixture, but the
machine-readable evidence meet reports zero geometrically verified rows.

## Exact diagnostics

Run the complete group with

```bash
python scripts/check_frontier_555_560.py
```

or individually with

```bash
python scripts/check_boundary_catalogue_provenance.py
python scripts/check_hall_microcensus_independence.py
python scripts/check_threshold_normal_provenance.py
python scripts/check_prefix_risk_census_provenance.py
python scripts/check_shell_incidence_provenance.py
python scripts/check_integration_evidence_closure.py
```

The shared machine-readable record is

```text
certificates/prime-patching-provenance-audit-555-560.json
```

The new audits verify six boundary lineage stages, two distinct Hall
microcensuses across 255 switch words, exact quotient-factorization and one
hidden-normal witness, the complete profile-risk arithmetic, four shell incidence
matrices indistinguishable on the cancelling fixture, and the exact six-row
coupling fixed point.

The next available theorem identifier is `PP3cpf`.
