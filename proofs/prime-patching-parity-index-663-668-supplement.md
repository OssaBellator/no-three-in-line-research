# Prime-patching parity index supplement: docs 663--668

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `663` | `PP3daz--PP3dbb` | Nine minimum-four sixteenth attempts, thirty-nine cores, a four-point correction to sixteen blocks, and the raw seventeenth spectrum | Finite corrected chain only |
| `664` | `PP3dbc--PP3dbe` | Exact overlap degree from motif-resource incidence, local Caro--Wei packing, and the two-stage Hall condition | Host coordinates and degree-two conditions absent |
| `665` | `PP3dbf--PP3dbh` | Integer separation of the source from all 4,475 legal endpoint matrices and global no-compensation theorem | Native matrix catalogue obstruction |
| `666` | `PP3dbi--PP3dbk` | Both two-pair deletions, all 288 optimal reroutings, all 294,912 rerouting/composition pairs, and exact coordinate-bound histograms | One canonical minimum-crossing matching only |
| `667` | `PP3dbl--PP3dbn` | Possible, robust, and impossible interval-cycle regimes plus exact setup repetition count | No coordinate macro interval graph |
| `668` | `PP3dbo--PP3dbq` | Candidate/evidence gate and unchanged fixed point | Zero promoted rows; all-`n` open |

## Conservation checks

- The sixteenth correction deletes and adds four points with identical row and
  column multisets.
- Hall overlap degrees are computed from shared resource incidence with duplicate
  intersections subtracted exactly.
- The threshold separator is additive, so every finite legal endpoint multiset
  retains a strictly positive displacement margin from the source.
- Every rerouted prefix anchor system remains a bijection on the surviving source
  cells; the greedy embeddings introduce no mixed-run collinear triple.
- Shell robust gain uses upper edge burdens and therefore holds uniformly over the
  full independent interval box.

## Promotion rule

The tranche advances finite coordinate evidence and exact impossibility and
robustness interfaces, but no result supplies a complete asymptotic coordinate
source path. Candidate completion remains `25/30`, all actual rows remain
`fixture_derived`, and the all-`n` theorem remains open.
