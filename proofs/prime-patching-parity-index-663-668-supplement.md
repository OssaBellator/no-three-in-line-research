# Prime-patching parity index supplement: docs 663--668

| Document | Theorems | Exact content | Evidence boundary |
|---|---|---|---|
| `663` | `PP3daz--PP3dbb` | Nine minimum-four sixteenth attempts, one certified four-point correction to sixteen blocks, complete raw seventeenth spectrum | Finite corrected chain only |
| `664` | `PP3dbc--PP3dbe` | Exact resource-incidence degree identity, local-load Caro--Wei bound, two-stage Hall promotion condition | Conditional host resources and centre conflicts absent |
| `665` | `PP3dbf--PP3dbh` | Integer separator for all 4,475 legal matrices, convex-hull exclusion, sharp nearest legal face | Global obstruction, not a legal primitive |
| `666` | `PP3dbi--PP3dbk` | One radius-four anchor repair, all 1,024 eleven-pair compositions, maximum coordinate 132 | One finite deletion case, no uniform recurrence |
| `667` | `PP3dbl--PP3dbn` | Possible and robust interval-cycle criteria plus exact worst-case repetition bound | No coordinate macro graph or burden intervals |
| `668` | `PP3dbo--PP3dbq` | Candidate/evidence gate and unchanged fixed point | Zero promoted rows; all-`n` open |

## Conservation checks

- The canonical sixteenth correction deletes and inserts four points with identical
  row and column multisets.
- The Hall local load counts every overlapping motif once per shared resource; the
  duplicate-overlap correction restores the exact simple-graph degree.
- The threshold separator is linear, so its nonnegative legal score is preserved
  by every convex combination and finite average.
- The prefix rerouting remains a bijection on the eleven surviving source rows and
  targets, changes exactly four assignments, and partitions all twenty-two source
  cells into anchors.
- Shell robust saving uses simultaneous upper burdens; possible saving uses
  simultaneous lower burdens. Both compare every edge with the same margin three.

## Promotion rule

The tranche contains one new finite coordinate lift, but no frontier supplies a
complete asymptotic coordinate path. Candidate completion remains `25/30`, all
actual rows remain `fixture_derived`, and the all-`n` theorem remains open.
