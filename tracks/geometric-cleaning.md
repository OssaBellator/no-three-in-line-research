# Scale-sensitive geometric cleaning and conflict regularization

**Branch:** `research/geometric-cleaning`

This track groups the unresolved geometric hypotheses behind S2, S5, L4 and P1. They form one dependency chain: partner density and pair-shadow control are needed to run local banks; stable local banks produce a clean/superregular completion host; candidate-only conflicts must then be regularized for the matching endpoint.

## Proved inputs

- S2: an active-coordinate shadow bound implies a near-complete superregular clone host;
- S4–S5: certified multicover deletion and sparse reverse-scale drift under a spread local bank;
- L1–L4: dense admissibility plus an anchor-load cap gives a simultaneous improving switch;
- P1: a full-product block state exists under small normalized pair/triple conflict mass;
- D1–D4: a near-complete clone host with low row/column triple load has an exact saturated solution.

## GC1 — Uniform admissible partner density

### Target statement

At dyadic height `H`, let `B` be a certified target batch in one permutation layer with `|B|<=cH`. Construct a partner pool `P` of size `Theta(n)` such that every target has at least `(1-delta)|P|` individually admissible partners, where admissibility means:

- the rectangle is collision-free between the two permutation layers;
- it creates no line defect of height at least `2H`;
- it respects all currently installed absorber and active-core blocks.

The constants `c,delta` must be uniform after every earlier switch at the same or higher scale.

## GC2 — Scale-sensitive anchor pair-shadow cap

### Target statement

Let `Z(B,P)` be the possible inserted cells generated by GC1. Prove that after deleting or absorbing at most `epsilon|B|` additional paid targets, every unchanged selected anchor `a` satisfies

\[
\lambda_H(a;Z)\le Cn,
\]

with `C` small enough for the L4 drift inequalities.

The cleaning process must produce one of:

1. the desired cap;
2. a paid endpoint-disjoint secant bank;
3. an algebraically structured core passed to the alternating-core branch.

Latent secant banks must be connected to actual destroyed syndrome weight; unweighted blocker banks are insufficient.

## GC3 — Intermediate-state stability

### Target statement

After any simultaneous switch supplied by L4, the next certified batch again admits GC1 and GC2 with the same constants, possibly after updating a bounded-size active reservoir. Prove:

- higher-height cleanliness remains invariant;
- active row and column deficits remain balanced;
- the number of newly exceptional anchors is charged to the potential decrease;
- the process cannot consume the partner pool faster than it destroys certified excess.

This closes the conditional batching conclusion in S5.

## GC4 — Candidate-only conflict regularization

### Target statement

For the final completion host or block product space, regularize candidate-only triples so that either:

1. maximum normalized pair/triple conflict mass per variable is below the P1 local-lemma threshold;
2. maximum row/column triple load is below the D1 threshold;
3. a dense candidate-only core has algebraic structure yielding an absorber or paid alternating bank.

The theorem must overcome the proved `Omega(n^4 log n)` total candidate-triple obstruction; it cannot rely on uniform independent pruning.

## GC5 — Clean-host endpoint theorem

### Target statement

Starting from a bounded-syndrome saturated seed and processing dyadic heights downward, GC1–GC4 produce one of:

- a zero-syndrome saturated configuration;
- a near-complete superregular clone host satisfying D1/D2;
- a product-state block host satisfying P1;
- a structured core delegated to the alternating-core branch.

Every delegation must carry paid incidence so the global potential decreases when the delegated repair returns.

## Quantities to track

- active-coordinate shadow `Q(X)`;
- anchor pair-shadow `lambda_H(a;Z)`;
- certified excess `Psi_H`;
- available partner degree and pool depletion;
- normalized block conflict mass;
- row/column residual triple load;
- syndrome weight transferred to structured cores.

## Required counterexamples

- translated absorber walls defeating synchronized states;
- cycle-like CSPs with bounded codegree but no private repair;
- states in which a large latent blocker bank destroys no current defect;
- intermediate switches that collapse partner density;
- candidate-only cores surviving all simple anchor deletion rules.

## Completion criterion

This branch is complete when GC1–GC5 remove the hypotheses from S2, S5, L4 and P1, with constants compatible with either the complete-clone endpoint or the superregular endpoint.