# All-n product track: effective repair and side-seven layered stage

**Branch:** `research/all-n-product-construction`

PX397--PX492 give an effective rectangle-label doubling reduction above
`10^2900`. PX493--PX575 isolate side seven, exhaust radius one, enumerate radius
two, and completely refute radius two in the `(3,2,2)`, `(4,3)`, and `(5,2)`
classes. PX576--PX591 enumerate the `(5,2)` radius-three layer and reject its
supports twelve through eighteen. PX592--PX598 begin an exact shared-state
attack on radius-three support twenty.

## Current ledger

| Item | Status | Current result |
|---|---|---|
| Effective closure root | **AUDITED** | PX492 closes the asymptotic numerical ledger at `N_3=10^2900`. |
| Side-seven factor census | **COMPLETE** | 132 saturated configurations, 488 ordered factors, four relative classes. |
| Direct side-six insertion recursion | **REFUTED** | All 21,952 inherited one-label extensions fail. |
| Fixed-selector coordinate repair | **REFUTED** | The four centre coordinate orbits fail in 2,227,923 exact CSP nodes. |
| Cross-half profile | **EXACT** | 926,852 top-clean pairs; 806,548 admit a separately clean bottom half; every union has a mixed-half triple. |
| Selector move graph | **CONNECTED** | Alternating cycles connect all 9,991,170 selectors with diameter at most fourteen. |
| Radius-one layer | **REFUTED** | All 1,748 selectors fail after 1,201,997,452 CSP nodes. |
| Radius-two selector layer | **ENUMERATED** | Exactly 989,303 selectors, generated by 45,477,868 directed parent moves. |
| Radius-two support 8--16 | **REFUTED** | 47,492 selectors fail through support sixteen. |
| `(3,2,2)` radius two | **COMPLETELY REFUTED** | All 4,433 selectors fail after 2,780,475,186 CSP nodes. |
| `(4,3)` radius two | **COMPLETELY REFUTED** | All 10,531 selectors fail after 9,442,860,381 CSP nodes. |
| `(5,2)` radius two | **COMPLETELY REFUTED** | All 26,550 selectors fail after 20,354,897,736 CSP nodes. |
| `(7)` radius two | **OPEN** | Supports through sixteen fail; support eighteen and above remain. |
| Radius-three selector layers | **ENUMERATED IN THREE CLASSES** | `(3,2,2)`: 77,026; `(4,3)`: 231,765; `(5,2)`: 468,452; total 777,243. |
| `(5,2)` radius-three supports 12--18 | **REFUTED** | All 43,336 selectors fail after 34,314,555,004 CSP nodes. |
| `(5,2)` radius-three support 20 | **ACTIVE CACHE FRONTIER** | 71,860 selectors initially; the largest 46-selector repeated top signature is now refuted, leaving 71,814. |
| Universal side-seven doubling | **OPEN** | No canonical class has yet produced a side-fourteen template. |
| Exact all-side closure | **OPEN** | No structural bridge covers every base below the cutoff. |

## Effective asymptotic constants

\[
T(N)=\lceil N^{3/5}\rceil,
\qquad
A_3=320,
\]

\[
\mathfrak d(N)\le10^{59}N^{16/109},
\qquad
N_3=10^{2900}.
\]

## Exact side-seven classes

| Relative type | Configurations | Ordered factors | Abstract selectors |
|---|---:|---:|---:|
| `(7)` | 60 | 120 | 1,323,522 |
| `(5,2)` | 32 | 128 | 2,269,620 |
| `(4,3)` | 20 | 80 | 1,975,428 |
| `(3,2,2)` | 20 | 160 | 4,422,600 |
| **Total** | **132** | **488** | **9,991,170** |

## Selector-coordinate search language

The two-column normal form is

\[
g_{ijs}=A_jH^sP^i,
\qquad
A_0=T,
\qquad
A_1=QT.
\]

After fixing the top row order, coordinate embedding uses

\[
A_0,A_1,R\in S_7.
\]

Alternating selected/unselected cycles preserve abstract degree two. Balanced
symmetric differences decompose into at most fourteen such cycles, yielding a
complete selector move graph.

The exact dangerous-point CSP rejects a branch precisely when a newly completed
selected point creates an integer-determinant zero with two completed points.
PX595 extends this to candidate families: a completed collinear host-edge triple
removes exactly the selectors containing all three edges.

## Radius-three support-twenty cache ledger

For the 71,860 `(5,2)` selectors at radius three and support twenty:

- the radius-two parent graph has 1,669,828 incidences;
- selector parent multiplicity ranges from 5 to 241;
- parent child degree ranges from 0 to 594;
- a deterministic greedy cover uses 699 radius-two parents;
- the first 100 greedy parents cover 43,726 selectors;
- there are 38,553 distinct top signatures and 38,553 bottom signatures;
- the maximum half-signature multiplicity is 46.

The largest repeated top signature has exactly 65,296 clean concatenated column
orders and 1,584 clean interleaved orders. A shared active-candidate bottom CSP
rejects all 46 selectors in all four orientations after 601,405 bottom-search
nodes. The active support-twenty count is therefore 71,814.

## Immediate frontier

1. **Remaining repeated top signatures.** Process support-twenty signatures in
   decreasing multiplicity, sharing each exact `(A_0,A_1)` top-order list.
2. **Parent-cover groups.** Compare the 699-parent cover with top-signature
   grouping; use the active-candidate recursion only where its coordinate tree
   is smaller than independent searches.
3. **Long seven-cycle class.** Continue radius-two support eighteen or construct
   a nonlocal host outside the current selector basins.
4. **Other completed classes.** Apply the same cache architecture to the
   radius-three support-twelve layers of `(3,2,2)` and `(4,3)`.
5. **Finite-range bridge.** Convert a successful side-seven or general-cycle
   theorem into arithmetic coverage below PX492.

## Verification

```bash
python scripts/verify_product_entry_invariant_dependencies.py

g++ -O3 -std=c++17 \
  scripts/verify_product_side_seven_cycle52_radius_three_support_twenty_reuse.cpp \
  -o /tmp/side7_c52_s20_reuse
/tmp/side7_c52_s20_reuse

g++ -O3 -std=c++17 \
  scripts/verify_product_side_seven_cycle52_radius_three_support_twenty_cache.cpp \
  -o /tmp/side7_c52_s20_cache
/tmp/side7_c52_s20_cache interleaved
for orientation in 0 2; do
  for shard in {0..15}; do
    /tmp/side7_c52_s20_cache concatenated "$orientation" "$shard"
  done
done
```

Every finite certificate uses exact integer determinants and deterministic
selector or top-order intervals. Exact all-side product closure and the
classical no-three-in-line conjecture remain open.
