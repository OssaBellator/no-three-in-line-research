# Exact recurrent physical-source gate progress

**Branch:** `research/exact-recurrent-lyapunov-audit`

The classical no-three-in-line conjecture remains open. This addendum records
only the first-host physical-source inventory and ingestion gate.

## ERL1r — exact physical source coverage

Seven installed artifacts were joined for host
`s4-75b04c45c1c8eac2` and deletion trace `{02,20}`:

```text
raw host-response projection
selected normalized provenance
exact dual-edge bridge
selected intrinsic line geometry
local reopening kernel
occupancy-moment upper certificate
installed operation registry
```

The sixteen-field worklist has the exact coverage census

```text
required physical fields       16
source-backed populated fields  0
unpopulated physical fields     16
physical occurrence records     0
```

Nearest evidence classes are

```text
normalized-only          5
schema-only              2
deletion-presence-only   2
intrinsic-geometry-only  1
catalog-only             1
local-candidate-only     1
upper-bound-only         1
none                     3
```

No surrogate class counts as a physical field. In particular:

- normalized fate `B` is not a physical ancestry record;
- line `x-y-1=0` is intrinsic response geometry, not physical ownership;
- registered strict dispatch is not a selected operation trace;
- local restorations are not installed intermediate states;
- `E2=69` is not a destroyed-load or parent-budget value.

## ERL1s — reject-by-default physical batch gate

A complete batch must provide

```text
source-defined physical occurrence domain
exact expected occurrence count
source-backed completeness proof
one unique fully sourced record per occurrence
```

Every record must preserve the dual-edge convention and populate physical
embedding, source trace, deletion causes, six provenance fields, complete legal
operations, complete intermediate states, exact child row, positive weights,
parent budget, and realized status.

Placeholder or surrogate source references are rejected. Empty operation,
state, or child enumerations are permitted only when an external source marks
their scope complete.

Current result:

```text
records                               0
accepted physical records             0
expected occurrence count known       0
schema-completion candidates rejected 2
batch complete                         0
promotion allowed                      0
```

Both dual-edge schema completions fail the physical gate before row analysis.

## ERL1t — chart-confinement source transfer

The five candidate responses occupy eleven of the sixteen standard side-four
chart cells. Their exact response-disjoint complement is

```text
{00,01,11,22,33}.
```

Therefore two source predicates

```text
background is confined to integer cells [0,3] x [0,3]
background is disjoint from the five-response union
```

force the background into the previously audited safe five-cell class. All 32
subsets were recomputed by direct triple counting, and every one has minimizer
face

```text
{2031,2310,3201}.
```

Under these predicates, neither a strict original-response reversal nor an
original-response tie is possible. All four global strict backgrounds violate
chart confinement. Their exact coordinate-cap census is

```text
L_infinity cap 4: 0 strict backgrounds
L_infinity cap 5: 2 strict backgrounds
L_infinity cap 6: 4 strict backgrounds.
```

The transfer is conditional: no current physical source proves chart
confinement or response disjointness.

## Executable artifacts

```text
scripts/check_exact_recurrent_first_host_physical_source_coverage.py
data/exact_recurrent_first_host_physical_source_coverage.json
docs/exact-recurrent-first-host-physical-source-coverage.md

scripts/check_exact_recurrent_first_host_physical_fibre_batch_gate.py
data/exact_recurrent_first_host_physical_fibre_batch_gate.json
docs/exact-recurrent-first-host-physical-fibre-batch-gate.md

scripts/check_exact_recurrent_first_host_chart_confinement_transfer.py
data/exact_recurrent_first_host_chart_confinement_transfer.json
docs/exact-recurrent-first-host-chart-confinement-transfer.md

.github/workflows/exact-recurrent-first-host-physical-fibre-gate.yml
```

## Next admissible result

The next proof-relevant artifact must be one of:

1. a source-backed proof that no physical `{02,20}` occurrence exists;
2. a complete source-backed batch of all such occurrences;
3. a partial batch explicitly marked incomplete, together with a source-defined
   occurrence domain and a quantified remaining-record worklist;
4. a source-backed proof of chart confinement and response disjointness for all
   occurrences, which would close the complete-score selector obstruction but
   not the remaining lineage and row fields.

Only after the gate accepts a complete batch may the branch compile exact child
rows or test strict Lyapunov inequalities for this host.

Physical realization, physical exclusion, chart-confinement provenance,
recurrent rows, strict Lyapunov slack, global termination, all-side transfer,
and `all_n_proved_by_checker` remain zero.
