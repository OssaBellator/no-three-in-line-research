# Exact recurrent evidence-scope overlap progress

## ERL2z — certificate-instance scope audit

The hybrid source-certificate antichain now has a dependency-aware evidence audit.

```text
minimal label patterns                     51
minimal menu patterns                     100
route classes                               5
```

Every minimal pattern repeats at least one field-name string. Repetition does not authorize de-duplication because each field is scoped to one state, family, or directed-edge certificate instance.

Cross-class lexical overlap is exactly:

```text
realization_status
```

No accepted theorem proves that one realization fact uniformly satisfies several certificate instances.

Current safe floors:

```text
label scalar                               11 scoped slots
menu scalar                                12 scoped slots
```

Unsafe physical-exclusion lexical floors:

```text
label scalar                                5 field-name strings
menu scalar                                 5 field-name strings
```

The five-name counts arise by collapsing repeated exact-edge route schemas and are not admissible proof burdens.

Maximum unsafe lexical savings:

```text
route                              label    menu
physical exclusion                   10      15
decorated outer reset                16      24
finite unrestorable capacity         18      27
terminal or improving output         20      30
bounded strict potential             22      33
```

A source-sharing quotient requires six explicit fields: certificate-instance domain, field-equivalence map, uniformity theorem, scope preservation, local-obligation preservation, and realization status.

```text
accepted sharing theorems                    0
populated sharing fields                     0
physical certificates                        0
```

Artifacts:

```text
scripts/check_exact_recurrent_first_host_evidence_scope_overlap_obstruction.py
data/exact_recurrent_first_host_evidence_scope_overlap_obstruction.json
docs/exact-recurrent-first-host-evidence-scope-overlap-obstruction.md
docs/ERL_EVIDENCE_SCOPE_OVERLAP_REVIEW_GATE.md
```

Physical occurrence coverage, legal transitions, recurrent child rows, strict Lyapunov closure, global termination, and `all_n_proved_by_checker` remain zero.
