# All-n product construction: current frontier map

**Branch:** `research/all-n-product-construction`  
**Reconciled:** 2 August 2026

This map records the active frontiers through PX1224. It is not a proof of the classical conjecture or an all-side product theorem.

## Effective asymptotic repair

The paired path is effective for `N>=10^2874`. At decimal order `2873`, the unchanged-inequality improvement threshold lies between `1.0979139` and `1.0979151`. A 9% improvement is insufficient through exponent `2873.0585` and guaranteed sufficient from `2873.0616` onward.

## Side-seven finite census

All support-twenty selectors of multiplicity at least three are exact. Multiplicity-two cases `0--1439` are exact. The committed cache contains `40,479` certified-infeasible selectors, two constructive selectors, `31,379` unclassified selectors, and `3,297,611,555` certified rejection-CSP nodes.

The unresolved set is exactly `2,400` multiplicity-two signatures (`4,800` selectors) plus all `26,579` multiplicity-one selectors. The next canonical multiplicity-two case is `1440`. Cases `1440--1449` are registered and uncounted.

## Certificate compression

For multiplicity-two case zero, orientation three, the expanded 208-reference vocabulary has 165 relaxed keys and covers 221 of 35,112 clean top orders. The 115-key basis for the original 204-top union has digest `12529763722981785837`; the 16-reference expansion has digest `16150749401146711547`. Exactly `34,891` clean top orders remain uncovered.

**Next:** continue semantic reference generation and recompute a compact irredundant basis for the 221-top union.

## Constructive-template analysis

Multiplicity-two case `1287`, selector zero, orientation zero is constructive and independently verified.

**Next:** compute its stabilizer, symmetry orbit, and normalized local-pattern inventory.

## Produced-base recursion

The opposite-pair double coset is completely obstructed in `cc` and `cf`. Fine-row pair indices `0--6799` are obstructed in both remaining orientations:

- `fc`: 6,800 geometries, `232,466,543` nodes, maximum `1,877,339`;
- `ff`: 6,800 geometries, `150,194,835` nodes, maximum `909,040`.

No constructive witness appears in these 13,600 fine-row geometries. Pair indices `6800--7199` are registered and uncounted.

## Remaining proof fronts

- Prove the decimal-2873 interval-specific improvement or reduce effective divisor loss.
- Prove cubic protected residual entropy and bounded rank-three completion multiplicity.
- Prove a global bounded-barrier repair or resampling theorem.
- Prove second-generation collateral concentration, monotone carry termination, or bounded-denominator absorption.

## Operational priorities

1. Promote side-seven cases `1440--1449` only after exact transcript replay.
2. Promote side-ten intervals `6800--7199` only after exact transcript replay.
3. Continue semantic expansion and case-`1287` template analysis.
4. Keep `STATUS.md`, `AUTOPROMPTER_HANDOFF.md`, the manifest, and checker synchronized with certified—not merely registered—boundaries.
