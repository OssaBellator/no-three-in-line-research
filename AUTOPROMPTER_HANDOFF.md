# Autoprompter continuity handoff

## Current goal

Continue the prime-power/composite-modulus construction in literal source order while preserving exact ownership, validation and honesty boundaries. The active phase is to turn source CMR1318--CMR1389 into durable inherited-coordinate diagonal-block ancestry, then extend the canonical installed registry and chained runner without claiming the missing global subcriticality theorem.

## Repository and branch

```text
repository = OssaBellator/no-three-in-line-research
branch = research/all-n-composite-modulus
authoritative installed theorem endpoint = CMR4037
```

The classical no-three-in-line conjecture remains open. Every checker and manifest must preserve:

```text
all_n_proved_by_checker = 0
```

## Canonical committed stack

```text
552 unique construction operation kinds
32 exact checker contracts
159 owner-changing kinds
393 same-owner kinds
57 installed checkers
```

Canonical runner and manifest:

```text
python scripts/run_prime_power_installed_construction_regression_552.py
aab192d2cd4682e51be31a09285141377b5786d231006950a898f36cf43b39fd
```

Canonical registry:

```text
contract = 77670b377285d73441fd7c19cdd83a115ce658ef263098f45c73a633f352d232
seal = 890827174d7c40a37b23c8c5ccba673ddd3ab39a7e3ae7cef4f88df5c1b0387b
```

Latest installed theorem ranges:

```text
CMR3894--3941 universal dirty-owner descent, exact small bases,
                 lifted target ancestry and finite-response nonclosure
CMR3942--3989 degree-two collateral banks, corrected line energy,
                 credit ledgers and rational spectral certificates
CMR3990--4037 full-grid sides three through six, extension-free
                 response families and the exact side-six trap class
```

## Completed work in the current phase

### Consolidated CMR1318--CMR1389 checker

Committed and remotely verified:

```text
commit = 51d49fd6b5de703280bd4e048e2a372c8290d76d
checker = scripts/check_prime_power_inherited_coordinate_diagonal_block_ancestry.py
contract = bff27495eef9188b3d90e5f36ee66ed889b0606c7fa92f8b3391a3114e03d826
```

The checker binds and executes the nine authoritative verifier modules for:

```text
last-entering structural owner persistence and owner-DAG triangularity
exact state-credit rows and componentwise host-uniform upper quotients
exact/dyadic line-profile classes and pair-moment envelopes
extension-free permanent and exact derangement marginals
line-composition kernels and their explicit side-five obstruction
exact row-column cylinder probabilities for created and destroyed credits
cross-line edge-selector, fractional matching and deterministic policy normal forms
```

The checker explicitly preserves:

```text
same_owner_diagonal_blocks_subcritical = 0
independent_line_kernel_sufficient = 0
global_target_collateral_inequality_proved = 0
all_n_proved_by_checker = 0
```

Local validation completed for this unit:

```text
checker source compiles
contract digest reproduced exactly
```

The complete nine-verifier consolidated execution has not yet been run in the current local environment.

## Decisions that must be preserved

1. **Installed exhaustiveness is not global exhaustiveness.** Registry flags apply only to the installed operation bank.
2. **Finite response is not minimum-zero closure.** A finite bank, scheduler or manifest does not prove the conjecture.
3. **Coordinate scope is literal.** Side-three through side-six potential tables apply only to the complete standard grid or a verified common affine image, not arbitrary scattered residual factors.
4. **One-layer target banks are not globally sufficient.** The exact side-six trap class remains a counterexample to that strategy.
5. **Last-entering physical ownership is the no-double-counting rule.** Fixed interfaces and later contractions do not receive duplicate collateral credit.
6. **The owner DAG is structural.** Same-owner repeated bank responses remain inside diagonal blocks; only strict child, wall, normalized-host or later-envelope exits move forward.
7. **Spectral reductions are not spectral certificates.** Block triangularity, exact quotients and rational certificate schemas do not prove `rho < 1` until actual host-uniform same-owner rows and strict weights are supplied.
8. **Independent line kernels are insufficient.** The realizable side-five obstruction from CMR1372 must remain in the executable bank.
9. **Cross-line assignment is a policy normal form, not global closure.** The selector/fractional identity proves exact optimization but does not construct a uniform all-owner selector.
10. **Blocked response rows use exact structural descent.** Complete blockage routes through the established minimal deficiency-one unit-wall mechanism.
11. **All global honesty flags remain zero unless directly proved.**

## Blockers

The active mathematical blocker is a globally valid target-versus-collateral inequality, or equivalently a host-uniform subcritical upper offspring matrix, for every canonical inherited-coordinate dirty owner.

Specific unresolved points:

```text
same-owner diagonal blocks need honest host-uniform offspring bounds
cross-line response-edge correlation must be controlled uniformly
primitive-height, prefix, quotient/carry and CRT classes need quantitative bounds
blocked rows must glue to strict unit-wall descendants without reverse ownership
all original construction actions still need a global transition-kind audit
weighted descent and structural descent are not yet combined into global termination
```

## Validation boundary

The earlier collateral/spectral checker and the 525- and 552-kind registry validators were executed locally and passed. The finite-grid wrapper compiles, but its complete inherited side-five and side-six exhaustive modules were not executed locally. The complete 55- and 57-checker repository runners were not executed locally.

For the new CMR1318--CMR1389 unit, source compilation and contract hashing passed locally. The consolidated nine-verifier checker has not yet been executed end to end. Workflow configuration is not CI success, and no successful workflow run is currently claimed.

## Uncommitted work

```text
uncommitted repository files = none known
uncommitted generated artifacts = none known
```

The next registry and runner are designed but not yet committed. No completed implementation exists only in chat.

## Exact next steps

```text
1. Build and locally execute the installed operation registry extending 552 kinds.
2. Register only genuine CMR1318--CMR1389 owner, quotient, probability,
   policy-selection, certificate and blocked-wall operations.
3. Commit and remotely verify the registry before starting the runner.
4. Extend the 57-checker manifest by the new checker and registry.
5. Add docs/527--529 and live continuations 93--95.
6. Add dedicated Python 3.10/3.12 checker, registry and runner workflows.
7. Synchronize STATUS.md, AUTOPROMPTER_HANDOFF.md and docs/11-open-bottlenecks.md.
8. Continue afterward through candidate-transversal Hall walls, rook-owner
   weights, cross-line owner assignment, harmonic bounds and lattice-capacity
   owner bounds.
```

The success criterion for this phase is a durable executable ancestry and honest registry/manifest extension. It is not an all-`n` proof and not yet the missing strict same-owner spectral certificate.
