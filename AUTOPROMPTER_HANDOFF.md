# Autoprompter continuity handoff

Checkpoint time: 2026-08-01 15:49 Australia/Melbourne

## Goal

Develop a rigorous all-`n` prime-patching route for the no-three-in-line research
program by advancing six linked frontiers:

1. boundary recleaning / marker-controller realization;
2. localized Hall transport and list decoding;
3. fractional direct-clean threshold layers;
4. support-chord repair words and constrained prefix codes;
5. clean-macro shell attenuation and scheduling;
6. global interaction/integration certificates.

The asymptotic all-`n` theorem remains open. Existing results are finite
reductions, exact certificates, conditional mechanisms, bounded corrected chains,
source-operation reductions, and explicit auxiliary sources; they must not be
described as a completed proof.

## Current branch

- Repository: `OssaBellator/no-three-in-line-research`
- Branch: `research/all-n-prime-patching`
- Verified pre-checkpoint research head: `09899d953867c1a1344e264312bdda4eefb0a581`.
- Pre-checkpoint head title: `Restore canonical evidence-gate chapter 626`.
- The branch was remotely verified identical to that commit before this refresh.
- Next available theorem identifier: `PP3cwv`.

## Completed work

The branch contains the cumulative six-frontier sequence through `docs/626`. The
latest canonical tranche is `docs/621--626`:

- `docs/621-budget-seven-corrected-boundary-chain.md`
  - Theorems `PP3cwd--PP3cwf`.
  - From the corrected seven-block state, seven of 520 eighth attempts have
    transversal number at most five, with twenty-one minimum transversals.
  - One seven-point degree-preserving correction produces a legal sixty-four-
    point eight-block state.
  - From that state, sixteen ninth attempts have transversal number at most five,
    with 120 minimum transversals.
  - One seven-point correction produces a legal seventy-two-point nine-block
    state.
  - At the tenth step only `(P2,-32)` and `(P3,-32)` have minimum transversal at
    most five; neither has a degree-preserving correction within budget seven.

- `docs/622-sharp-three-matching-hall-reserve-threshold.md`
  - Theorems `PP3cwg--PP3cwi`.
  - In residual `K_{6,6}`, deleting three partial matchings and one arbitrary edge
    always leaves a perfect matching.
  - Five residual resources are insufficient, witnessed by a forbidden `K_{3,3}`
    decomposed into three matchings.
  - The exact finite conditional interface therefore requires eight resources per
    side before a local pair consumes two, with partner, source, and host-defect
    restrictions all matching-shaped.
  - Auxiliary bad-vertex and bounded-degree checkers give sufficient larger-pool
    extraction conditions; these do not replace the sharp six-resource theorem.

- `docs/623-atomic-two-swap-generation-of-threshold-trades.md`
  - Theorems `PP3cwj--PP3cwl`.
  - Every one of the eight nearest legal degree-four targets has exactly six
    ordered length-two conservative-swap paths.
  - There are forty-eight paths using twenty distinct nonnegative,
    margin-preserving intermediate matrices.
  - None of the twenty intermediates is decomposable into four legal
    no-three-in-line layers.
  - The fifth threshold candidate field is complete at matrix-source level; the
    remaining geometric obligation is one atomic two-swap compound.

- `docs/624-retained-anchor-reservoir-price.md`
  - Theorems `PP3cwm--PP3cwo`.
  - The exact maximal-unary-run histogram has aggregate `1212286655580`, mean
    `209/29`.
  - Two retained anchors per run require aggregate reservoir `2424573311160`,
    mean `418/29`, and worst case twenty-two.
  - A one-cell-one-anchor rule using the nineteen non-unary cells fails on
    `4858898044` trees, proportion `289/10005`.
  - An explicit integer-parabola source realizes all 1,024 run compositions with
    distinct rows and columns, zero cross-run triples, and maximum coordinate
    magnitude `40802`.
  - One deleted anchor per run clears every anchor-pair blocker on that run line.

- `docs/625-binary-odd-shell-column-frontier.md`
  - Theorems `PP3cwp--PP3cwr`.
  - Among all feasible nonnegative odd columns of `l_1` norm at most three,
    `(1,1,1)` is uniquely optimal for target service `(12,10,8)`.
  - The nondominated `(uses,total active controls)` frontier is
    `(2,14)`, `(4,13)`, and `(6,12)`.
  - The optimum identity is `2A+4B+6D=(12,10,8)`, using twelve active controls
    and eight idle slots instead of the recorded fifteen active controls.
  - The word `DABABBBDDDDDIIIIIIII` is feasible at every prefix with zero startup
    buffer.
  - No geometric all-cycle clean macro realizing `D=(1,1,1)` is known.

- `docs/626-repeated-transition-source-generation-evidence-gate.md`
  - Theorems `PP3cws--PP3cwu`.
  - Candidate field completion rises from `24/30` to `25/30` because the
    threshold matrix-generation source path is complete.
  - The fixture fixed-point total remains
    `705466760524005697/3623878655999606784`, with positive slack
    `200502903475895999/3623878655999606784`.
  - All six actual rows remain `fixture_derived`; zero rows are promoted and
    geometric closure remains false.

Machine-readable record:

- `certificates/prime-patching-repeated-transitions-621-626.json`

Reproducibility files:

- `scripts/check_boundary_eighth_corrected_transition.py`
- `scripts/check_boundary_budget_seven_chain.py`
- `scripts/check_hall_bad_vertex_reserve.py`
- `scripts/check_hall_quantitative_reserve_extraction.py`
- `scripts/check_hall_three_matching_reserve_threshold.py`
- `scripts/check_threshold_two_swap_generation.py`
- `scripts/check_prefix_retained_anchor_reservoir.py`
- `scripts/check_prefix_parabola_source_anchors.py`
- `scripts/check_shell_binary_odd_column_frontier.py`
- `scripts/check_repeated_transition_source_generation_gate.py`
- `scripts/check_frontier_621_626.py`
- `proofs/prime-patching-parity-index-621-626-supplement.md`

The latest combined validation command is:

```bash
python scripts/check_frontier_621_626.py
```

The exact standalone audits underlying the canonical chapters and Python
compilation passed in the local execution runtime. The boundary budget-seven
checker reaches nine blocks and certifies the tenth-step stop; the Hall,
threshold, prefix, shell, and evidence audits all returned `status: passed`.
The complete historical chained runner was not re-executed locally because the
isolated runtime does not contain a full repository checkout; the group runner
invokes `scripts/check_frontier_615_620.py` first.

The interrupted response produced overlapping commits. The canonical theorem
sequence, certificate, supplement, runner, and chapter paths have now been
consolidated; alternate finite extraction checkers are retained only as auxiliary
diagnostics.

## Decisions and conventions

- Continue on `research/all-n-prime-patching`.
- Use sequential, reviewable commits and exact rational or integer arithmetic.
- Continue theorem numbering from `PP3cwv`.
- Keep the six-frontier organization stable.
- Every tranche must include a stored certificate and checker.
- State explicitly that the all-`n` theorem remains open.
- Do not replace proof obligations with bounded computation.
- Track candidate completion separately from actual geometric evidence.
- Treat finite corrected chains, conditional reserve lemmas, matrix-level source
  paths, and explicit auxiliary sources as intermediate evidence, not verified
  global rows.
- Negative results and minimal counterexamples are valid frontier progress.

## Current blockers

- Boundary: the budget-seven corrected chain reaches nine blocks but has no tenth
  transition. A larger correction budget, wider offset range, noncanonical core,
  or periodic corrected-state component is missing.
- Hall: the sharp theorem assumes six unused resources and three matching-shaped
  restricted forbidden families. The asymptotic conditional host has not been
  shown to supply that structure.
- Threshold: every target has a repository-native two-swap source path, but each
  visible intermediate is illegal. No geometric atomic compound is known.
- Prefix: the parabola source realizes the incidence model but is not the
  two-per-row/two-per-column saturated PP3 retained source; the internal reservoir
  fails on a positive fraction.
- Shell: `(1,1,1)` has exact throughput and zero-buffer benefits but no geometric
  clean-macro realization or collateral-interaction bound.
- Integration: all direct rows and coupling coefficients remain fixture-derived;
  finite small lengths remain downstream of genuine realization.

## Uncommitted work

- No completed theorem, script, certificate, supplement, or continuity change is
  left uncommitted.
- The research head was remotely verified before this handoff refresh.
- The repository connector cannot inspect unrelated external local clones.

## Exact next steps

1. Fetch this handoff and verify the branch head.
2. Start theorem numbering at `PP3cwv`.
3. Build `docs/627--632` around escaping the boundary budget stop and realizing
   actual geometric source operations.
4. Boundary: enumerate tenth-step corrections at budget eight over every minimum
   core; if the graph still terminates, widen offsets and search for a corrected-
   state cycle or a different block catalogue.
5. Hall: derive six unused resources and three matching-shaped restricted
   forbidden families from one asymptotic superregular host, using the auxiliary
   bad-vertex/degree extraction bounds when useful.
6. Threshold: realize one atomic two-swap compound on actual source cells while
   keeping every exposed geometric state legal, or prove a source invariant
   excluding all recorded operations.
7. Prefix: embed the parabola anchor reservoir into a saturated PP3 retained
   source and charge the exact occupancy and one-deletion-per-run credit.
8. Shell: construct an all-cycle clean macro with incidence `(1,1,1)` and audit
   its zero-buffer schedule, collateral interactions, and unit-cost assumption.
9. Integration: promote only rows with complete coordinate source paths;
   otherwise preserve the closed evidence gate and unchanged fixture fixed point.
10. Run all diagnostics, Python compilation, the chained runner in a complete
    checkout, verify the remote head, and refresh this handoff.
