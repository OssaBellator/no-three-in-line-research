# ERL selector-face closure leverage review gate

This gate applies to any claim that minimizer-face congruence resolves the restored-menu selector boundary or simplifies the recurrent transition graph.

## Tie-break substitution

A complete menu-local substitute for the absent lexicographic tie-break must prove all three response-pair types:

```text
2031 ~ 2301
2031 ~ 2310
2031 ~ 3201.
```

Review must reject:

- `2031~2310` alone as a restore-both proof; it covers only the 32 `restore_02` cases;
- `2031~2310` plus `2031~3201` as complete; it covers 56 cases and leaves eight four-way faces;
- `2031~2301` alone as useful coverage; it is only the final-eight bottleneck after the other pairs;
- any claim of tie-break substitution while the source-import gate accepts zero proof modes.

## No automatic state quotient

Face congruence is menu-local. It does not identify selected `3201` in state `01` with selected `2031` in states `10` and `11`.

A selected-state quotient requires a separate theorem carrying all ten fields in the closure-leverage manifest. In particular it must quantify over the distinct legal menu states, use one physical occurrence domain, preserve one persistent owner, and prove operation, child-row, payment, and closure-route congruence.

Review must reject any graph quotient inferred only from:

```text
equal complete score
membership in one minimizer face
one response-pair spelling
one operation-signature class
a menu-local face theorem.
```

## Scalar consequence

Without the additional selected-state theorem, all existing counts remain:

```text
four menu states
eight directed menu edges
six changing and two neutral edges
six label-scalar route covers
fourteen menu-scalar route covers.
```

Even with the hypothetical global quotient, the total menu edges requiring a non-label route become six rather than the current five. It is therefore not a closure shortcut.

## Current endpoint

```text
accepted pair types                         0 of 3
source-discharged tied cases                0 of 64
global selected-state quotient fields       0 of 10
selected-state quotient accepted            0
promotion to recurrent closure              0
all_n_proved_by_checker                     0.
```

No review item authorizes physical restoration, selector substitution, transition-graph reduction, strict Lyapunov closure, global termination, or an all-n claim.
