# Realization-compensation evidence gate

This chapter integrates `docs/693--699` under the strict promotion rule used by
the preceding tranches: a row is promoted only by a complete coordinate source
path with exposed-state legality and recurrent or asymptotic continuation.

## PP3dfg — Current frontier advances

The tranche adds five exact advances.

1. **Boundary.** Reconstructing the canonical eighteenth state corrects the stale
   point `(42,193)` to `(42,378)`. Sharp finite corrections advance the chain to
   twenty-one blocks and 168 points. The raw twenty-second minimum is six, with 26
   minimum attempts and 178 cores; all 68,580 minimum-budget preserving
   replacements are rejected.
2. **Hall.** Every finite simple maximum-degree-two defect graph has an integer
   no-three realization whose row/column conflict graph is its line graph. An
   explicit synthetic ten-motif packet retains 28 of 30 centres, and a repeated
   chain has exact retention `27K+1`; coordinate chains through six packets are
   certified. Separately, all 1,636 two-motif degree-two packet graphs have exact
   transfer audits.
3. **Threshold.** The separator applies at every rolling width. For `K` minimum
   batches and width `w`, the exact maximum number of legal cyclic windows is
   `max(0,5K-w+1)`. Wider rolling memory cannot improve the fixed-width asymptotic
   density beyond `5/8`.
4. **Prefix.** Exact route deduplication leaves 3,624 optimal routes per deletion
   class. Every distinct route passes all 176 compositions with at most four runs,
   covering 5,271,552 physical route/composition pairs with zero failures.
5. **Shell.** Connector tours must retain their full uncertainty burden vectors.
   Statewise shortest tours, edgewise worst charges, and worst-loss tour selection
   can all be suboptimal for the exact integer repetition objective.

These results strengthen a finite coordinate chain, a synthetic coordinate Hall
packet, an all-width obstruction, a bounded coordinate lift, and a robust
execution interface. None supplies an all-length prime-patching construction.

## PP3dfh — Candidate ledger remains `25/30`

```text
boundary     4/5
Hall         4/5
threshold    5/5
prefix       5/5
shell        5/5
integration  2/5
----------------
total       25/30
```

All six actual evidence rows remain `fixture_derived`, and the promoted set is
empty.

- Boundary reaches twenty-one blocks but has no recurrence or periodic state
  invariant. Budget seven on the twenty-second frontier remains open.
- Hall has an explicit repeatable synthetic coordinate packet, but its motif
  resources and defect labels are designed rather than extracted from the
  prime-patching host.
- Threshold closes every rolling width inside the forced minimum alphabet, but no
  genuinely unexposed operation or enlarged alphabet is constructed.
- Prefix covers every optimal route only through four runs on one fixed source;
  848 longer compositions and all larger source sizes remain open.
- Shell has the exact robust vector tour objective, but no coordinate macro graph
  supplies its components, path-burden vectors, and gain vectors.

Therefore geometric closure remains false.

## PP3dfi — Fixed point and promotion gate remain unchanged

The exact fixture total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

No coupling coefficient changes because no result in this tranche supplies a
complete recurrent or asymptotic coordinate source path derived from the actual
prime-patching host. The all-`n` theorem remains open, and the next theorem
identifier is `PP3dfj`.

The exact gate is `scripts/check_realization_compensation_gate.py`, and the chained
audit is `scripts/check_frontier_693_700.py`.
