# Protected selector certificates have exact deletion, batching and owner-labelled ancestry

This chapter records **CMR3426--CMR3445** and installs CMR577--CMR604 as literal construction ancestry beyond the narrower CMR577--CMR592 checkpoint.

Executable checker:

```text
scripts/check_prime_power_selector_protected_certificate_ancestry.py
```

## CMR3426 — literal disjoint-conflict host

The checker exhausts matchable side-three bipartite hosts and edge-disjoint compatible three-edge prescriptions. Every prescription is represented by its literal physical edges.

## CMR3427 — matching-preserving deletion pass

For each packed prescription, the lexicographically first nonessential edge is deleted. The child host is regenerated and required to retain a perfect matching.

## CMR3428 — fully forced terminal alternative

If no edge is deletable, all three edges lie in the current essential core. Essentiality persists under the remainder of the deletion pass.

## CMR3429 — essential-core packing bound

The final essential core is regenerated and checked to be a matching. Fully forced edge-disjoint triples consume three distinct core edges and satisfy the exact `floor(t/3)` capacity bound.

## CMR3430 — private deleted-edge code and restoration payment

Every killed conflict receives one private physical deleted edge. Exhaustive restoration subsets verify that recreation requires that same edge, so `s` recreated conflicts require `s` distinct restored edges.

## CMR3431 — protected contact universe

For every nonempty partial matching of side four, the checker forms the canonical protected extension and exact allowed universe. Every blocked allowed edge meets a protected source or target vertex.

## CMR3432 — finite contact stock

At protected size `k`, the blocked physical-edge universe is checked against `2k(n-1)`. One physical edge receives one or two exact source/target contact labels.

## CMR3433 — protected wall extraction

The protected vertices cover every distinct blocked edge. The checker extracts a row or column wall of degree at least the ceiling of the support divided by `2k`.

## CMR3434 — contact token splice

Each protected wall is partitioned into prefix cells. The heavy-token or dispersed-token alternative is checked with the square-root threshold.

## CMR3435 — recurrent unavailable-set extraction

Inside one fixed protected selector universe, every nonempty unavailable set is treated as a literal recurrent-core candidate. The finite subset-stock inequality is evaluated for a sample recurrence threshold.

## CMR3436 — batch absorption

The unprotected part of each persistent set is decomposed by maximum matching. A two-edge matching gives a compatible batch which strictly enlarges the protected matching.

## CMR3437 — small-cover persistent wall

When the batch matching is too small, an exact minimum vertex cover is generated. Together with the protected vertices it covers the whole persistent set and yields the stated row/column wall degree.

## CMR3438 — aggregate reintroduction or joint persistence

All selected common-absence histories of a fixed edge set obey the multi-edge absence-run bound. Failure of aggregate return payment yields one jointly persistent unavailable core.

## CMR3439 — bulk-growth depth

Every successful batch adds exactly its batch size to the protected matching. The sample side-four chase attains the exact finite depth bound.

## CMR3440 — weak-slack conversion

For `q=4,...,8` and `n=5,...,10`, the threshold-one selector lower bound is checked against `77/480`. Small unavailable thresholds are converted into explicit near-static collateral lower bounds.

## CMR3441 — conditioned persistent-core amplification

Conditional subset-counting formulas are evaluated after fixing one persistent edge. The checker verifies finite conditioned histories or enlargement to a chosen target-rank persistent core.

## CMR3442 — owner-labelled state stock

For sample `p=2,h=3,t=8`:

```text
selector signatures = 1,618,176
protected states = 14,563,584
```

## CMR3443 — owned line and token stock

The same sample gives:

```text
owned line signatures = 29,360,185,344
owned labelled token-edge pairs = 4,893,364,224
```

Toy episode families attain the finite owned-line and owned-token recurrence inequalities.

## CMR3444 — exhaustive finite census and corruption rejection

The report includes:

```text
434 disjoint-conflict packings
486 processed packed conflicts
324 matching-preserving deletions
162 fully forced packed conflicts
802 restoration subsets
414 recreated-conflict incidences
208 protected contact states
2,256 blocked contact-edge incidences
4,095 persistent unavailable-set profiles
3,264 batch-absorption profiles
831 small-cover wall profiles
120 slack parameter checks
8 rejected corruptions
```

Contract:

```text
b82d83290aa95e41743fe1db9dbbf3a26c09801f40888e42cc1c8e0f0eefd0f0
```

## CMR3445 — T02 consequence and honesty boundary

```text
disjoint_conflict_deletion_ancestry_proved = 1
protected_contact_token_ledger_exact = 1
dynamic_selector_recurrent_set_batching_exact = 1
selector_slack_persistent_core_exact = 1
owned_certificate_stock_exact = 1
selector_protected_certificate_ancestry_proved = 1

global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

Heavy-line, secant-star, protected-absorption and interface execution operations beginning at CMR605 remain to be installed. No all-`n` theorem is claimed.
