# Structural-enlargement evidence gate

The five preceding chapters enlarge the finite models rather than relabel the
obstructed fixtures.  This chapter records exactly what improved and prevents
candidate enlargements from being promoted as geometric closure.

### Theorem PP3cty -- PROVED / ENLARGEMENT OUTCOME VECTOR

The exact outcomes are:

```text
boundary: 2112 widened degree-preserving swaps fail;
Hall: every 4 x 5 label injection has minimum blocker number two;
threshold: one unique source augmentation has 120 legal five-layer decompositions;
prefix: repeated-chord aggregate is 925166131890;
shell: the recorded source catalogue has no odd-sum action.
```

#### Proof

Each entry is the asserted output of one independently executable checker in
`docs/597--601`. ∎

### Theorem PP3ctz -- PROVED / UNCHANGED SOURCE-FIELD COUNT

Candidate source-field completion remains `23/30`.

#### Proof

The Hall and threshold results enlarge candidate hosts but do not identify them
with actual endpoint or threshold sources.  The boundary corrector still fails,
the prefix model uses coincident chords, and the shell odd action is absent.
Thus no previously missing source coordinate becomes complete. ∎

### Theorem PP3cua -- PROVED / CLOSED GEOMETRIC EVIDENCE GATE

All six actual ledger rows remain `fixture_derived`, zero rows are promoted, and
geometric closure remains false.  The fixture fixed-point total remains

```text
705466760524005697 / 3623878655999606784
```

with positive slack

```text
200502903475895999 / 3623878655999606784
```

below one quarter.

#### Proof

The evidence meet requires a complete coordinate source path for every promoted
row.  None is supplied here.  Exact rational subtraction gives the displayed
slack. ∎

Machine-readable record:

```text
certificates/prime-patching-structural-enlargements-597-602.json
```

Run:

```bash
python scripts/check_structural_enlargement_evidence_gate.py
```
