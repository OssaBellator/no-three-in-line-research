# Adaptive unavailable-edge absorption, token splicing and temporal scheduling are exact operations

This chapter records CMR3258--CMR3275 and installs CMR507--CMR521 as literal construction ancestry.

Executable checker:

```text
scripts/check_prime_power_adaptive_unavailable_token_temporal_ancestry.py
```

## CMR3258 — literal adaptive residual context

The checker exhausts every side-three paid-line trace and every unavailable residual-edge set. The trace vertices and surviving residual bipartite graph are generated explicitly.

## CMR3259 — maximum unavailable matching

On the vertices disjoint from the paid-line trace, the lexicographically first maximum unavailable matching is generated by exhaustive search.

## CMR3260 — exact forbidden-matching extension

The paid-line trace and maximum unavailable matching are extended to a literal residual perfect matching. Its unavailable edges are checked to be exactly the unavailable trace edges together with the absorbed matching.

## CMR3261 — free absorption operation

Every absorbed matching edge becomes forbidden by the adaptive derangement cylinder and therefore requires no restoration.

## CMR3262 — exact König cover

A minimum row-column vertex cover of the surviving unavailable graph is generated and checked to have size equal to the maximum absorbed matching.

## CMR3263 — complete remaining-inventory cover

The union of the paid-line trace vertices and the König cover is checked to meet every unavailable edge still allowed by the adaptive cylinder.

## CMR3264 — heavy unavailable star

For every nonempty remaining inventory, the checker identifies a row or column vertex meeting at least the ceiling of the inventory divided by the cover size.

## CMR3265 — exhaustive adaptive census

```text
4,096 adaptive profiles
4,116 absorbed-edge incidences
1,150 free-absorption profiles at threshold two
2,946 small-cover profiles
3,988 heavy-star profiles
6,403 heavy-star edge incidences
16,404 cover-vertex incidences
```

## CMR3266 — exact star-prefix occupancy

For side eight, both row and column stars are partitioned at every nonroot binary prefix depth. The class sizes sum exactly to the star degree and respect the exact cell capacity.

## CMR3267 — heavy-token branch

At the square-root threshold, a prefix class meeting the threshold is recorded as one heavy unavailable full token.

## CMR3268 — dispersed-token bank

When no class is heavy, one unavailable edge is selected from every occupied prefix class. The selected witnesses occupy pairwise distinct labelled token cells.

## CMR3269 — exhaustive token census

```text
8,160 star-depth profiles
5,008 heavy-token profiles
12,672 heavy-token edge incidences
3,152 dispersed-token profiles
9,600 dispersed token witnesses
```

## CMR3270 — one-token finite stock or recurrence

All three-episode histories of two-edge witness sets in a four-edge token universe are exhausted. Exactly 114 histories satisfy the finite-stock branch and 102 contain a pair recurring in all three episodes.

## CMR3271 — exact labelled parent stock

For sample `p=2,h=3,t=8`, the complete labelled nonroot token-edge stock is checked as

```text
(p+1)(h-1)t^2 = 384.
```

## CMR3272 — absence-run ledger

All six-time binary availability histories and every nonempty set of selected absent episode times are exhausted. The number of continuous absence runs is checked to be at most one plus the absent-to-present reintroduction count.

## CMR3273 — reintroduction or persistent blocker

Among histories with at least three selected absent occurrences, every case has either a reintroduction or one continuous absence run containing at least three occurrences. The finite census contains 169 reintroduction endpoint histories and 133 persistent-run histories.

## CMR3274 — free-absorption temporal stock and corruption rejection

All three-episode histories of two-edge absorbed matchings in a nine-edge universe are exhausted:

```text
42,084 finite absorption histories
4,572 recurrent absorbed-edge histories
```

Eight independently resealed report corruptions are rejected.

Contract digest:

```text
d7c2c9bef971f9cb2d4b3298ef908641fb9afb8d80ba8f381202ee100a6efcc0
```

## CMR3275 — T02 consequence and honesty boundary

Installed kinds:

```text
adaptive-unavailable-matching-absorption
unavailable-row-column-cover-extraction
heavy-unavailable-star-extraction
unavailable-star-heavy-token
unavailable-star-dispersed-token-bank
unavailable-token-finite-stock
unavailable-token-reintroduction-payment
unavailable-token-persistent-blocker
free-absorption-finite-stock
```

Exact flags:

```text
adaptive_unavailable_absorption_ancestry_proved = 1
unavailable_star_token_splice_proved = 1
unavailable_token_temporal_scheduler_exact = 1
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

The remaining unpriced endpoint is one persistent unavailable blocker inside one owner/envelope epoch. No all-`n` theorem is claimed.
