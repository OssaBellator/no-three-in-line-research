# ERL minimizer-face scalar review gate

A complete-minimizer-face potential may replace a selected-response potential only when all items below are satisfied.

## Exact combinatorial interface

- [x] The five face classes `A,B,C,D3,D4` are reconstructed from the restoration-selector manifest.
- [x] The face-transition graph is exactly `K_{2,3}` with six reversal pairs.
- [x] All 64 orientations are classified: 46 acyclic and 18 cyclic.
- [x] Exactly fourteen acyclic covers project to the existing menu-state covers.
- [x] Exactly thirty-two covers are genuinely background-sensitive.
- [x] Every cover leaves four external transitions per background: three changing and one neutral.

## Source requirements

- [ ] A complete physical occurrence domain is supplied.
- [ ] Every occurrence is assigned an exact physical minimizer face.
- [ ] The `D3`/`D4` split is source-backed whenever a background-sensitive cover is used.
- [ ] A source-backed scalar value is attached to every physical face class in the chosen cover.
- [ ] Strict descent is proved on every paid physical face transition.
- [ ] Every external physical face transition has one accepted closure route.
- [ ] Owners, legal operations, child rows, payments, and route identities are occurrence-faithful.

## Prohibited promotions

Do not infer a face-scalar certificate from:

- selected-label agreement;
- the audit's lexicographic tie-break;
- equal next gaps;
- equal complete-score vectors;
- a shared operation-signature class;
- the abstract existence of one of the 46 acyclic orientations.

The fourteen menu-projectable covers add no new ordinary menu cover. The thirty-two background-sensitive covers add flexibility only after physical `D3`/`D4` classification and do not lower the route burden.

Current accepted physical face covers: **0**.

`all_n_proved_by_checker` must remain `0` until the physical domain, exact coefficients, execution closure, strict descent, and all-side transfer are separately proved.
