# Side-four joint binding source coverage audit

The joint binding-input manifest requires one exact installed source record to carry, together, a recurrent-state key, installed weight, transition-occurrence provenance and parent-rule provenance.

This audit checks the installed owner/fate ancestry range `docs/340` through `docs/348`. Those files establish collision-class compression, weighted-certificate formats, line-energy kernels, selector stability, raw-fibre lineage and blocker structure. They do not populate the recurrent-state table demanded by the binding manifest.

The executable contract is:

```text
data/prime_power_side_four_joint_binding_source_coverage_contract.json
scripts/check_prime_power_side_four_joint_binding_source_coverage.py
```

The checked result is:

```text
candidate_source_count = 9
qualifying_population_source_count = 0
first_manifest_record_populated = 0
source_coverage_audit_complete = 1
binding_input_population_complete = 0
global_binding_constructed = 0
global_binding_incompatibility_proved = 0
all_n_proved_by_checker = 0
```

This is a source-coverage obstruction only. It does not prove that a global binding is mathematically impossible. It proves that the audited installed ancestry range cannot honestly supply even the first manifest population record.

The next constructive artifact must therefore be a recurrent-state population table or a narrower source that explicitly supplies all required provenance fields. Local coordinates, local aliases, certificate formats and kernel identities are insufficient substitutes.
