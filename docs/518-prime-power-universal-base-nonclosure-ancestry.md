# Universal-range descent and small-factor bases are exact, while collateral closure remains open

This chapter records **CMR3894--CMR3913** and checks source theorems CMR1166--CMR1197.

Canonical checker:

```text
scripts/check_prime_power_universal_base_nonclosure_ancestry.py
```

Contract:

```text
1872628306b27c0e3240fe4be2acb27f319c9d2f36acead6b47deaa6838efd36
```

## CMR3894--CMR3898 — universal target descent

Every dirty target splits uniquely into pure residual, anchored residual or fixed-core form. Residual targets have a current degree-two response bank; fixed targets return to a lifted owner. Complete blockage descends through a strict unit wall, and every positive child side decreases.

## CMR3899--CMR3903 — exact small joint bases

All six relabelled side-three opposite permutations are checked. Each of the 36 nonopposite target edges has one unique containing derangement and one unique response derangement, across 288 restricted singleton-bank cases. Side two has two labelled assignments but one complete physical board, contains no physical triple, and contracts rigidly as an induced interface. Side one has one forced one-layer edge and no disjoint joint factor.

## CMR3904--CMR3907 — small-interface ancestry

For every final fixed target, the last fixation event and unique backward edge lineage are checked over 1,900 finite ancestry profiles. Every nontrivial root has a lifted side-at-least-three owner with the existing target bank; a root side two is already clean.

## CMR3908--CMR3911 — finite response does not imply minimum zero

The checker validates an explicit positive-minimum model with a finite nonimproving response bank, a dirty conditioned singleton, and finite-stock exhaustion. It separately checks the averaging and exact new-minus-lost identities.

```text
global_target_collateral_inequality_proved = 0
```

CMR1196 remains an open target and is not included among the proved theorem identifiers.

## CMR3912--CMR3913 — finite canonical classes and honesty boundary

The remaining averaging problem is reduced to six canonical response classes: residual targets, lifted fixed targets, loaded lines/stars, minimal unit walls, side-three singleton banks and rigid small interfaces.

```text
universal_range_target_descent_exact = 1
small_joint_factor_base_exact = 1
small_interface_target_ancestry_exact = 1
finite_response_nonclosure_exact = 1
universal_base_nonclosure_ancestry_proved = 1
global_target_collateral_inequality_proved = 0
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```
