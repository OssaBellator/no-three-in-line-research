# Compensating-mechanism evidence gate

This chapter closes the `657--662` tranche while preserving the distinction
between exact finite or conditional mechanisms and complete geometric source
paths.

## PP3daw — Frontier advances

The tranche adds five exact advances:

1. a corrected radius-64 boundary chain through fifteen blocks;
2. a two-level Hall packing theorem composing motif overlap with centre conflicts;
3. a complete threshold exposure obstruction after perfect buffer balancing;
4. an explicit fourteen-pair saturated anchor reservoir checked on all 8,192
   unary compositions;
5. an exact finite-repertoire shell envelope with quota and robustness laws.

These results sharpen every local interface but do not supply an asymptotic
coordinate construction.

## PP3dax — Candidate ledger remains `25/30`

Candidate completion remains

```text
boundary     4/5
Hall         4/5
threshold    5/5
prefix       5/5
shell        5/5
integration  2/5
----------------
total       25/30.
```

Every actual row remains `fixture_derived`; no row has a complete
coordinate-source-to-global-coupling proof, so the promoted set is empty.

In particular:

- fifteen corrected blocks do not imply recurrence;
- two-level Hall graph certificates have not been derived from host coordinates;
- balanced threshold schedules expose only illegal intermediates and retain
  sign-coherent endpoint drift;
- the fourteen-pair reservoir is finite and lacks an anchor-compatible recurrence;
- shell repertoire burdens have not been measured from geometric macros.

## PP3day — Fixed point and closure remain unchanged

The fixture fixed-point total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

Because every coupling row remains fixture-derived, geometric closure is false.
The all-`n` theorem remains open.

The strict audit is `scripts/check_compensating_mechanism_gate.py`.
