# Repeated-transition and source-generation evidence gate

This chapter records the consolidated evidence status after `docs/621--625`.

## 1. Candidate-field ledger

### Theorem PP3cws -- PROVED / CANDIDATE COVERAGE IS 25 OF 30

The candidate source-field scores are

```text
boundary 4/5, Hall 4/5, threshold 5/5,
prefix 5/5, shell 5/5, integration 2/5,
```

for total `25/30`. Threshold gains one field because every nearest legal target
has a complete repository-native two-swap source-matrix path.

## 2. Actual evidence gate

### Theorem PP3cwt -- PROVED / ZERO GEOMETRIC ROW PROMOTION

All six actual rows remain `fixture_derived`; no row is promoted.

The unresolved source paths are:

- boundary: the budget-seven path reaches nine blocks but has no tenth transition;
- Hall: the asymptotic host has not supplied six unused resources with partner,
  source, and host-defect restrictions all partial matchings;
- threshold: every two-swap path exposes an illegal intermediate, and no atomic
  geometric compound is known;
- prefix: the parabola reservoir is not the saturated PP3 retained source, while
  the internal nineteen-cell reservoir fails on a positive fraction;
- shell: the zero-buffer `(1,1,1)` action has no clean-macro realization; and
- integration: no source-derived coefficients are available for a promoted meet.

#### Proof

Each row fails at least one complete coordinate source-path requirement. ∎

## 3. Fixed-point arithmetic

### Theorem PP3cwu -- PROVED / ARITHMETIC SLACK UNCHANGED

The fixture fixed-point total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

The arithmetic closure test is positive, but geometric closure is false.

The all-`n` theorem remains open.
