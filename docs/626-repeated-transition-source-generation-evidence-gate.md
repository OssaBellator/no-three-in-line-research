# Repeated-transition and source-generation evidence gate

This chapter records the evidence status after `docs/621--625`.

## 1. Candidate-field ledger

### Theorem PP3cws — PROVED / CANDIDATE COVERAGE RISES TO 25 OF 30

The candidate source-field scores are

```text
boundary    4/5
Hall        4/5
threshold   5/5
prefix      5/5
shell       5/5
integration 2/5
```

for a total of `25/30`.

The threshold candidate gains one field because every nearest legal target now
has a complete repository-native conservative residual-pivot path from the stored
source matrix.

#### Proof

`scripts/check_repeated_transition_source_generation_gate.py` reconstructs the
ledger.  The other frontiers gain sharper composition or source-price data but no
additional completed candidate field. ∎

## 2. Actual evidence gate

### Theorem PP3cwt — PROVED / ZERO GEOMETRIC ROW PROMOTION

All six actual ledger rows remain `fixture_derived`; no row is promoted to
`geometrically_verified`.

#### Proof

Boundary lacks a corrected-state cycle, Hall lacks a bound on real bad vertices,
threshold lacks geometric pivot edits, prefix lacks retained anchor cells, shell
lacks the all-cycle clean macro, and integration therefore lacks verified inputs.
∎

## 3. Fixed-point arithmetic

### Theorem PP3cwu — PROVED / ARITHMETIC SLACK UNCHANGED

The fixture fixed-point total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

The arithmetic closure test remains positive, but geometric closure is false.

#### Proof

The checker reconstructs and sums the six exact rational fixture coordinates,
then evaluates the evidence meet separately. ∎

The all-`n` theorem remains open.
