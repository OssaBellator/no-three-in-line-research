# Integrated execution across every current all-n frontier

CMR2390--CMR2405 add typed artifacts for the six induction-handoff assertions and exact
open/closed dispositions for all 252 exceptional selector chambers. This chapter integrates
those new registries with the thirteen-frontier atomic execution manifest.

## Theorem CMR2406 — PROVED AS AN INTERFACE

The execution stack contains exactly:

1. one forty-three-target atomic frontier certificate;
2. one typed handoff-assertion artifact registry; and
3. one exceptional-chamber disposition registry.

The atomic certificate fixes thirteen genuine frontier groups and separates provisional
research-start dependencies from proof-closure dependencies.

## Theorem CMR2407 — PROVED

All three certificates must use the same final induction handoff and quotient semantic
refinement. The handoff artifact registry must bind the exact handoff contained in the atomic
certificate's final dossier, and the chamber registry must bind the exact quotient refinement
contained in that handoff.

Parallel proof universes cannot be substituted merely by reusing state, assertion or chamber
names.

## Theorem CMR2408 — PROVED

Each of the six handoff targets is synchronized with its assertion-specific artifact bundle. A
handoff target is complete exactly when the corresponding assertion is effectively closed and
its typed registry contains the required single artifact bundle.

The synchronization record retains the assertion ID, target ID, completion flags and exact
bundle digest.

## Theorem CMR2409 — PROVED

The exceptional targets are synchronized with the canonical 252-chamber registry:

\[
\boxed{
T20\text{ complete}\iff\texttt{exceptional\_zero\_rows\_ready}=1
}
\]

and

\[
\boxed{
T21\text{ complete}\iff\texttt{hard\_core\_rows\_ready}=1.
}
\]

Thus the high-level exceptional frontier cannot close while any of its 232 zero-selector or 20
hard-core chamber dispositions remains open, and a complete chamber bank cannot drift away from
the semantic obligation target ledger.

## Theorem CMR2410 — PROVED

The integrated readiness gate requires simultaneously:

- the complete atomic frontier target stack;
- exact typed handoff-artifact coverage; and
- complete exceptional-chamber disposition.

The checker publishes the independent component gates before forming their conjunction.

## Theorem CMR2411 — PROVED

The exact current blocker record publishes:

- research-actionable atomic targets;
- proof-actionable atomic targets;
- the minimum parallel proof-wave depth to the root target;
- open zero-selector and hard-core chamber counts; and
- open typed handoff-assertion bundles.

These quantities provide one synchronized execution surface across every current frontier.

## Theorem CMR2412 — HONEST EXECUTION BOUNDARY

A synchronized target, artifact and chamber registry proves internal documentary consistency
only. It does not prove any source statement, geometry theorem, transition rule, chamber
closure, induction assertion or root implication true. Research-actionable does not mean
proof-actionable, and neither means proved.

The checker permanently publishes

\[
\boxed{\texttt{all\_n\_proved\_by\_checker}=0.}
\]

## Corollary CMR2413 — EXECUTABLE ENDPOINT

`scripts/check_prime_power_atomic_frontier_execution.py` validates the fixed thirteen-frontier,
forty-three-target dual-DAG manifest and its exact obligation, premise and handoff coverage.

`scripts/check_prime_power_current_frontier_execution.py` composes that manifest with the typed
handoff-assertion artifact registry and canonical exceptional-chamber dispositions, enforces all
shared identities and publishes the synchronized all-frontier gate and blocker record.

Both scripts were syntax-compiled locally. The atomic definition self-test reports thirteen
frontiers, forty-three targets and complete topological coverage of both dependency graphs. No
genuine frontier target or chamber closure is claimed complete.
