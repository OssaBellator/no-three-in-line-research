# ERL selector-face congruence review gate

A restored-menu tie-break may be replaced by complete minimizer-face congruence only when the exact worklist below is discharged with source-backed physical evidence.

## Exact worklist

```text
raw anchored background comparisons      104
menu/signature cells                       20
signature-level response-pair obligations  32
menu-parametric theorem domains             4
cross-menu response-pair types              3.
```

The three pair types are:

```text
2031 ~ 2301   2 signature obligations,  8 raw comparisons
2031 ~ 2310  20 signature obligations, 64 raw comparisons
2031 ~ 3201  10 signature obligations, 32 raw comparisons.
```

## Accepted proof granularities

A proposal must use one explicitly declared level.

### Signature-specific

Populate all 32 obligations in `data/exact_recurrent_first_host_selector_face_congruence_worklist.json`.

### Menu-parametric

Supply all four exact menu/pair theorems and prove uniformity over their complete operation-signature domains.

### Cross-menu parametric

Supply all three pair-type theorems and explicitly quantify `2031~2310` over both `restore_02` and `restore_both`, including their different legal menu states.

A theorem proved at a coarser level must imply every finer obligation it replaces.

## Required evidence

Every signature-level obligation, or the theorem replacing it, must supply:

```text
physical_occurrence_domain_ref
legal_menu_state_ref
operation_signature_domain_ref
common_owner_ref
operation_congruence_ref
child_row_congruence_ref
payment_congruence_ref
closure_route_congruence_ref
realization_status.
```

The references must join occurrence-faithfully to one domain and persistent owner lineage.

## Invalid shortcuts

The following do not establish face congruence:

```text
equal complete scores
the same lexicographically selected label
the same next-energy gap
the four-coordinate operation signature alone
one response-pair theorem at only one menu
an operation-kind name without occurrence mapping
equal child-row labels without equal weights or payments
an abstract closure route not joined to both responses
synthetic fixtures at another side.
```

The operation signature compresses the finite score worklist only. Its physical completeness remains unproved.

## Current state

```text
populated signature evidence slots       0 of 288
accepted signature obligations           0 of 32
imported menu-parametric theorems         0 of 4
imported cross-menu theorems              0 of 3
selector tie-break substitution allowed  0.
```

This gate does not authorize physical restoration legality, persistent owner identity, recurrent child rows, strict Lyapunov closure, global termination, or `all_n_proved_by_checker=1`.
