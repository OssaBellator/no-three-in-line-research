# Prime-power side-four joint global binding attempt

## Purpose

This unit performs the next checked step after the seven-class joint local weight compatibility result: it asks whether the two sampled parent states and seven exact child classes can be bound to installed global recurrent states and weights.

## Inputs

- `data/prime_power_side_four_joint_sample_weight_compatibility_contract.json`
- `scripts/check_prime_power_owner_fate_lineage_kernel_ancestry.py`

The joint contract supplies two sample rows, two local parent symbols and seven distinct exact child classes. The ancestry checker is the installed-stack boundary for the recurrent owner/fate and compulsory weighted layers.

## Checked result

The attempt is **underdetermined**.

The installed ancestry boundary still records:

```text
owner_fate_rows_populated_all_recurrent_states = 0
compulsory_weighted_certificates_complete = 0
actual_global_parent_rule_complete = 0
```

Consequently, the repository does not currently supply enough checked information to identify either sample's global parent-state key, map all seven exact child classes to installed recurrent states, or attach nine installed parent/child weights.

This is not a proof of incompatibility. It is a checked certificate that the requested binding cannot yet be derived from the installed data without inventing state keys or promoting the local witness to a global Lyapunov vector.

## Files

```text
data/prime_power_side_four_joint_global_binding_attempt_contract.json
scripts/check_prime_power_side_four_joint_global_binding_attempt.py
```

Contract SHA-256:

```text
787039d4458fe2e5ee1400e76815cee6dc8b4425d04436453083a91ce7d9f077
```

## Preserved honesty flags

```text
checked_global_binding_attempt_complete = 1
global_binding_constructed = 0
global_binding_incompatibility_proved = 0
joint_sample_global_weight_bindings_complete = 0
joint_sample_global_recurrent_compatibility_proved = 0
all_n_proved_by_checker = 0
```

## Next target

Populate an installed recurrent-state table for the relevant owner/fate classes, including exact state keys, normalized global weights and transition occurrence provenance. Then rerun the binding attempt as either a constructive binding or a genuine checked incompatibility certificate.
