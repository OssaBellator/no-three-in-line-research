# Bounded-denominator occurrence-slot collision lineage

This note records BDA5fw--BDA5ga. It refines the physical-potential source-prefix and internal merge--segment--split witnesses from BDA5fr--BDA5fv.

## Contract

Fix the complete primitive-potential network, canonical restoration-path pair, maximal common segment, ordered capacity slots, primitive weights and edge gains. Every occupied slot carries one complete physical potential occurrence and each successful restoration use debits it exactly once.

## Theorem

1. Every localized restoration collision has a canonical least occupied segment slot.
2. Equal occurrence tokens on the two paths give an exact double consumption of one primitive-potential occurrence.
3. Distinct occurrence tokens assigned to the same slot give a non-injective arithmetic slot dictionary, preserving the two conflicting physical and gain addresses.
4. An absent token returns the first missing predecessor, damping or issuance record.
5. Hence an injective occurrence-to-slot map with one-use debits excludes every restoration-path collision. Hidden potential creation, relabelling, slot reuse, changed denominator state, or suppressed gains is a reset.

This is conditional on constructing the actual arithmetic network and proving its complete occurrence lineage.

## Deterministic audit

Run `python scripts/verify_bda_occurrence_slot_collision_lineage.py`.

No statement here proves BDA6 or the no-three-in-line conjecture.