# Final dossier integrity beyond the implication-premise contract

CMR2342--CMR2349 assemble ten ordinary implication premises above the semantic closure dossier.
CMR2350--CMR2357 separately require noncircular dependency support among the typed proof
artifacts. This chapter composes those two endpoints into one final documentary integrity gate.

## Theorem CMR2358 — PROVED AS AN INTERFACE

The final integrity certificate contains exactly:

1. one CMR2342--CMR2349 final implication-premise contract; and
2. one CMR2350--CMR2357 obligation-artifact support-DAG certificate.

Both certificates must use the identical typed artifact registry.

## Theorem CMR2359 — PROVED

The artifact-support integrity gate is one exactly when the support graph is acyclic,
dependency-aligned and complete over every immediate prerequisite artifact bundle of each
proved obligation.

## Theorem CMR2360 — PROVED

The final contract gate is inherited exactly from CMR2347. It requires the finite interface
gate, typed artifact coverage, closure of every dependency of
`GLOBAL_QUOTIENT_IMPLIES_ALL_N`, an admissible termination mode and all ten implication
premises effectively closed.

The root implication obligation itself may remain open and actionable. Consequently the
combined integrity dossier is a pre-root handoff rather than a certificate that presupposes
the desired implication.

## Theorem CMR2361 — PROVED

The combined integrity gate is

\[
\boxed{
\texttt{final\_dossier\_integrity\_ready}
=
\texttt{final\_implication\_contract\_ready}
\land
\texttt{artifact\_support\_integrity\_ready}.
}
\]

A final dossier cannot become ready while its proof-artifact support is circular, downstream or
incomplete, even if every opaque status flag is marked proved.

## Theorem CMR2362 — PROVED

The checker publishes the exact open premise IDs, any proved obligations with incomplete
artifact support, the maximum artifact-support depth and the digests of the contract, support
DAG, shared artifact registry and final gate record.

## Theorem CMR2363 — PROVED

The combined certificate permanently publishes

\[
\boxed{\texttt{all\_n\_proved\_by\_checker}=0.}
\]

No documentary readiness flag is reinterpreted as a machine proof of the conjecture.

## Theorem CMR2364 — HONEST FINAL INTEGRITY BOUNDARY

A ready integrity dossier would establish exact internal linkage, noncircular documentary
support and closure of every declared proof premise needed before the root implication. It
would not close that root, verify the mathematical truth, completeness or sufficiency of the
human-readable proof artifacts.

## Corollary CMR2365 — EXECUTABLE ENDPOINT

`scripts/check_prime_power_final_dossier_integrity.py` composes the final implication contract
with the obligation-artifact support DAG, requires one shared typed registry and publishes the
strict combined readiness gate without ever declaring `D(n)=2n` proved.

The checker was syntax-compiled in the publication environment. With the current open semantic
obligations, the final integrity gate remains zero.
