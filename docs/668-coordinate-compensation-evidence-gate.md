# Coordinate-compensation evidence gate

This chapter integrates `docs/663--667` under the same strict promotion rule used
by the earlier evidence gates: a row is promoted only by a complete coordinate
source path with exposed-state legality and asymptotic or recurrent continuation.

## PP3dbo — Current frontier advances

The tranche adds five exact results:

1. a corrected radius-64 boundary chain through sixteen blocks, followed by a
   complete raw seventeenth obstruction;
2. a host-auditable Hall packing certificate derived directly from local resource
   multiplicities;
3. an integer hyperplane separating the threshold source from the convex hull of
   all 4,475 legal four-layer matrices;
4. a complete coordinate realization of one optimal four-assignment `13 -> 11`
   anchor rerouting across all 1,024 compositions;
5. exact possible and robust shell-cycle criteria under independent burden
   intervals.

These improve the finite and conditional mechanisms but do not give an all-length
coordinate construction.

## PP3dbp — Candidate ledger and evidence levels

Candidate completion remains

```text
boundary     4/5
Hall         4/5
threshold    5/5
prefix       5/5
shell        5/5
integration  2/5
----------------
total       25/30.
```

All six actual evidence rows remain `fixture_derived`, and the promoted set is
empty.

- The boundary chain reaches sixteen blocks but has no recurrence or corrected
  seventeenth transition.
- The Hall resource-incidence theorem has not been instantiated by conditional
  host coordinates.
- The threshold result is a global obstruction, not a legal compensating
  primitive.
- The prefix lift is coordinate-level but finite and covers one selected deletion
  case rather than a uniform recurrence.
- The shell interval theorem has no coordinate macro graph or certified robust
  positive cycle.

Therefore geometric closure remains false.

## PP3dbq — Fixed point and promotion gate remain unchanged

The exact fixture total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

No coupling coefficient changes because none of the current results supplies a
complete asymptotic coordinate source path. The all-`n` theorem remains open.

The audit is `scripts/check_coordinate_compensation_gate.py`.

## Next frontier

The next theorem identifier is `PP3dbr`. The next tranche should prioritize the
unique minimum-three seventeenth boundary attempt, coordinate instantiation of the
Hall resource lists, a threshold model escaping the legal-convex separator, a
uniform prefix rerouting choice rule, and a coordinate macro graph with a robust
positive cycle.
