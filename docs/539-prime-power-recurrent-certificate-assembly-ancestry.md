# Installed recurrent certificate-assembly ancestry

This chapter installs the canonical executable ancestry for source CMR1582--CMR1629.

```text
checker = scripts/check_prime_power_recurrent_certificate_assembly_ancestry.py
contract = 195b9f8ce43391860ea28b7b0d882250fc0f13222b16e67f432c786cc2d80285
```

The installed object is an exact recurrent-certificate protocol. It does not claim that the required assignment duals, integer slacks, thin tables or labelled SCC blocks are already strict.

## Theorem CMR4230 -- PROVED

The checker binds exactly source chapters 301--306 and their six verifier entrypoints, covering CMR1582--CMR1629 without silently importing a later source range.

## Theorem CMR4231 -- PROVED

Every rational response law has one rational doubly stochastic allowed-edge marginal. Return and selector row sums expand exactly by conditional last-entering owner loads.

## Theorem CMR4232 -- PROVED

For selector-to-return cap `T`, the exact coupled spectral scalar is one assignment expectation with edge score

```text
g_ret + T*g_sel
```

Separate worst return and selector matchings are not added.

## Theorem CMR4233 -- PROVED

One rational assignment dual with objective below one certifies the complete return-selector block strict. Clearing denominators gives one finite strict integer system.

## Theorem CMR4234 -- PROVED

Finite exchange, owner, height, token, prefix, carry, root, fixed-interface and thin labels may be inserted into the shared assignment only through exact classes or an honest componentwise upper quotient.

## Theorem CMR4235 -- PROVED

Strong, singleton and endpoint-overlap line-clean rows share one exact integer weighted count. Each coefficient class has one exact strict inequality, maximal budget, unavailable-edge cap and additive integer slack.

## Theorem CMR4236 -- PROVED

The integer budget test is equivalent to the rational permanent comparison. It is not a proof that all inherited geometric rows have positive slack.

## Theorem CMR4237 -- PROVED

For any rational gap from one, every paid-pair selector is either a bounded return splice or has one quantitatively concentrated finite rank/profile class.

## Theorem CMR4238 -- PROVED

Exact or uniform rook probabilities convert a concentrated selector expectation into a candidate-count lower bound. Rational denominators give an exact integer localization certificate.

## Theorem CMR4239 -- PROVED

At prime-field depth, one source root channel contains at most one ordered translated pair with residual support size one or two. First support labels are finite-resource transitions.

## Theorem CMR4240 -- PROVED

Excess prime-field recurrence fixes one exact ordered pair, partner type and local-rank pattern. Reused response support enters the return kernel; only one exact fixed-interface atom can remain as a new base row.

## Theorem CMR4241 -- PROVED

Every repeated rank-one or rank-two fixed-interface prescription has an exact contracted rook probability and one finite rational offspring row.

## Theorem CMR4242 -- PROVED

For every chosen thin-side cap, the normalized fixed-interface/thin row stock is finite and admits a finite rational linear-program or strict integer-certificate search.

## Theorem CMR4243 -- PROVED

Collision, local-line, fixed-interface and CRT provenance must be retained in the exact state. Premature label projection can create artificial self-loops or cycles.

## Theorem CMR4244 -- PROVED

After contracting exact labelled strongly connected components, the quotient is block upper triangular. Strict local block certificates glue by explicit reverse-topological rational scaling and denominator clearing.

## Theorem CMR4245 -- HONEST ENDPOINT

The checker contract and twelve-corruption fixture audit are exact, while the following remain zero:

```text
return_selector_assignment_dual_globally_strict = 0
line_clean_integer_slacks_globally_positive = 0
critical_selector_classes_closed = 0
fixed_interface_thin_table_subcritical = 0
labelled_crt_recurrent_blocks_subcritical = 0
same_owner_diagonal_blocks_subcritical = 0
global_target_collateral_inequality_proved = 0
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

The checker contract was reproduced locally. The complete six-verifier execution was not observed in the current environment.
