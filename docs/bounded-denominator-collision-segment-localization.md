# Bounded-denominator collision-segment localization

This note refines BDA5fm--BDA5fq on the complete primitive-potential restoration network. It turns each paired restoration-path collision into one canonical contiguous arithmetic capacity segment.

## Contract

Fix the canonical irreducible restoration core, its canonical split, the canonical cycle-cancelled integral unit paths paying the two sides, and one named positive-pressure arithmetic cut arc. A collision witness is one unit restoration path from each side paired at one occupied capacity slot. Retain every physical-potential, source-potential, primitive weight, gain, compatibility and restoration field.

## BDA5fr--BDA5fv

1. The two simple directed paths have a unique maximal common contiguous directed segment containing the collision arc.
2. Its complete entry, exit, edge word, restoration endpoints and side labels form a canonical arithmetic segment address.
3. The segment either begins at one physical potential source and later splits, or begins internally with distinct incoming edges and ends with distinct outgoing edges, giving a merge--shared-segment--split witness. Distinct restoration terminals force the split before the terminal edge.
4. Collision multiplicity is preserved under grouping by complete segment signature. Among `M` witnesses with `Q` signatures, one signature occurs at least `ceil(M/Q)` times.
5. A local gain, valuation or denominator argument excluding the retained source-prefix or internal segment excludes the witness. Otherwise the exact shared potential capacity must be paid. Omitted gains or paths, merged sources, changed denominator state or independent potential reuse is a reset.

## Consequence

The remaining BDA obstruction is one singleton restoration shortage or one completely addressed pair of restoration paths sharing a canonical physical/source-potential segment.

## Scope

This is conditional on the fixed complete arithmetic network and canonical integral path decomposition. It does not construct the weights or prove BDA6 or the no-three-in-line conjecture.
