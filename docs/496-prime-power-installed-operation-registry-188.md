# The canonical installed construction registry contains one hundred eighty-eight operation kinds

This chapter records **CMR3562--CMR3573**.

Executable registry:

```text
scripts/check_prime_power_installed_operation_registry_188.py
```

## CMR3562 — immutable 138-kind base

```text
base registry = 931588a34f8817674a8e819aecf2e5aa79c2809a04b8e20c6520e95b2bf423f3
base kinds = 138
base contracts = 22
base owner-changing kinds = 42
```

## CMR3563 — product rectangle operations

```text
product-conflict-pure-mixed-decomposition
product-mixed-atom-universe
product-mixed-atom-rectangle
product-dirty-rectangle-concentration
product-protected-prescription-deletion
product-essential-core-concentration
```

## CMR3564 — essential transfer and forced-product operations

The registry distinguishes essential contraction, transferred triggers, skeleton-only forced certificates, free-trigger deletion, alternating deletion/contraction budgets, essentiality-loss witnesses, certificate-edge deletion, skeleton churn, entering-edge escape, finite witness stock, token payment and persistent forced-certificate dispatch.

## CMR3565 — pure-factor recursion operations

```text
pure-factor-complete-essential-core-contraction
pure-factor-core-conflict-decomposition
pure-factor-anchored-deletion
pure-factor-renormalization
pure-factor-restoration-payment
pure-factor-strict-recursion
```

## CMR3566 — factor-prefix operations

The registry separately records canonical factor envelopes, strict child splits, transport tables, routing skeletons, routing recurrence, heavy-child and dispersed-child dispatch, and the strict recursion endpoint.

## CMR3567 — multi-child operations

Cross-child pure/mixed decomposition, exact boxes, dirty-box concentration, nonessential child-edge deletion and all-essential child certificates receive distinct identifiers.

## CMR3568 — routing-change operations

Entering/leaving routing support, finite routing-edge stock, exact token payment and recurrent routing-child products are registered separately.

## CMR3569 — mixed-child and forced-child operations

The final bank distinguishes active-atom deletion, deletion monotonicity, forced certificates, mixed-clean additivity, strict child recursion, finite forced-certificate stock, paid escape and ancestry dispatch.

## CMR3570 — exact theorem and contract binding

All fifty entries bind nonempty CMR629--CMR690 ancestry and checker contract:

```text
0ce977177196fbffe8c8ffc446346dde64347999b299307f96ca74d4586e4e12
```

The two immutable entry modules are:

```text
scripts/product_factor_child_registry_entries_a.py
scripts/product_factor_child_registry_entries_b.py
```

## CMR3571 — installed census and payment classes

```text
188 operation kinds
23 checker contracts
62 owner-changing kinds
126 same-owner kinds
```

New payment counts:

```text
18 owner-witness-stock
12 scheduler-dispatch
6 local-family-restriction
4 strict-child-descent
3 strict-factor-contraction
2 factor-product-dispatch
2 routing-change-churn
2 labelled-interface-payment
1 edge-reintroduction
```

## CMR3572 — seals and corruption rejection

Thirteen independent mutations are rejected, including duplicate kinds, false contracts, missing ancestry, invalid owner/payment pairs, fake scheduler endpoints and incorrect restriction, contraction, routing or restoration owners.

Registry contract:

```text
f70e412fffd0e8e1c0987fe3b274d138b382a8624867c6e2f0b3ebc0193a034e
```

Registry seal:

```text
4d4875c010c4a6eac3d51eb6492d2db39322bc1b17015dc0dc50eb10946fecfc
```

## CMR3573 — honesty boundary

```text
installed_transition_kind_bank_188_exhaustive = 1
product_factor_child_operations_registered = 1
installed_payment_assignment_188_complete = 1

global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

The 188-kind bank is exhaustive only over its installed identifiers. It is not a proof that the original construction contains no later or unregistered operation.
