# Coordinate-identification evidence gate

`docs/579--583` sharpen every realization frontier with either a coordinate
candidate or a decisive obstruction.  This chapter updates the evidence ledger
without promoting benchmark rows to geometric closure.

## 1. Coordinate progress certificate

### Theorem PP3crw -- PROVED / COORDINATE-IDENTIFICATION STATUS

The candidate-field completion count remains

```text
23/30.
```

The new exact information is:

- boundary: eight five-block words survive at offset radius thirty-two, but none
  extends to six blocks;
- Hall: all twelve decoded coordinate pairs have no-three-in-line perfect
  matching extensions;
- threshold: the aligned matrix has no decomposition into four legal layers;
- prefix: terminal cell multisets cannot determine ancestry-sensitive risk;
- shell: `docs/517` and the canonical action model are connected by a full-rank
  incidence matrix.

No missing source field is completed for an actual global row.

#### Proof

Each statement is certified by one of the exact checkers in this tranche.  The
promotion requirements remain unchanged, so the completed-field count does not
increase. ∎

## 2. Arithmetic fixed point remains feasible

### Theorem PP3crx -- PROVED / FIXTURE FIXED-POINT RECHECK

For the fixture-derived base vector and coupling matrix, the exact minimal fixed
point still has total

```text
705466760524005697 / 3623878655999606784
```

and exact slack below one quarter

```text
200502903475895999 / 3623878655999606784 > 0.
```

#### Proof

Exact Gaussian elimination solves `(I-C)y=b`, and substitution verifies all six
coordinate equations. ∎

## 3. Coordinate evidence does not promote rows

### Theorem PP3cry -- PROVED / CONTINUED NO-PROMOTION GATE

All six actual direct rows remain `fixture_derived`; no row is promoted to
`geometric_verified`, and geometric closure remains false.

#### Proof

The boundary and threshold results are obstructions.  The Hall host is a
coordinate complete-grid candidate rather than an identified prime-patching
host.  The prefix result proves the need for richer ancestry data.  The shell
bridge reaches a stored cycle fixture but not actual clean-macro resources.
Direct rows and coupling coefficients therefore retain missing coordinate source
paths. ∎

## 4. Machine-readable record

The certificate is

```text
certificates/prime-patching-coordinate-identification-579-584.json
```

and the exact checker is

```bash
python scripts/check_coordinate_identification_evidence_gate.py
```

The group runner is

```bash
python scripts/check_frontier_579_584.py
```

The all-`n` theorem remains open.
