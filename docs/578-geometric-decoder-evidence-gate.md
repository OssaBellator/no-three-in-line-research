# Geometric-decoder evidence gate

`docs/572` records source-aligned benchmarks but promotes no global row.  The
new decoder work completes one additional candidate field while preserving the
distinction between repository-typed models and coordinate-level
prime-patching geometry.

## 1. Updated candidate completion

### Theorem PP3cre -- PROVED / TWENTY-THREE OF THIRTY FIELDS

The candidate-field completion vector is now

```text
boundary    3/5,
Hall        5/5,
threshold   4/5,
prefix      5/5,
shell       5/5,
integration 1/5.
```

Thus twenty-three of thirty candidate fields are complete.  Hall joins prefix
and shell as an internally complete repository-typed model because every
quotient choice now has explicit grid cells and perfect-matching extensions.

#### Proof

`docs/573--577` supply the new field evidence.  The Hall decoder completes its
previously missing matching-extension witness; the other frontier denominator
counts are unchanged. ∎

## 2. Arithmetic feasibility is unchanged

### Theorem PP3crf -- PROVED / FIXTURE FIXED-POINT STABILITY

The fixture-derived direct rows and coupling matrix are unchanged, so the exact
minimal fixed-point total remains

```text
705466760524005697 / 3623878655999606784
```

with positive slack

```text
200502903475895999 / 3623878655999606784.
```

#### Proof

Solve `(I-C)y=b` by exact rational elimination and substitute the result into
all six equations. ∎

## 3. Geometric nonpromotion

### Theorem PP3crg -- PROVED / DECODER GATE REMAINS CLOSED

No actual global row is promoted.  Boundary has no five-block realization in
the tested state space; Hall lacks coordinate-level endpoint-cell
identification; threshold lacks the complete geometric normal list; prefix
lacks a support-chord coordinate decoder; shell lacks clean-macro resource
identification; and integration lacks geometric coupling coefficients.

Positive arithmetic slack and repository-typed completion therefore do not
imply the all-`n` theorem.

#### Proof

Each listed missing map is required to identify a benchmark loss with an actual
prime-patching loss.  Since every direct row remains `fixture_derived`, the
geometric evidence meet is unchanged and the closure rule is false. ∎

## 4. Machine-readable record

The tranche certificate is

```text
certificates/prime-patching-geometric-decoders-573-578.json
```

and the group runner is

```bash
python scripts/check_frontier_573_578.py
```

## 5. Prime-patching consequence

The next work should target the remaining coordinate maps, not expand the
fixture ledger: a larger or corrected boundary catalogue, actual Hall endpoint
cells, the complete threshold inequality list, support-chord cell decoding,
clean-macro resource identities, and source-derived cross-frontier couplings.
