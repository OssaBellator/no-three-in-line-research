# The canonical one-hundred-thirty-eight-kind stack has a chained thirty-six-checker regression

This chapter records **CMR3526--CMR3537**.

Canonical runner:

```text
scripts/run_prime_power_installed_construction_regression_138.py
```

## CMR3526 — immutable 117-kind base

The runner imports the complete 34-checker manifest and verifies:

```text
d259fb78a7b8a2ccbf7387c3fdd7a780b1e21f0366179c08af5ebb068cda3507
```

## CMR3527 — exact extension

```text
scripts/check_prime_power_protected_interface_execution_ancestry.py
scripts/check_prime_power_installed_operation_registry_138.py
```

## CMR3528 — chained manifest seal

```text
e04a2859b6d64b738d7e300a4fd42c371ef476aefb73542e4649bda99a8b88b3
```

## CMR3529 — exact checker count

The canonical manifest contains exactly 36 checkers.

## CMR3530--3535 — validation contract

The runner enforces unique paths and theorem flags, source existence, Python compilation, exact SHA-256 contracts, deterministic execution, one JSON report, expected theorem flags and permanent `all_n_proved_by_checker = 0`. Static mode validates the complete source/manifest chain without running mathematical regressions.

## CMR3536 — failure isolation

Full mode identifies the exact failing checker and captures stdout/stderr.

## CMR3537 — validation consequence

```text
installed_transition_regression_138_complete = 1
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

A passing run validates the installed finite claims and registry binding only.
