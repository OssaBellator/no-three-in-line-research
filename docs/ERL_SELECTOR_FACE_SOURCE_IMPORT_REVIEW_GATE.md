# ERL selector-face source-import review gate

A restored-menu tie may be discharged by face congruence only through one complete import mode.

## Accepted modes

```text
signature-specific   32 records, 288 evidence slots
menu-parametric       4 records,  40 evidence slots
cross-menu            3 records,  30 evidence slots.
```

Every accepted record must refer to one source-defined physical occurrence domain, legal menu state, persistent owner, legal operation, recurrent child row, payment rule, and closure route.

For parametric modes, the theorem must also prove completeness of the exact signature and menu domain it replaces.

## Abstract-source boundary

The owner/fate ancestry and installed operation-registry documents jointly expose schemas for:

```text
common owner
operation
child row
payment
closure route.
```

This two-source schema cover is not an occurrence-faithful response-pair theorem. Neither document names `2031`, `2301`, `2310`, or `3201`, and neither supplies a first-host pair join.

Do not accept any of the following as source import:

```text
union of abstract component schemas
equal complete-score values
equal selected labels
membership in one operation-signature class
same installed payment class
same-owner preservation for an unnamed operation kind
nonempty generic continuation rule
partial records mixed across proof modes.
```

## Current gate

```text
first-host pair theorems found        0
signature records accepted            0 of 32
menu-parametric theorems accepted      0 of 4
cross-menu theorems accepted           0 of 3
accepted proof modes                   0 of 3
selector tie-break substitution        0.
```

No gate result authorizes physical restoration legality, recurrent child rows, strict Lyapunov closure, global termination, or `all_n_proved_by_checker=1`.
