# Selected routing, rollback blockers and terminal unit walls have exact executable ancestry

This chapter records **CMR3846--CMR3865** and installs source theorems CMR1094--CMR1165.

Canonical checker:

```text
scripts/check_prime_power_selected_scheduler_terminal_wall_ancestry.py
```

Contract:

```text
f10e632f119fdd56a830ddeb8b9d15d18769f59e703d0dad7a59f243a7c8246c
```

## CMR3846--CMR3849 — selected routing and parameter-free stocks

All 247 matchable side-three hosts select one actual minimum matching, restrict immediately to its routing skeleton and factor exactly over the realised strict children. No routing-history recurrence is used on this selected lineage. Lambda-free owner, protected-growth and deletion-root formulas are checked through side twenty.

## CMR3850--CMR3852 — permanent minimum losses and fixed-core reopening

Nested normalized host restrictions make every canonical lost-minimum edge permanent and distinct inside its segment. The finite fixture checks 6,480 nested transitions. Every compatible common prescription contracts exactly; 10,584 later-host comparisons recondition the core when the lifted anchor survives or expose a genuine missing anchor edge.

## CMR3853--CMR3855 — rollback support and blocker covers

Every infeasible bank state has nonempty missing support. Forty-one normalized bank fixtures receive distinct greedy blocker covers. All 232 restoration subsets are bulk-redeleted while preserving the stored anchor, and 960 missing-support incidences are regenerated.

## CMR3856--CMR3860 — selected scheduler currencies and terminal Hall walls

The parameter-free owner, protected, deletion, minimum-loss, blocker and contracted-rank currencies are checked over sixty parameter profiles. All 12,013 blocked degree-two side-three graphs receive a canonical Hall witness, exact cross-cut containment and quantitative row/column wall bounds.

## CMR3861--CMR3863 — minimal blocker unit walls

Among the blocked graphs, 1,095 inclusion-minimal covers are exact Hall cuts of deficiency one. Every restored blocker is essential, producing 1,863 distinct private matching incidences and the exact unit-wall child-side identity.

## CMR3864 — terminal certificate response banks

All 576 ordered degree-two side-four response boards are generated. They contain 1,944 perfect matchings, support 2,304 fixed-target response slots and 576 loaded-line bank profiles. Feasible responses enter the minimum scheduler; completely blocked banks enter the minimal unit-wall descent.

## CMR3865 — honesty boundary

```text
minimum_selected_routing_normalization_exact = 1
minimum_loss_normalization_exact = 1
fixed_core_reopening_normalization_exact = 1
target_bank_rollback_support_exact = 1
rollback_blocker_cover_normalization_exact = 1
selected_scheduler_finite_response_exact = 1
terminal_blocker_hall_wall_exact = 1
minimal_blocker_unit_wall_exact = 1
terminal_certificate_bank_descent_exact = 1
selected_scheduler_terminal_wall_ancestry_proved = 1

global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

The checker closes the literal CMR1094--CMR1165 local scheduler and terminal-wall operations. It does not prove that the later universal-range, finite-base, collateral and spectral operations are exhausted.
