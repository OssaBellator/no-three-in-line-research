# Completion-attempt evidence gate

`docs/591--595` test the finite obligations left by the replacement-search tranche. They produce exact obstructions and one conditional lattice completion, but no complete coordinate source path for a global ledger row.

## 1. Completion vector

### Theorem PP3ctg -- PROVED / FINITE COMPLETION-ATTEMPT RECORD

The five frontier outcomes are:

```text
boundary: 12/12 exact saturation refills fail;
Hall: every complete grid bijection contains six singleton-blocker choices;
threshold: nearest legal replacements have loss (2,1) or (3,0);
prefix: convex interval chords have zero collinear support triples;
shell: a unit odd-coset action repairs the lattice conditionally at cost 1/20.
```

Candidate source-field completion remains

```text
23/30.
```

No new actual coordinate source path is completed.

#### Proof

Each line is certified by one exact checker in `docs/591--595`. None supplies the missing prime-patching coordinate identification or source price required for an actual row. ∎

## 2. Arithmetic ledger remains unchanged

### Theorem PP3cth -- PROVED / FIXTURE FIXED-POINT CONTINUITY

The fixture-derived coupled system retains exact total

```text
705466760524005697 / 3623878655999606784
```

and exact slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

#### Proof

The completion attempts change no accepted direct row or coupling coefficient. The exact fixture system is therefore identical to that of `docs/590`. ∎

## 3. Evidence-gated nonpromotion

### Theorem PP3cti -- PROVED / ZERO GLOBAL ROWS PROMOTED

All six actual direct ledger rows remain `fixture_derived`. Positive fixture arithmetic slack and conditional completion mechanisms do not imply geometric closure. The all-`n` prime-patching theorem remains open.

#### Proof

Boundary lacks a saturation-preserving repair; Hall lacks a uniformly robust actual host; threshold lacks a source price; prefix lacks a genuine collinearity-producing support decoder; shell lacks a source odd-coset action; and integration lacks geometric couplings and finite residual certificates. ∎

## 4. Machine-readable record

The evidence record is

```text
certificates/prime-patching-completion-attempts-591-596.json
```

and is checked by

```bash
python scripts/check_completion_attempt_evidence_gate.py
```

The complete group runner is

```bash
python scripts/check_frontier_591_596.py
```

## 5. Prime-patching consequence

The current frontiers are no longer blocked by unspecified finite searches. Each remaining obligation is structurally explicit: wider boundary replacement, a richer Hall host, a priced threshold mass transfer, a nontrivial support incidence geometry, a source odd-coset shell action, and finally source-derived coupled ledger coefficients.
