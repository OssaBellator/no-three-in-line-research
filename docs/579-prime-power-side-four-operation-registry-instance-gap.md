# Side-four operation registry instance gap

The installed 1166-kind operation registry is exhaustive at the operation-kind level through CMR1965, but it is not a recurrent transition-instance table. Registry entries contain operation names, theorem sources, owner effects, payment classes, continuations and contract seals; they do not contain exact recurrent-state keys, installed weights, occurrence witnesses, normalization identifiers, recurrent-block identifiers or parent-rule witnesses.

Checked files:

```text
data/prime_power_side_four_operation_registry_instance_gap_contract.json
scripts/check_prime_power_side_four_operation_registry_instance_gap.py
```

The result is a typed source exclusion, not an incompatibility proof.
