# Side-seven semantic uncovered-top expansion: first 16 references

This chapter starts semantic-union expansion from clean top orders outside the established 192-reference union for multiplicity-two case zero, orientation three. It is a proof-compression result, not a classification of additional signatures.

## PX1194 — canonical uncovered reference tranche

The established 192-reference vocabulary has 150 relaxed keys and covers 204 of 35,112 clean top orders. In canonical top-enumerator order, the first 16 top orders outside that union are exactly indices `192` through `207`.

All 16 references were minimized selector-by-selector and replayed against their complete clean extension families. Reference `197` reuses the new key first generated by reference `192`; the other 15-reference addresses contribute distinct keys relative to the incrementally expanded vocabulary.

## PX1195 — semantic union expansion

The tranche adds 15 distinct relaxed keys:

- vocabulary: `150 -> 165` keys;
- exact union: `204 -> 221` clean top orders;
- union growth: 17 clean top orders;
- remaining uncovered complement: `34,891` clean top orders.

The 16 references have 20 total clean extensions. Their pair-mask size distribution is

`5:1, 6:11, 7:4`.

The fact that 16 new references yield 17 new covered top orders is the first direct expansion beyond the original 204-top island.

## PX1196 — exact bottom replay

All selector covers were replayed against every extension using `201,600` exact bottom checks. The exact expansion digest is

`16150749401146711547`.

GitHub Actions run `30745334036` produced the measurement transcript. The deterministic expected-output replay then passed in run `30745396363` before promotion.

## Verification

```bash
python scripts/verify_product_side_seven_multiplicity2_case0_orientation3_semantic_uncovered16.py
```
