# Replacement-search evidence gate

`docs/585--589` replace several incompatible fixtures by sharp finite repair or
replacement statements.  This chapter updates the evidence ledger without
promoting a benchmark as a prime-patching row.

## 1. Replacement outcomes

### Theorem PP3cso -- PROVED / FIVE-FRONTIER REPLACEMENT CERTIFICATE

The current exact replacement outcomes are:

```text
boundary:  minimum three destructive point deletions for four sixth-step cases;
Hall:      six robust and six one-cell-fragile decoded pairs;
threshold: nearest fully legal conservative matrix has l1 distance six;
prefix:    exact ancestry-labelled interval-span aggregate;
shell:     index-two action-to-cycle resource lattice.
```

Each statement has an independent checker and a finite witness or obstruction.

#### Proof

Combine `PP3crz--PP3csn` and their stored audits. ∎

### Theorem PP3csp -- PROVED / UNCHANGED FIELD COMPLETION

Candidate source-field completion remains

```text
boundary    3/5,
Hall        4/5,
threshold   4/5,
prefix      5/5,
shell       5/5,
integration 2/5,
```

or `23/30` in total.  The new work sharpens the missing maps but does not finish
one of them:

- boundary repairs still need row/column refill;
- Hall still needs the actual host exclusion table;
- threshold replacement mass lacks an inequality source;
- prefix intervals lack a grid chord embedding;
- shell unit debts fail the source lattice type test.

#### Proof

No previously missing promotion field obtains every required source identifier,
coordinate map, and geometric verification. ∎

## 2. Arithmetic remains separate

### Theorem PP3csq -- PROVED / ZERO-PROMOTION FIXED-POINT GATE

All six direct ledger rows remain `fixture_derived`, so zero rows are promoted.
The stored fixture fixed point remains

```text
705466760524005697 / 3623878655999606784
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

Positive arithmetic slack therefore still does not imply geometric closure.

#### Proof

The checker verifies the evidence labels and exact rational subtraction.  The
closure rule requires both arithmetic feasibility and complete geometric source
paths. ∎

## 3. Machine-readable audit

The replacement record is

```text
certificates/prime-patching-replacement-search-585-590.json
```

and the group runner is

```bash
python scripts/check_frontier_585_590.py
```

## 4. Prime-patching consequence

The all-`n` theorem remains open.  The strongest next moves are no longer vague:
construct a saturation-preserving boundary refill after the sharp three-point
repair, select a Hall decoder robust to the actual host exclusions, derive a
legal threshold matrix from genuine inequalities, embed the ancestry intervals
as support chords, and identify clean-macro resources inside the even-sum shell
lattice.
