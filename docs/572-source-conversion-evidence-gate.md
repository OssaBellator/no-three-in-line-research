# Evidence gate after source-level conversions

`docs/567--571` replace several free-standing fixtures by repository-typed or
source-derived benchmark data.  This chapter recomputes the evidence gate without
confusing those improvements with geometric realization of the six global loss
rows.

## 1. Frontier conversion status

### Theorem PP3cqm -- PROVED / SOURCE-CONVERSION FIELD AUDIT

The thirty candidate extraction fields retain twenty-two completed fields.  The
new results refine their quality but do not complete any missing prime-patching
coordinate map:

1. boundary: locally legal band offsets exist, but the shortest bounded cycles
   fail globally;
2. Hall: quotient choices biject with complete-grid pairs, but endpoint cells are
   unidentified;
3. threshold: one source matrix and its observables are aligned, but the actual
   residual normal list is absent;
4. prefix: dual grading and structural risk are exact, but geometric risk decoding
   is absent;
5. shell: service incidence is source-derived, but its prime-patching shell map is
   absent;
6. integration: only the benchmark fixed point is sourced.

#### Proof

Apply the field requirements fixed in `docs/555--566` to each new certificate.
No previously missing geometric coordinate field is supplied, although several
benchmark interfaces are strengthened. ∎

## 2. Arithmetic feasibility remains unchanged

### Theorem PP3cqn -- PROVED / FIXTURE FIXED-POINT STABILITY

The stored six-row coupling system still has exact minimal total

```text
705466760524005697 / 3623878655999606784
```

and positive margin

```text
200502903475895999 / 3623878655999606784
```

below `1/4`.

#### Proof

The tranche changes provenance and benchmark decoders, not the stored base rows
or coupling coefficients.  Exact rational iteration, equivalently solving the
single directed coupling cycle, reproduces the stated fixed point. ∎

## 3. Closure gate remains closed

### Theorem PP3cqo -- PROVED / NO ROW PROMOTION FROM BENCHMARK CONVERSION

No actual global row is promoted by `docs/567--571`.  All six row evidence levels
remain `fixture_derived`, and geometric closure remains false.

#### Proof

Each row still lacks at least one machine-checkable map to a prime-patching
coordinate object, loss signature, or coupling event.  Source-derived benchmark
consistency is weaker than geometric verification, so the evidence meet cannot
increase. ∎

## 4. Exact diagnostic

Run

```bash
python scripts/check_source_conversion_evidence_gate.py
```

or the complete tranche with

```bash
python scripts/check_frontier_567_572.py
```

## 5. Prime-patching consequence

The branch now distinguishes three useful achievements: local coordinate
obstructions, repository-typed decoders, and source-derived benchmark models.
None is silently promoted to an actual loss row.  The all-`n` theorem remains
open.
