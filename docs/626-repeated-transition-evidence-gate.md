# Repeated-transition evidence gate

This chapter records the consolidated evidence status after `docs/621--625`
without upgrading finite or auxiliary-source results into global prime-patching
evidence.

## 1. Candidate-field ledger

### Theorem PP3cws -- PROVED / CANDIDATE COVERAGE IS 25 OF 30

The candidate source-field scores are

```text
boundary    4/5
Hall        4/5
threshold   5/5
prefix      5/5
shell       5/5
integration 2/5
```

for a total of `25/30`.

The threshold candidate gains one field because every nearest legal target has a
complete repository-native two-swap source-matrix path.  The strengthened
boundary, Hall, prefix, and shell results sharpen interfaces but do not complete
another candidate field.

#### Proof

The evidence checker reconstructs the six candidate scores and verifies their
sum.  ∎

## 2. Actual evidence gate

### Theorem PP3cwt -- PROVED / ZERO GEOMETRIC ROW PROMOTION

All six actual ledger rows remain `fixture_derived`; no row is promoted to
`geometrically_verified`.

The unresolved source paths are:

- boundary: the budget-seven corrected path reaches nine blocks but has no tenth
  transition at that budget;
- Hall: neither the bad-vertex bound nor the forty-two-resource degree-two reserve
  is derived from the asymptotic conditional host;
- threshold: every two-swap path has an illegal visible intermediate and no
  atomic geometric compound is known;
- prefix: the parabola reservoir is not the saturated PP3 retained source, while
  the internal nineteen-cell reservoir fails on a positive fraction;
- shell: the optimal `(1,1,1)` all-cycle action has no clean-macro realization;
- integration: no source-derived coupling coefficients are available.

#### Proof

Each row fails at least one complete coordinate source-path requirement, so the
strict provenance meet rejects every promotion.  ∎

## 3. Fixed-point arithmetic

### Theorem PP3cwu -- PROVED / ARITHMETIC SLACK UNCHANGED

The fixture fixed-point total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

The arithmetic closure test remains positive, but geometric closure is false.

#### Proof

The checker reconstructs and sums the six exact rational fixture coordinates,
then evaluates the evidence meet separately.  ∎

The all-`n` theorem remains open.
