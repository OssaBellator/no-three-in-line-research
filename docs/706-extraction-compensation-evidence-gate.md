# Extraction-compensation evidence gate

This chapter integrates `docs/701--705` under the strict promotion rule: a row is
promoted only by a complete host-derived coordinate path with recurrent or
asymptotic continuation.

## PP3dfy — Exact advances in the current tranche

The tranche adds five exact results:

1. all 178 minimum-six twenty-second cores reject preserving repairs through
   budget seven, totaling 80,525,340 new rejected permutations;
2. a thirty-centre degree-two Hall packet retaining at least twenty-eight must
   have one of nine component signatures and at least twenty-four isolated
   centres;
3. two nonnegative threshold separators force every equality multiple to have
   scale `8K`, hidden count `3K`, and four copies of each of the same five legal
   primitive types;
4. every optimal prefix route passes all 1,024 compositions on the fixed
   thirteen-pair source, totaling 7,421,952 distinct coordinate embeddings;
5. three-state connector-tour optimization is classified over all 262,144 small
   directed burden tables, including exact joint repetition objectives.

These close finite layers and sharpen extraction interfaces. None supplies a
host-derived all-length coordinate construction.

## PP3dfz — Candidate ledger remains `25/30`

```text
boundary     4/5
Hall         4/5
threshold    5/5
prefix       5/5
shell        5/5
integration  2/5
----------------
total       25/30
```

All six actual evidence rows remain `fixture_derived`, and the promoted set is
empty.

- Boundary obstruction is stronger, but a budget-seven transition from a raw
  minimum-seven attempt, a budget-eight repair, and every recurrence question
  remain open.
- Hall has a sharp host filter, but no prime-patching host supplies the required
  ten resource-disjoint motifs and private defect labels.
- Threshold equality multiples are rigid only within the current primitive
  alphabet and exposed rolling model.
- Prefix is complete for one fixed source size, but no compatible larger reservoir
  or source-size recurrence is known.
- Shell retains exact uncertainty vectors, but no coordinate macro graph supplies
  the burden and robust gain data.

Therefore geometric closure remains false.

## PP3dga — Fixed point and promotion gate remain unchanged

The exact fixture total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

No coupling coefficient changes because no result in this tranche supplies a
complete recurrent or asymptotic host-derived coordinate path. The all-`n`
theorem remains open, and the next theorem identifier is `PP3dgb`.

The exact audit is `scripts/check_extraction_compensation_gate_706.py`.
