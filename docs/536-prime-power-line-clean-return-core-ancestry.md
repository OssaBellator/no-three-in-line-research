# Installed line-clean and return-core ancestry

This chapter installs the executable ancestry represented by source CMR1510--CMR1581. It binds the nine literal source chapters and their nine finite verifier entrypoints to one checker contract:

```text
scripts/check_prime_power_line_clean_return_core_ancestry.py
0a1620bf756ec529255be95c072a8435d870762644908a5000418d084948aec4
```

The installed object is a recurrent-row reduction and exact coefficient interface. It is not a proof that the resulting line-clean or return blocks are subcritical.

## Theorem CMR4182 -- PROVED

The checker binds exactly these source chapters:

```text
docs/292-prime-power-extension-free-partial-matching-line-clean.md
docs/293-prime-power-repeated-token-atomic-compression.md
docs/294-prime-power-extension-free-line-clean-rook-rows.md
docs/295-prime-power-line-clean-uniform-permanent-envelope.md
docs/296-prime-power-line-clean-factor-signature-sharpening.md
docs/297-prime-power-line-clean-singleton-fractional-factor.md
docs/298-prime-power-paid-pair-selector-return-splice.md
docs/299-prime-power-trace-centre-line-clean-splice.md
docs/300-prime-power-returned-edge-exchange-kernel.md
```

No later source range is silently included.

## Theorem CMR4183 -- PROVED

For every extension-free response side at least four, deleting any allowed partial matching preserves a perfect response. The complete allowed trace of one nonaxis real line is such a partial matching.

## Theorem CMR4184 -- PROVED

A target-safe line-clean response selects no response-layer cell on the cleaned line. Consequently its same-owner, same-line collateral coordinate is exactly zero. Off-line collateral remains and is not discarded.

## Theorem CMR4185 -- PROVED

Repeated full-token recurrence compresses to three atomic recurrent currencies:

```text
labelled repeated-edge return
one jointly persistent paid-pair selector
one fixed trace-line or trace-incidence signature
```

Finite token-edge stock and successful persistent-edge absorption do not create extra diagonal rows.

## Theorem CMR4186 -- PROVED

Every line-clean response count and every compatible rank-one through rank-three prescription probability has an exact degree-two component rook formula. The forbidden board factors into alternating paths and even cycles, with one target-edge deletion--contraction step.

## Theorem CMR4187 -- PROVED

Every line-clean host has a spanning `(d-3)`-regular core and the uniform permanent coefficient

\[
\left(\frac d{d-3}\right)^d.
\]

The resulting rank and restricted-host bounds dominate the exact off-line rook row but need not be sharp.

## Theorem CMR4188 -- PROVED

Every line-clean trace belongs to exactly one uniform coefficient class:

```text
strong derangement-extendable trace: (d/(d-2))^d
singleton opposite-edge remainder: (d(d-2)/((d-1)(d-3)))^d
target-endpoint-overlap trace: (d/(d-3))^d
```

The exact component rook ratio remains available in every class.

## Theorem CMR4189 -- PROVED

For an exact paid-pair selector with candidate expectation below one, the least strict threshold gives a candidate-free completion with an explicit finite restoration cap.

## Theorem CMR4190 -- PROVED

First restoration labels form a finite monotone resource stock. After refining the state by that stock, only repeated restoration incidences remain in the recurrent return row.

## Theorem CMR4191 -- PROVED

If the repeated-return row is `(alpha,beta)` in return/selector columns and the selector-to-return cap is `T`, the exact recurrent block

\[
\begin{pmatrix}
\alpha&\beta\\
T&0
\end{pmatrix}
\]

is subcritical exactly when

\[
\alpha+\beta T<1.
\]

After clearing denominators this is one strict integer inequality.

## Theorem CMR4192 -- PROVED

A recurrent fixed trace signature has at most `t^2` rooted centres in one side-`t` envelope epoch. Excess recurrence fixes one centre and therefore one nonaxis real trace line.

## Theorem CMR4193 -- PROVED

When the fixed rooted centre may be used as target, the complete trace is target-disjoint. Its recurrent execution row is therefore strong or singleton; the endpoint-overlap coefficient cannot occur.

## Theorem CMR4194 -- PROVED

For a transition between two perfect matchings, leaving and entering edges have canonical same-source and same-target exchange bijections. Every exchange pair lies on one alternating cycle.

## Theorem CMR4195 -- PROVED

Every recreated physical credit has one absolute last-entering owner and is transported uniquely to that owner's same-source returned predecessor. This gives an exact classwise returned-edge kernel, not a total-churn surrogate.

## Theorem CMR4196 -- PROVED BY EXECUTABLE CONTRACT

The consolidated checker verifies source presence, compiles and executes all nine inherited verifier entrypoints, binds the contract digest and rejects thirteen independent fixture corruptions.

The checker source and mutation audit were validated locally. The full nine-verifier execution was not observed in the current environment.

## Theorem CMR4197 -- HONEST ENDPOINT

The installed bank proves the exact coefficient and ancestry interfaces above while preserving:

```text
line_clean_recurrent_rows_subcritical = 0
return_selector_block_subcritical = 0
critical_selector_candidate_regime_closed = 0
trace_without_root_target_execution_closed = 0
same_owner_diagonal_blocks_subcritical = 0
global_target_collateral_inequality_proved = 0
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

The remaining task is numerical: bound the exact line-clean and exchange rows strongly enough to prove every recurrent class strict, while retaining fixed-interface, thin and CRT labels.
