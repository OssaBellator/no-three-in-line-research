# ERL draft integration scope

This file marks the current review boundary for the exact recurrent Lyapunov
research branch. The branch contributes finite local classifications, exact
lineage projections, background-sensitive score compilers and honesty-preserving
certificate formats. It does not claim legal global reopening, recurrent
subcriticality, termination, arbitrary side-length transfer, or the
no-three-in-line conjecture.

The integer two-point strict-reversal classification for first host
`s4-75b04c45c1c8eac2` is complete: exactly four strict backgrounds exist, and
`3210` is never uniquely minimal. The current side-four host/response projection
cannot distinguish a safe completion from a strict-reversal completion because
it stores no coordinate-labelled background.

The next integration gate is therefore a source-backed complete physical fibre
for deletion set `{02,20}` containing backgrounds, deletion causes, physical
owner/provenance labels, installed legal operations and exact child rows.
