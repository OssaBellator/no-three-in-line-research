# ERL draft integration scope

This file marks the current review boundary for the exact recurrent Lyapunov
research branch. The branch contributes finite local classifications, exact
lineage projections, background-sensitive score compilers and honesty-preserving
certificate formats. It does not claim legal global reopening, recurrent
subcriticality, termination, arbitrary side-length transfer, or the
no-three-in-line conjecture.

The integer two-point strict-reversal classification for first host
`s4-75b04c45c1c8eac2` is complete: exactly four strict backgrounds exist over
the full integer lattice, and `3210` is never uniquely minimal. The current
side-four host/response projection cannot distinguish a safe completion from a
strict-reversal completion because it stores no coordinate-labelled background.

The next integration gate is a source-backed complete physical fibre for
deletion set `{02,20}` containing:

1. every coordinate-labelled background occurrence;
2. deletion causes and physical owner/provenance labels;
3. installed legal operations and intermediate states;
4. exact child multiplicities, weights and parent budget.

The four strict backgrounds and infinite tie classes must then be excluded,
rerouted, paid strictly or retained as labelled recurrent states.
