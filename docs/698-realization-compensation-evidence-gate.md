# Realization-compensation evidence gate

This chapter integrates `docs/693--697` under the strict promotion rule used by
the preceding tranches: a row is promoted only by a complete coordinate source
path with exposed-state legality and recurrent or asymptotic continuation.

## PP3dfa — Current frontier advances

The tranche adds five exact results.

1. The canonical eighteenth state is reconstructed with `(42,378)`, all six
   minimum-four nineteenth cores are obstructed through budget six, and a sharp
   budget-seven correction advances the finite chain to nineteen blocks. The raw
   twentieth frontier has nine minimum-four cores.
2. Every six-centre, two-motif source/host defect packet with label loads at most
   two is classified. Exactly 1,636 packet graphs are realizable, and all 6,544
   one-through-four packet transfer values agree with direct independence numbers.
3. The threshold separator applies at every rolling width. For `K` minimum batches
   and width `w`, the exact maximum number of legal windows is
   `max(0,5K-w+1)`.
4. Exact route deduplication leaves 3,624 optimal routes per prefix deletion class.
   Every distinct route passes all 176 compositions with at most four runs,
   covering 5,271,552 physical route/composition pairs.
5. Shell connector optimization is vector-valued under uncertainty. Statewise
   tours, edgewise worst charges, and worst-loss tour selection can all be
   suboptimal for the joint repetition objective.

These results sharpen a finite coordinate transition, exact combinatorial
realization targets, all-width obstructions, bounded coordinate lifts, and robust
execution interfaces. None supplies an all-length coordinate construction.

## PP3dfb — Candidate ledger remains `25/30`

```text
boundary     4/5
Hall         4/5
threshold    5/5
prefix       5/5
shell        5/5
integration  2/5
----------------
total       25/30
```

All six actual evidence rows remain `fixture_derived`, and the promoted set is
empty.

- Boundary reaches nineteen blocks, but no recurrence or periodic state invariant
  continues the correction chain.
- Hall now has a complete two-motif defect-labelled packet target, but no coordinate
  motifs realize it with exact periodic interfaces.
- Threshold closes all rolling widths inside the minimum forced alphabet, but no
  genuinely unexposed operation or enlarged alphabet is constructed.
- Prefix covers every optimal route only through four runs on one fixed source;
  848 longer compositions and all larger source sizes remain open.
- Shell has the exact robust vector tour objective, but no coordinate macro graph
  supplies its component, path-burden, and gain vectors.

Therefore geometric closure remains false.

## PP3dfc — Fixed point and promotion gate remain unchanged

The exact fixture total remains

```text
705466760524005697 / 3623878655999606784,
```

with positive slack below one quarter

```text
200502903475895999 / 3623878655999606784.
```

No coupling coefficient changes because no result in this tranche supplies a
complete recurrent or asymptotic coordinate source path. The all-`n` theorem
remains open, and the next theorem identifier is `PP3dfd`.

The exact audit is `scripts/check_realization_compensation_gate.py`.
