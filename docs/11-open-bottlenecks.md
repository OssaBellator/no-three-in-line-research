# Open bottlenecks and research roadmap

## 1. Current proof status

The no-three-in-line conjecture remains open. The branch now has exact structural
reductions, response laws, line-clean and return certificate forms, selector
capacity compilers, exact geometric multiplicity and line-energy formulas, a
corrected 740-host side-four/five geometric layer, completed rank-three fibre
numerators, nested and line-occupancy assignment compilers, and a label-preserving
auxiliary/CRT assembly protocol.

No theorem yet proves that every positive minimum of the real-triple potential
becomes zero.

The honesty condition is unchanged:

> finite response, finite resource use and structural descent are not by
> themselves potential improvement.

A completion must exhibit a lower-potential response or an exact weighted
inequality guaranteeing one.

## 2. Closed structural components

Last-entering ownership makes the complete offspring matrix block upper triangular
after retaining all labels needed to determine future rows:

\[
\rho(A)=\max_i\rho(A_{ii}).
\]

Strict child descent, earlier-depth transfer, first private-edge/token/
restoration/support/signature use and ordinary later-owner exits are off-diagonal
only when the selected policy executes them. All finite transfer collateral glues
after the genuinely recurrent labelled blocks are certified.

The broad labels “loaded line”, “repeated token”, “diffuse selector”, “root
translation”, “fixed trace” and “return churn” are no longer primitive undefined
rows in the regimes already reduced.

## 3. Exact response and line-clean rows

For response side `d`, opposite matching `O` and target `e`, use

\[
H_e=K_{d,d}\setminus(O\cup\{e\}).
\]

Deleting a complete allowed nonaxis line trace preserves a target-avoiding
response for `d>=4`. Every response denominator and contracted prescription
probability is an exact component-rook computation.

The exact and universal line-clean budget currency is

\[
W_d
=(d-1)(d-2)V_1+(d-2)V_2+V_3+(m+1)b(d-1)(d-2),
\]

with strict test

\[
(dv)^dW_d<D u^d d(d-1)(d-2).
\]

Rank/profile/geometric capacities enter the same integer currency. Failure
localizes one explicit candidate or unavailable-edge class.

## 4. Exact geometric multiplicity and line energy

For fixed background `B`, prescription multiplicities are:

- rank three: one;
- rank two:
  \[
  |B\cap\ell(x,y)|;
  \]
- rank one:
  \[
  \sum_{\ell\ni x}C(|B\cap\ell|,2).
  \]

For actual response `Q`, put

\[
h_\ell=|B\cap\ell|,
\qquad
k_\ell=|Q\cap\ell|.
\]

Then

\[
\Psi(B\cup Q)-\Psi(B)
=
\sum_\ell
\left[
 k_\ell C(h_\ell,2)
 +C(k_\ell,2)h_\ell
 +C(k_\ell,3)
\right].
\]

Packed-height, background-potential and bounded-congestion current-triple charges
remain available. Background triples become payment only through an explicit
current labelled charge map.

## 5. Corrected geometric host layer

Matching normalization remains exact for denominators but not for Euclidean
collinearity. The residual geometric stabilizer fixing `0,1` is trivial for every
`d>=3`.

The matching denominator classes expand exactly to:

### Side four

\[
4\cdot1+41\cdot2=86
\]

raw coordinate-labelled hosts.

### Side five

\[
2\cdot1+2\cdot2+24\cdot3+96\cdot6=654
\]

raw coordinate-labelled hosts.

Thus the first corrected geometric layer contains exactly

\[
\boxed{740}
\]

raw hosts before owner and provenance refinements. Denominators are reused inside
matching fibres; geometric numerators are computed separately.

## 6. Exact marginal and nested assignment compilers

Define

\[
a_1(x)=\sum_{\ell\ni x}C(|B\cap\ell|,2),
\]

\[
a_2(\{x,y\})=|B\cap\ell(x,y)|,
\]

and let `a_3(P)` indicate a collinear response triple.

For a uniform host,

\[
A_{\mathrm{line}}
=
\sum_xa_1(x)z(x)
+
\sum_{|P|=2}a_2(P)z(P)
+
\sum_{|P|=3}a_3(P)z(P),
\]

with strict certificate

\[
A_{\mathrm{line}}<ZD.
\]

The higher ranks may instead be peeled into ordinary assignments. Put

\[
J_2(e)=\mathcal A_{G/e}(f\mapsto a_2(\{e,f\})),
\]

and

\[
J_3(e,f)=\mathcal A_{G/\{e,f\}}(g\mapsto a_3(\{e,f,g\})),
\]

\[
H_3(e)=\mathcal A_{G/e}(f\mapsto J_3(e,f)).
\]

Then rank two is at most `(1/2)A_G(J_2)` and rank three is at most
`(1/6)A_G(H_3)`. The denominator-cleared strict test is

\[
6l_1+3l_2+l_3<6ZD.
\]

Exact marginals and nested assignments may be mixed by rank or class.

## 7. Completed rank-three fibre table

Across all 740 raw side-four/five hosts,

\[
\Psi(Q)\in\{0,1,2,4\}.
\]

Thirty-seven side-four hosts have rank-three row zero. Every side-five host has a
positive uniform rank-three expectation, but the sharp universal integer cap is

\[
5A_3\le7Z.
\]

Every denominator has a sharper exact integer numerator cap in CMR1809--CMR1810.
Rank three is therefore no longer an open side-four/five computation.

## 8. Line-occupancy capacity compiler

For each exact host and real line, compute

\[
\tau_G(\ell)=\max_{Q\in\operatorname{PM}(G)}|Q\cap\ell|.
\]

This is a zero-one assignment optimum. The complete deterministic energy is
bounded by

\[
\mathcal C_G(B)
=
\sum_\ell
\left[
 \tau_G(\ell)C(h_\ell,2)
 +C(\tau_G(\ell),2)h_\ell
 +C(\tau_G(\ell),3)
\right].
\]

The rank-one part is an explicit sum of line-indicator assignment duals. Rank two
and rank three may be replaced by sharper exact marginal or nested values.

## 9. Unified return-selector-geometric assignment

For selector restoration cap `T`, define one outer score

\[
\gamma_T(e)
=
g_{\mathrm{ret}}(e)
+Tg_{\mathrm{sel}}(e)
+a_1(e)
+J_2(e)/2
+H_3(e)/6.
\]

Every complete response is bounded by the score of its matching. One outer
assignment dual or class-supported cover certificate therefore controls return,
selector and all three geometric ranks simultaneously.

With common denominator `Z`, define

\[
\Gamma_T(e)
=
6(R(e)+T S(e)+A(e))+3J(e)+H(e).
\]

The strict integer certificate is

\[
U_i+V_j\ge\Gamma_T(i,j),
\qquad
\sum_iU_i+\sum_jV_j<6ZD.
\]

This is the preferred current certificate surface. It avoids adding incompatible
outer maxima.

## 10. Owner-support, selector and auxiliary modules

If possible owner edges lie in support `A` of matching number `mu(A)`, distinct
rank-`r` owned prescription mass is at most

\[
\mu(A)C(d-1,r-1).
\]

Prime-field reused-support states have support size one or two when every retained
child owner lies in that support.

For selector denominator `D`, class capacities with total below `D` eliminate
criticality, give an exact restoration cap and feed the unified outer score.

Certified auxiliary modules eliminate through the nonnegative resolvent
`(I-D)^{-1}`. All owner, collision, local-line, interface and CRT labels remain
until exact orbit equivalence or honest domination is proved.

## 11. Genuine recurrent modules

The labelled diagonal quotient now needs certificates only for:

1. **Unified outer assignment blocks.** Return, selector and geometric scores on
   one response host.
2. **Background-populated geometric fibres.** The 740 raw hosts after attaching
   actual line loads and provenance.
3. **Critical-selector survivors.** Classes whose integer capacities do not fall
   below their denominator.
4. **Reused-support survivors.** Small-support rows not closed because of large
   background coefficients or out-of-support owners.
5. **Geometric fixed-interface/thin modules.** Until certified and eliminated by
   exact resolvents.
6. **Residual collision/local-line blocks.** States recurring with every retained
   provenance label unchanged.

## 12. Recommended next lemmas and computations

1. **Background fibre population.** Attach exact background line loads, owner,
   collision, local-line, interface and CRT labels to every raw host.
2. **Rank-one line duals.** Compute `tau_G(ell)` and add line-indicator duals, or
   generate direct `a_1` superlevel covers.
3. **Rank-two nested tables.** Compute `J_2(e)` classwise; compare with exact rook
   marginals and line-occupancy caps.
4. **Insert rank-three caps.** Use the completed denominator-specific `A_3` table
   in every side-four/five row.
5. **Unified outer certificates.** Build `Gamma_T` and search integer vertex
   weights below `6ZD`.
6. **Corrected offspring routing.** Remove old, forced, contracted and duplicate
   triples, then retain absolute owner and provenance labels.
7. **Auxiliary elimination.** Certify geometric thin, fixed-interface and support
   modules and replace them by exact resolvent excursions.
8. **Final SCC certificate.** Publish one strict integer vector for every surviving
   labelled SCC and apply CRT gluing.

## 13. Computational priorities

- Reuse matching denominators, but never use matching-only geometric quotienting.
- Work on the exact 86 side-four and 654 side-five raw hosts.
- Preload the completed rank-three numerator table.
- Compute rank-one line duals and rank-two nested assignments.
- Assemble one unified outer score instead of separate return/selector/geometric
  maxima.
- Preserve owner, collision, local-line and CRT provenance throughout.
- Clear all denominators and independently verify every final strict integer row.

## 14. Current proved endpoint

Through **CMR1829**:

- strict transfers and first-use resources are off-diagonal;
- geometric prescription multiplicity and line energy are exact;
- matching-only symmetry is proved insufficient for geometric rows;
- the corrected side-four/five host layer has exactly 740 raw hosts;
- every raw-host rank-three response numerator is tabled;
- rank-two and rank-three energy admit nested assignment certificates;
- every host has an exact line-occupancy capacity table;
- return, selector and geometric collateral share one outer assignment score;
- certified auxiliary modules eliminate by nonnegative resolvents;
- collision and local-line labels have a proved SCC/CRT gluing protocol.

There is still no complete proof. The next genuine advance is to populate the 740
hosts with the actual background/provenance data and publish the first unified
outer integer certificates.
