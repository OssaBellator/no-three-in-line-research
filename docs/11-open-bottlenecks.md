# Open bottlenecks and execution roadmap

## 1. Current proof status

The no-three-in-line conjecture remains open. The authoritative theorem ledger reaches **CMR3701**. Every checker, bridge, fixture, runtime manifest and regression preserves:

```text
all_n_proved_by_checker = 0
```

No finite census, registry digest, manifest or workflow result proves the all-`n` theorem.

## 2. Canonical validation surface

```text
runner = scripts/run_prime_power_installed_construction_regression_239.py
manifest = fdad5ad0e23bd87f4b3f00d736ef70435b6d117e6822b4dc79f5a24557cea315
operation kinds = 239
checker contracts = 25
owner-changing kinds = 77
same-owner kinds = 162
checkers = 43
```

Newest direct entrypoints:

```text
scripts/check_prime_power_target_anchor_lineage_ancestry.py
scripts/check_prime_power_complete_branch_distinguishing_rank.py
scripts/check_prime_power_installed_operation_registry_239.py
```

Inspect actual workflow runs before claiming CI success.

## 3. T01 source truth

The source registry remains infrastructure only. Completion requires authoritative primary sources, stable locators, exact transcription, matching hashes, ordinary mathematical verification and human review.

## 4. T02 installed construction surface

### Through CMR3585

Typed contexts, routing, deletion/restoration, envelopes, Hall walls, rollback normal forms, line-clean cylinders, persistent crosses, canonical selectors, protected interfaces and CMR629--CMR690 product/factor/child ancestry are installed.

### CMR3586--CMR3609

CMR691--CMR747 are sealed against exact inherited owner-stock, target-handoff, recurrent-target, returned-edge and unit-wall contracts. Eleven existing kinds cover the operational range without duplicate registration.

### CMR3610--CMR3657

CMR748--CMR829 install:

```text
protected target-line reserves and pair neutralization
absence-run and neutralization-certificate ledgers
labelled target-pair deletion or double contraction
owner-independent forward return ancestry forests
anchor private batches, restoration and bulk redeletion
idempotent private normalization and active-context absorption
finite physical-edge structural owner lineage
```

### CMR3658--CMR3701

CMR830--CMR853 correct completeness. The aggressive anchor-batch pass remains a valid structural subbranch, but the complete response is the union of viable single-edge children. Full common-core contraction leaves a core-free residual family. Minimum child-cover width is the distinguishing transversal number; for a full matching factor it is exactly the directed feedback-vertex number of the exchange digraph.

Current exact flags include:

```text
cmr691_747_existing_operation_coverage_exact = 1
target_anchor_lineage_ancestry_proved = 1
complete_state_exclusion_branching_exact = 1
viable_branch_essential_core_exact = 1
distinguishing_rank_exchange_digraph_exact = 1
complete_branch_distinguishing_rank_proved = 1
installed_transition_kind_bank_239_exhaustive = 1
installed_payment_assignment_239_complete = 1
installed_transition_regression_239_complete = 1

all_owner_operations_proved = 0
all_scheduler_operations_proved = 0
all_restoration_operations_proved = 0
all_returned_edge_operations_proved = 0
all_envelope_operations_proved = 0
all_construction_ancestry_proved = 0
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

Installed-bank exhaustiveness is not global construction exhaustiveness.

## 5. Immediate operation audit: CMR854 onward

Audit the coupled two-layer distinguishing-width chapters in literal theorem order. Install every genuine operation involving:

```text
low-rank distinguishing prescriptions
coupled exchange support and cycle transversals
conflict-load concentration
compressed viable branch covers
complete branch-tree width and scheduler accounting
later owner, restoration, return, envelope and scheduler actions
```

For each operation record the literal parent, exact child family, theorem-derived owner effect, branch-cover/stock/reintroduction/descent payment, contract and corruption rejection.

## 6. Global blockers

After all operations are installed:

1. prove directly that every original construction step belongs to exactly one registered kind;
2. close recurrent routing, restoration, target, interface, branch-width and scheduler endpoints;
3. combine strict descents, finite stocks and complete branch-cover bounds into global termination.

## 7. Downstream frontiers

T03--T04 require genuine operation populations. T05--T19 require arbitrary-`n` coverage and semantic proofs. All T20/T21 chamber arguments, T22--T31 premises, T35--T40 handoffs, T41 review, T42 sign-off and T43 remain open. T32--T34 are documentary aggregation gates only.

No local checker, registry or runtime manifest substitutes for those proofs.
