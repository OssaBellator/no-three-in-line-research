# Open bottlenecks and execution roadmap

## 1. Current proof status

The no-three-in-line conjecture remains open. The authoritative theorem ledger reaches **CMR4421**. Every checker and manifest preserves `all_n_proved_by_checker = 0`.

## 2. Canonical validation surface

```text
runner = scripts/run_prime_power_installed_construction_regression_1030.py
manifest = cb57bcbf2eb5278b8265975f948fec9970ba8539c4bc3c7e1ff12f5128d31626
operation kinds = 1030
checker contracts = 40
owner-changing kinds = 164
same-owner kinds = 866
checkers = 73
```

## 3. Installed geometric-fibre surface

Source CMR1774--CMR1829 corrects matching normalization to coordinate-labelled geometric fibres, installs exact marginal and nested line-energy certificates, enumerates 740 side-four/five raw hosts, tabulates rank-three fibres, computes line-occupancy capacities and combines return, selector and geometric terms in one outer assignment.

The certificate surfaces are exact. Complete provenance population and global strictness remain open.

## 4. Active frontier

```text
occupancy moment census
label-weighted unified assignment LP
full-line diagonal parity laws
moment Pareto envelopes
executable labelled assignment manifest
rank-three fibre slack classes
response-averaged line moments
rank-three slack line-budget allocation
```

## 5. Exact blockers

1. Populate exact geometric fibres with every owner, collision, line, interface and CRT provenance label.
2. Certify rank-one and rank-two fibre rows or dominate them by exact moment/occupancy certificates.
3. Construct a globally strict unified outer assignment with one common response law.
4. Use rank-three slack to pay the remaining background-dependent ranks without double counting.
5. Preserve labelled recurrent SCCs through auxiliary elimination and CRT assembly.
6. Prove global transition exhaustiveness and global termination.

## 6. Validation boundary

```text
checker contract = locally reproduced
registry 1030 contract/census/seal = locally reproduced
runner 1030 manifest = locally reproduced
seven-verifier checker = not executed end to end locally
complete 73-checker runner = not executed locally
workflow success = not observed
```

Workflow configuration is not CI success. No local checker or manifest substitutes for the missing global proofs.
