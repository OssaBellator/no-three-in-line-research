# Open bottlenecks and execution roadmap

## 1. Current proof status

The no-three-in-line conjecture remains open. The authoritative theorem ledger reaches **CMR4373**. Every checker and manifest preserves `all_n_proved_by_checker = 0`.

## 2. Canonical validation surface

```text
runner = scripts/run_prime_power_installed_construction_regression_974.py
manifest = 58a5c6eaa877331a9c4711835d9a3ce84c9def64001eea9ffb301325b7620be5
operation kinds = 974
checker contracts = 39
owner-changing kinds = 164
same-owner kinds = 810
checkers = 71
```

## 3. Installed large-load and multiplicity surface

Source CMR1702--CMR1773 installs exact rank-mass conservation, multiplicity-aware line-clean and owner-support thresholds, exact geometric multiplicity formulas, packed and potential-only bounds, labelled background-triple charges and exact line-energy profiles.

Strict responses are proved only when the displayed host, destroyed-load, owner-support and multiplicity/profile hypotheses hold.

## 4. Active frontier

```text
geometric orbit-fibre correction
exact marginal rook line-energy compiler
nested higher-rank assignments
coordinate-labelled host and rank-three censuses
line-occupancy capacities
unified outer return-selector-geometric score
```

## 5. Exact blockers

1. Matching-level normalization must be expanded to geometric fibres before using collinearity numerators.
2. Every residual large-load failure must be controlled by exact marginal, nested-assignment, occupancy or host-census certificates.
3. Background-triple and support charges must retain labelled ownership and congestion.
4. A unified outer score must preserve return, selector and geometric provenance without adding incompatible maxima.
5. Global transition exhaustiveness and termination remain open.

## 6. Validation boundary

```text
checker contract = locally reproduced
registry 974 contract/census/seal = locally reproduced
runner 974 manifest = locally reproduced
nine-verifier checker = not executed end to end locally
complete 71-checker runner = not executed locally
workflow success = not observed
```

Workflow configuration is not CI success. No local checker or manifest substitutes for the missing global proofs.
