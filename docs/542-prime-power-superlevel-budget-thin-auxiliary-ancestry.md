# Installed superlevel, budget, thin-census and auxiliary ancestry

```text
checker = scripts/check_prime_power_superlevel_budget_thin_auxiliary_ancestry.py
contract = e155ea311c24a9f04e1a603877190d1e60f9928a4607546635a9198344e53ad0
source = CMR1630--CMR1701
```

## Theorem CMR4278 -- PROVED

The checker binds exactly docs 307--315 and their nine literal verifier entrypoints.

## Theorem CMR4279 -- PROVED

Every combined return-selector edge score has an exact finite superlevel decomposition. Superlevel matching numbers and König vertex covers give honest assignment upper bounds.

## Theorem CMR4280 -- PROVED

A weighted superlevel-cover sum below one is a direct strict certificate for the complete coupled return-selector block. Common-denominator clearing gives a strict integer certificate.

## Theorem CMR4281 -- PROVED

Geometric class score caps and source/target support covers compile to nested superlevel covers or an equivalent rational assignment dual. Return and selector maxima are never optimized separately.

## Theorem CMR4282 -- PROVED

The strong, singleton and endpoint-overlap line-clean permanent ratios are increasing for all response sides at least four. Their universal floors are respectively

```text
1/16
81/4096
1/256
```

## Theorem CMR4283 -- PROVED

Those floors give host-uniform integer line-clean budgets, rank-pure ranges and unavailable-edge reserves. Passing the budget proves strict improvement; failing it does not.

## Theorem CMR4284 -- PROVED

Finite selector class capacities sum to an exact rational upper bound. Positive integer slack below the response denominator excludes criticality and gives an explicit restoration cap.

## Theorem CMR4285 -- PROVED

Mixed exact and residual selector capacities may be combined. A critical selector must retain at least one class meeting the exact capacity threshold.

## Theorem CMR4286 -- PROVED

Matching-level fixed-interface boards normalize exactly to identity opposite matching and target `(0,1)`, with residual stabilizer `S_(d-2)`. Canonical orbit coding preserves every retained label.

## Theorem CMR4287 -- PROVED

The normalized thin response census through side five has 4, 45 and 124 executable canonical hosts on sides three, four and five. Side two has no extension-free response and side three is forced.

## Theorem CMR4288 -- PROVED

After forced contractions, the exact matching-level rank caps are

```text
side 4: 3/4, 2/3, 1/2
side 5: 2/3, 2/5, 1/4
```

for ranks one, two and three.

## Theorem CMR4289 -- PROVED

The rank-one/two census checks 12,295 extendable prescription instances. The rank-three census checks 15,469 instances including the four forced side-three prescriptions.

## Theorem CMR4290 -- PROVED

Rank/profile, owner, height, token, prefix, carry and availability capacities compile into one exact weighted line-clean count. Budget failure localizes to one explicit overflow class.

## Theorem CMR4291 -- PROVED

A certified auxiliary recurrent module eliminates exactly by the nonnegative rational resolvent `(I-D)^(-1)`. Full-block subcriticality is equivalent to subcriticality of the effective core `A+B(I-D)^(-1)C`.

## Theorem CMR4292 -- PROVED

Effective-core certificates lift constructively to the full labelled block and clear to finite strict integer inequalities. Independently certified auxiliary blocks eliminate additively.

## Theorem CMR4293 -- HONEST ENDPOINT

The checker contract and sixteen fixture corruptions are exact while preserving:

```text
return_superlevel_cover_globally_strict = 0
universal_line_clean_budgets_close_all_classes = 0
selector_capacity_classes_closed = 0
normalized_thin_geometric_rows_subcritical = 0
auxiliary_effective_core_subcritical = 0
same_owner_diagonal_blocks_subcritical = 0
global_target_collateral_inequality_proved = 0
global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

The checker contract was reproduced locally. The complete nine-verifier execution was not observed locally.
