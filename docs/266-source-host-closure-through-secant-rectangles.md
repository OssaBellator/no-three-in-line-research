# Source-host closure through old-grid shadow and secant rectangles

PP3axq--PP3axx correctly separated source-host certificates from the current restart
potential.  The subsequent endpoint-shadow and secant-cover reductions now remove the
need for a separate cumulative source-host hypothesis.

The closure has two parts.

1. Unary retained-pair obstruction is current old-grid endpoint-shadow credit.
2. Every two-inserted-cell source obstruction either admits fixed-template
   lexicographic descent or becomes an alternating rectangle bank.  At the adaptive
   secondary scale, the rectangle source-pair and inserted-triple terms vanish after
   chromatic amplification; its remaining failures are current unary/binary potential
   support.

Thus source-host preparation feeds into current payment or source-valid completion.  It
does not regenerate an independent infinite family of host-only lines.

## 1. Hard unary rectangle support is current potential support

A hard-unary endpoint cell in a rectangle or cross-block host is an old-grid cell
`z` for which two retained source points form a nonaxis blocker pair through `z`.

### Proposition PP3aze -- PROVED

Every hard-unary source-invalid endpoint cell counted in the rectangle interaction
graph contributes at least one unit to `Xi_old`.  A positive-density hard-unary
rectangle core therefore localizes to current `Theta_E^+` credit.

#### Proof

The selected endpoint cell lies in the old grid.  Its blocker pair is retained after
the marked source deletions.  The two unavoidable axis pairs contain the deleted
column or row controllers and therefore are not retained-pair certificates.  Hence the
witness is nonaxis and is counted by PP3ayb.  Positive-density support enters the
star/matching and permanent-block payment theorem PP3aye. ∎

Consequently the positive-density alternative PP3st is no longer host-only after
adjoining `Xi_old`.

## 2. Two-inserted-cell source mass vanishes in the cross-block state space

Let a complete secant-shadow cover be converted by PP3ayp--PP3azc into a
resource-disjoint alternating rectangle bank.  Use the adaptive secondary scale

```text
q<=m^kappa,
0<kappa<1/60,
```

and the cross-block supervariable construction.

### Proposition PP3azf -- PROVED

After unary regularization and equitable colour selection, the complete normalized
source-validity contribution of anchored-pair and inserted-triple events satisfies

```text
K^2 kP/H^2 + K^3 k^2Q/H^3=o(1).
```

This remains true for the secant-cover rectangle bank.

#### Proof

The secant two-switch rectangles are resource-disjoint endpoint rectangles with the
same fixed-rank source-event normal form as the banks in PP3tb--PP3td.  The source
preparation and random secondary thinning depend only on endpoint-resource
disjointness and the global bank scale.  Proposition PP3tb gives `kP/H^2=o(1)` and
PP3tc gives `k^2Q/H^3=o(1)`.  The spread constant is fixed. ∎

Thus anchored-pair, transition, and inserted-triple source mass cannot consume a
positive fraction of the rectangle credit at the selected scale.

## 3. Complete rectangle endpoint after the source correction

### Theorem PP3azg -- PROVED / CONDITIONAL EXISTING CURRENT-POTENTIAL CONVERSION INTERFACES

A target-order source-certificate branch generated while realizing one currently
credited repair has one of the following outcomes.

1. A fixed tentative state has a secant-clean conditioned completion, and the
   lexicographic pair

   ```text
   (Theta_E^+,sigma_(A,R))
   ```

   decreases.
2. Unary retained-pair or hard-unary endpoint support produces current `Xi_old`
   credit and strict `Theta_E^+` payment.
3. A complete secant-shadow cover produces a fixed-centre rectangle star or a
   resource-disjoint rectangle bank.
4. The rectangle bank has a source-valid joint state whose insertion cost is below the
   original current credit.
5. Rectangle conversion produces concentrated unary or binary current-potential
   weight, which enters the existing `A_2/B_3/B_4`, star, fan, path, partner, or
   endpoint-bank payment chain.
6. An explicit conditional Hall, alternating, non-superregular, or current-conversion
   interface fails.

No outcome is another untyped source-host line family.

#### Proof

Use PP3ayj--PP3ayl until a clean conditioned completion succeeds or PP3aym gives a
covered host.  Extract and switch rectangles by PP3ayp--PP3azc.  Vanishing-density hard
unary support is regularized by PP3sr--PP3ss; positive-density hard unary support is
current payment by PP3aze.  The anchored-pair and triple terms vanish by PP3azf.
Therefore the paid cross-block criterion PP3te fails only through unary or binary
current shadow weight, or through one of its explicit host hypotheses.  Current
weights are localized and paid by the existing conversion interfaces through
PP3aui. ∎

The original allocation-failure credit remains designated throughout items 1 and 3;
it is spent only when a source-valid joint state is installed or an earlier favourable
entry deletion removes it.

## 4. Source-host closure is acyclic

### Theorem PP3azh -- PROVED / CONDITIONAL EXISTING CURRENT-POTENTIAL CONVERSION INTERFACES

The focused fixed-infrastructure chain now has the acyclic order

```text
fixed allocation failure
 -> current potential structure or source-host structure
 -> old-grid unary payment / fixed-template secant descent
 -> secant rectangle reduction
 -> chromatic source-mass closure
 -> current potential payment or source-valid joint completion
 -> strict Theta_E^+ decrease.
```

No arrow returns to an unprocessed source-host certificate class.

#### Proof

Unary source support leaves the host layer at PP3aze.  Every remaining source event has
at least two inserted cells and enters the fixed-template/rectangle route.  Proposition
PP3azf removes those source-event terms from the terminal cross-block objective.
The residual terms are current potential weights or explicit current host failures.
Thus the rectangle endpoint cannot return a new anchored-pair or inserted-triple mass
at the same scale. ∎

This replaces the additional source-certificate host-closure hypothesis introduced in
PP3axx.

## 5. Corrected fixed-attempt termination

### Corollary PP3azi -- PROVED / CONDITIONAL NAMED CURRENT CONVERSION INTERFACES

Assume the named current candidate-shadow, active-anchor, old-grid endpoint-shadow,
conditional-Hall, alternating, and endpoint/rectangle conversion interfaces all return
one of:

```text
strict Theta_E^+ payment,
source-valid joint completion spending the original credit,
or robust final-state completion.
```

Then every failed fixed-infrastructure allocation attempt produces a strict decrease
of the nonnegative integer `Theta_E^+`, unless the patch is installed directly.
Repeated attempts therefore terminate.

#### Proof

Current failure extraction and designated-credit integrity are PP3alc--PP3aut and
PP3axa--PP3axh.  Activation clearing is PP3avn--PP3awb.  Source-host realization is
PP3azg--PP3azh.  Every noncompletion branch is now current payment.  Add the exact
identities defining `Theta_E^+` and apply integer descent. ∎

## 6. Revised assembly frontier

### Corollary PP3azj -- PROVED

There is no remaining independent cumulative source-certificate rich-line hypothesis
inside the master prime-patching chain.

The local assembly frontier is now the narrower current call matrix:

1. verify that every unary/binary current-weight localization invoked after PP3azg has
   its required pool size, marked size, layer, puncture, and role-domain hypotheses;
2. verify the conditional Hall and non-superregular endpoint calls used by the secant
   rectangle bank;
3. consolidate those verified calls into the corrected master exact-width lemma.

The separate global frontier remains the prime-minus-one seed theorem.

The no-three-in-line conjecture remains unproved.
