# Target reserves, anchor normalization and physical edge lineage have literal ancestry

This chapter records **CMR3610--CMR3633** and installs source theorems CMR748--CMR829.

Canonical checker:

```text
scripts/check_prime_power_target_anchor_lineage_ancestry.py
```

Contract:

```text
2f846aa50012d196dffc90511de0c254782b1a5696b1cb25ee1cf1b73a990359
```

## CMR3610--CMR3614 — protected target-line reserve

Complete envelope boards of sides six through ten are generated. Old and opposite-layer matchings plus every tested reserve of nonaxis matching-lines leave minimum degree at least half and an exact perfect matching. Reserve capacity and dyadic localization are checked.

## CMR3615--CMR3618 — pair extraction and global neutralization

Historical target pairs are split by matching-vertex incidence. The finite fixtures realize both fan and globally compatible-bank branches. Ordered two-layer rematching avoids the live target and every compatible-bank witness.

## CMR3619--CMR3620 — absence-run temporal ledger

All `2^18=262,144` three-cell availability histories of length six are checked. Every cell satisfies `rho<=1+I`, the summed run bound holds, and the exact token multiplier is revalidated.

## CMR3621--CMR3622 — recurrent labelled target pairs

All 247 matchable side-three hosts and 1,152 active compatible pair prescriptions are enumerated. Exactly 702 expose a matching-preserving edge deletion and 450 contract as double-essential pairs. The six-type recurrence bound is checked.

## CMR3623--CMR3625 — global return ancestry forest

All 720 forward out-degree-one forests on six ordered deletion generations are generated. Every link strictly increases loss time, every path is acyclic, and path-label recurrence or finite physical-edge stock is checked.

## CMR3626--CMR3629 — anchor batches and restoration

All twelve side-three ordered saturated anchors are processed. Twenty-four target-destroying candidates receive pairwise disjoint entering batches. The pass forces the target and a labelled pair branch-locally, and 4,608 restoration subsets are bulk-redeleted while preserving the stored anchor. Private normalization is idempotent and erases all 4,096 pure reopening profiles in the fixed fixture.

## CMR3630--CMR3631 — active normalized context

Newly enabled states use a newly added edge, and every nonimproving enabling edge is absorbed into the private union. All 72 single-edge state losses expose an exact first-loss witness; the active-transition recurrence bound is checked.

## CMR3632 — physical edge lineage

Exact disjoint product fixtures assign every selected edge uniquely to fixed interface data or one child factor. Side-descending owner-stage sums, envelope owner slots and restoration concentration are checked through ambient side twelve.

## CMR3633 — finite consequence and honesty boundary

Observed census includes:

```text
155 protected-line Hall checks
262,144 neutralization histories
786,432 cell absence-run checks
247 labelled-pair hosts
1,152 active labelled pairs
702 pair-edge deletions
450 double contractions
720 forward ancestry forests
2,520 ancestry paths
24 private entering batches
4,608 bulk-redeletion subsets
4,096 normalization profiles
10 rejected contract corruptions
```

Exact flags:

```text
recurrent_target_line_reserve_exact = 1
protected_line_pair_neutralization_exact = 1
neutralized_pair_temporal_ledger_exact = 1
recurrent_labelled_target_pair_ancestry_proved = 1
global_return_ancestry_forest_exact = 1
anchor_state_batch_rejection_exact = 1
anchor_batch_restoration_ledger_exact = 1
stored_anchor_bulk_redeletion_exact = 1
private_batch_normalization_exact = 1
active_context_absorption_exact = 1
physical_edge_lineage_exact = 1
target_anchor_lineage_ancestry_proved = 1

global_transition_kind_bank_exhaustive = 0
global_termination_proved = 0
actual_global_parent_rule_complete = 0
all_n_proved_by_checker = 0
```

This checker installs CMR748--CMR829 only. The completeness-preserving single-edge child union beginning at CMR830 remains the next literal frontier.
