name: Product side-seven multiplicity-two semantic cover core top zero

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/product-side-seven-m2-semantic-cover-core-top0.yml

permissions:
  contents: read

jobs:
  measure:
    name: semantic-cover-core-top0
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - name: Check durable trigger
        id: gate
        shell: bash
        run: |
          if grep -qx 'run-semantic-cover-core-top0' .github/product-side-seven-m2-semantic-cover-core-top0-trigger.txt; then
            echo 'run=true' >> "$GITHUB_OUTPUT"
          else
            echo 'run=false' >> "$GITHUB_OUTPUT"
          fi
      - name: Compile semantic minimizer
        if: steps.gate.outputs.run == 'true'
        run: >-
          g++ -O3 -std=c++17
          scripts/measure_product_side_seven_multiplicity2_case0_orientation3_semantic_cover_core_top0.cpp
          -o /tmp/measure-semantic-cover-core-top0
      - name: Measure semantic cover core
        if: steps.gate.outputs.run == 'true'
        run: >-
          /tmp/measure-semantic-cover-core-top0
          | tee product-side-seven-m2-semantic-cover-core-top0.txt
      - name: Upload exact transcript
        if: always() && steps.gate.outputs.run == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: product-side-seven-m2-semantic-cover-core-top0
          path: product-side-seven-m2-semantic-cover-core-top0.txt
          if-no-files-found: warn
