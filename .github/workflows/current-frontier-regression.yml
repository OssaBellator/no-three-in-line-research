name: Current frontier regression

on:
  push:
    branches:
      - research/all-n-composite-modulus
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: current-frontier-regression-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  regression:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.10"
          - "3.12"
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run validator mutation tests
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/test_prime_power_current_frontier_regression.py

      - name: Verify canonical prescription partition
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_canonical_prescription_partition.py

      - name: Verify complete local target response
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_target_trigger_response_partition.py

      - name: Verify hard-core exchange normal form
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_exchange_normal_form.py

      - name: Verify hard-core exchange realisability
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_exchange_realisability.py

      - name: Verify hard-core two-point classification
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_two_point_classification.py

      - name: Verify hard-core collinear backgrounds
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_collinear_backgrounds.py

      - name: Verify hard-core pivot-line energy
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_pivot_line_energy.py

      - name: Verify hard-core extremal stability
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_extremal_stability.py

      - name: Validate T03 hard-core population bridge
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/check_prime_power_hard_core_population_bridge.py --self-test

      - name: Prepare runtime artifact directory
        run: mkdir -p artifacts

      - name: Run strict runtime manifest audit
        run: >-
          python -B -S -s scripts/check_prime_power_reproducible_runtime_manifest.py
          --self-test
          --manifest artifacts/current-frontier-runtime-python-${{ matrix.python-version }}.json

      - name: Run current-frontier regression
        env:
          PYTHONDONTWRITEBYTECODE: "1"
          PYTHONHASHSEED: "0"
        run: python scripts/run_prime_power_current_frontier_regression.py

      - name: Upload runtime manifest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: current-frontier-runtime-python-${{ matrix.python-version }}
          path: artifacts/current-frontier-runtime-python-${{ matrix.python-version }}.json
          if-no-files-found: warn
          retention-days: 30
