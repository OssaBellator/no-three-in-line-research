name: Installed construction regression 656

on:
  push:
    paths:
      - 'scripts/run_prime_power_installed_construction_regression_656.py'
      - 'scripts/check_prime_power_candidate_transversal_rook_capacity_ancestry.py'
      - 'scripts/check_prime_power_installed_operation_registry_656.py'
      - 'docs/532-prime-power-installed-construction-regression-656.md'
      - '.github/workflows/installed-construction-regression-656.yml'
  pull_request:
  workflow_dispatch:

jobs:
  regression:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Static manifest audit
        run: python scripts/run_prime_power_installed_construction_regression_656.py --static-only
      - name: Run installed construction regression
        run: python scripts/run_prime_power_installed_construction_regression_656.py
