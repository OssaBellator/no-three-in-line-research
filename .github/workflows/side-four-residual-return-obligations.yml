name: Side-four residual return obligations

on:
  push:
    paths:
      - 'data/prime_power_side_four_selected_response_provenance_manifest.json'
      - 'data/prime_power_side_four_return_exchange_context_contract.json'
      - 'data/prime_power_side_four_residual_return_obligation_contract.json'
      - 'scripts/check_prime_power_side_four_return_exchange_context.py'
      - 'scripts/check_prime_power_side_four_residual_return_obligations.py'
      - 'docs/559-prime-power-side-four-residual-return-obligations.md'
      - '.github/workflows/side-four-residual-return-obligations.yml'
  pull_request:
  workflow_dispatch:

jobs:
  residual-return:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python scripts/check_prime_power_side_four_selected_response_provenance_manifest.py
      - run: python scripts/check_prime_power_side_four_return_exchange_context.py
      - run: python scripts/check_prime_power_side_four_residual_return_obligations.py
