name: Product side-seven next equal-class semantic vocabulary

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/product-side-seven-m2-semantic-next-equal-vocabulary.yml

permissions:
  contents: read

jobs:
  measure:
    name: semantic-next-equal-vocabulary
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - name: Check durable trigger
        id: gate
        shell: bash
        run: |
          if grep -qx 'run-semantic-next-equal-vocabulary' .github/product-side-seven-m2-semantic-next-equal-vocabulary-trigger.txt; then
            echo 'run=true' >> "$GITHUB_OUTPUT"
          else
            echo 'run=false' >> "$GITHUB_OUTPUT"
          fi
      - name: Compile vocabulary measurement
        if: steps.gate.outputs.run == 'true'
        run: >-
          g++ -O3 -std=c++17
          scripts/measure_product_side_seven_multiplicity2_case0_orientation3_semantic_vocabulary_next_equal.cpp
          -o /tmp/measure-semantic-next-equal-vocabulary
      - name: Measure vocabulary coverage
        if: steps.gate.outputs.run == 'true'
        run: >-
          /tmp/measure-semantic-next-equal-vocabulary
          | tee product-side-seven-m2-semantic-next-equal-vocabulary.txt
      - name: Upload exact transcript
        if: always() && steps.gate.outputs.run == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: product-side-seven-m2-semantic-next-equal-vocabulary
          path: product-side-seven-m2-semantic-next-equal-vocabulary.txt
          if-no-files-found: warn
