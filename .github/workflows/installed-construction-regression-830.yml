name: Installed construction regression 830

on:
  push:
    paths:
      - 'scripts/run_prime_power_installed_construction_regression_830.py'
      - 'scripts/check_prime_power_recurrent_certificate_assembly_ancestry.py'
      - 'scripts/check_prime_power_installed_operation_registry_830.py'
      - 'docs/541-prime-power-installed-construction-regression-830.md'
      - '.github/workflows/installed-construction-regression-830.yml'
  pull_request:
  workflow_dispatch:

jobs:
  regression:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Static manifest audit
        run: python scripts/run_prime_power_installed_construction_regression_830.py --static-only
      - name: Run installed construction regression
        run: python scripts/run_prime_power_installed_construction_regression_830.py
