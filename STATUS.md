# Status and honesty ledger

**Last updated:** 27 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open.  This repository does **not** contain a complete proof.

The authoritative collision-free theorem ledger is split across:

- `proofs/composite-modulus-theorem-index-live.md` through CMR747;
- `proofs/composite-modulus-theorem-index-live-continuation.md` through CMR869;
- `proofs/composite-modulus-theorem-index-live-continuation-2.md` through CMR1197;
- `proofs/composite-modulus-theorem-index-live-continuation-3.md` through CMR1629;
- `proofs/composite-modulus-theorem-index-live-continuation-4.md` from CMR1630 through CMR1709.

Matching side, prime-power envelope side and inherited coordinate span remain
separate parameters throughout the current endpoint.

## Structural and spectral endpoint

Every genuinely new physical triple has one absolute last-entering edge owner.
Owner, product, wall, routing, host, depth and monotone-resource exits form
finite transfer graphs.  After retaining every label needed to determine future
rows and contracting exact strongly connected components,

\[
\boxed{\rho(A)=\max_i\rho(A_{ii}).}
\]

Strict child descent, earlier-depth handoff, first token/private-edge/restoration/
support/signature use and ordinary later-owner exits are off-diagonal only when
the chosen policy actually executes the corresponding transition.

Finite response, finite resource use and structural descent are not by
themselves potential improvement.  Every recurrent labelled block still needs a
strict numerical certificate.

## Exact response and line-clean laws

For response side `d`, opposite matching `O` and target edge `e`, use

\[
H_e=K_{d,d}\setminus(O\cup\{e\}).
\]

Every compatible prescription has an exact rook class and every candidate has
one canonical owner before sampling.  Every allowed partial matching, including
the complete trace of a nonaxis line, may be deleted from `H_e` while preserving
at least one target-avoiding response when `d>=4`.

For a deleted trace `X`, put

\[
F=O\cup X\cup\{e\},
\qquad
D_0=O\cup X.
\]

The graph `D_0` decomposes into alternating paths and even cycles, and

\[
\mathcal R_F(z)
=
\mathcal R_{D_0}(z)+z\mathcal R_{D_0-u-v}(z).
\]

Hence

\[
N_d(F)=\sum_j(-1)^jr_j(F)(d-j)!,
\qquad
\Pr(P\subseteq R)=\frac{N_{d-r}(F/P)}{N_d(F)}.
\]

The complete off-line response row is an exact rational component-rook dot
product.

## Exact, universal and classwise line-clean budgets

For corrected off-line rank counts `V_1,V_2,V_3`, unavailable-edge count `b`
and current potential `m`, define

\[
\boxed{
W_d
=(d-1)(d-2)V_1
+(d-2)V_2
+V_3
+(m+1)b(d-1)(d-2).
}
\]

For fractional factor `q=u/v` and destroyed target load `D_S(e)=D`, strict
improvement is exactly

\[
\boxed{
(dv)^dW_d
<
D u^d d(d-1)(d-2).
}
\]

The strong, singleton and endpoint-overlap permanent ratios are strictly
increasing for `d>=4`, with universal floors

\[
\boxed{
\frac1{16},
\qquad
\frac{81}{4096},
\qquad
\frac1{256}.
}
\]

Thus the automatic universal budgets are

\[
W_d\le\left\lceil\frac{D(d)_3}{16}\right\rceil-1,
\]

\[
W_d\le\left\lceil\frac{81D(d)_3}{4096}\right\rceil-1,
\]

and

\[
W_d\le\left\lceil\frac{D(d)_3}{256}\right\rceil-1.
\]

Rooted-target trace recurrence uses only the strong and singleton classes.

CMR1678--CMR1685 compile inherited class capacities into

\[
\widehat W
=
\sum_\chi c_{r(\chi)}C_\chi
+(m+1)B(d-1)(d-2),
\]

where

\[
c_1=(d-1)(d-2),
\qquad c_2=d-2,
\qquad c_3=1.
\]

If `widehat W` fits the exact or universal budget, one positive integer slack
certifies every realization in the class family.  Failure forces one explicit
candidate class or the unavailable-edge coordinate above a quantitative integer
threshold.

## Combined return-selector assignment

The exact returned-edge exchange kernel and every subunit paid-pair selector use
one combined edge score

\[
\boxed{h_T(a)=g_{\rm ret}(a)+Tg_{\rm sel}(a).}
\]

The coupled scalar is

\[
\alpha+T\beta
=
\sum_a p(a)h_T(a),
\]

where `p` is a doubly stochastic response-edge marginal.  One rational assignment
dual below one certifies the complete block.

For positive score levels `tau_j`, let

\[
E_j=\{a:h_T(a)\ge\tau_j\}.
\]

Then

\[
\max_Q\sum_{a\in Q}h_T(a)
\le
\sum_j(\tau_j-\tau_{j-1})\nu(E_j).
\]

By Konig's theorem, every matching number may be replaced by an explicit
source/target cover.

CMR1670--CMR1677 give the geometric cover compiler.  If class `s` has score cap
`H_s` and a source/target cover `C_s`, the nested cover objective is

\[
\boxed{
\sum_{z\in L\cup R}\max\{H_s:z\in C_s\}.
}
\]

This is a feasible assignment dual.  An objective below one, or its strict
integer denominator-cleared form, proves

\[
\boxed{\alpha+T\beta<1.}
\]

Alternative source and target covers for each class may be optimized jointly as
a finite set-cover problem.  The shared response matching is retained throughout;
incompatible return and selector maxima are never added.

## Critical-selector capacity compiler

For one exact selector host, write

\[
A_L=\sum_\chi\frac{a_\chi}{D}
\]

and prove integer capacities

\[
0\le a_\chi\le C_\chi.
\]

Put `C=sum C_chi`.  If

\[
\boxed{C<D,}
\]

then criticality is impossible and the selector has exact gap

\[
\boxed{\eta=\frac{D-C}{D}.}
\]

With response side `n` and unavailable-edge count `B`, its restoration cap is

\[
\boxed{
T_C
=
\left\lfloor
\frac{D[2(n-1)+B]}{(n-1)(D-C)}
\right\rfloor.
}
\]

This cap feeds directly into `h_{T_C}` and the shared assignment/cover
certificate.  If `C>=D`, only classes meeting the necessary capacity threshold
need sharper enumeration.  There is no remaining diffuse critical-selector row.

## Prime-field root, fixed-interface and thin bases

For envelope exponent `k>=2`, root-channel normalization enters a strict child
factor.  At `k=1`, every root channel contains at most one ordered pair and a
support of size one or two.  First support use is finite resource.  Recurrence is
reused support, returned-edge currency or one exact fixed-interface atom; the
broad prime-field root-translation row is removed.

Every opposite matching and forbidden target may be relabelled to

\[
\boxed{(O,e)=(I_d,(0,1)).}
\]

The residual stabilizer is `S_{d-2}`.  Deleted traces, interface prescriptions and
all owner/collision/local-line/CRT labels are canonicalized equivariantly.  One
exact row is computed per orbit and every orbit certificate lifts to the fully
labelled table.

The exhaustive normalized census through side five is:

| side | canonical hosts | matching-level endpoint |
|---:|---:|---|
| 2 | 0 | no extension-free response |
| 3 | 4 | every positive rank-at-most-three prescription is forced |
| 4 | 45 | nonforced caps `3/4`, `2/3`, `1/2` in ranks one, two, three |
| 5 | 124 | caps `2/3`, `2/5`, `1/4`; no forced positive rank-at-most-three prescriptions |

The rank-three census contains 448 extendable side-four instances, 28 forced,
and 15,017 side-five instances, none forced.

For every response law and residual rank `r`, prescription mass is exactly

\[
\boxed{
\sum_{P:\operatorname{rank}P=r}\Pr(P\subseteq Q)=\binom dr.
}
\]

Forced contractions consume one full unit each.  A corrected class therefore
uses the minimum of its pointwise probability capacity and its conserved
rank-mass capacity.  After denominator clearing this is an exact integer class
capacity for selector, line-clean and thin-table compilation.

## Certified auxiliary-block elimination

Let a labelled recurrent block be partitioned as

\[
M=
\begin{pmatrix}A&B\\C&D\end{pmatrix},
\]

where the auxiliary module `D` already has `rho(D)<1`.  Its exact nonnegative
resolvent is

\[
R_D=(I-D)^{-1}\ge0.
\]

CMR1694--CMR1701 prove

\[
\boxed{
\rho(M)<1
\quad\Longleftrightarrow\quad
\rho\bigl(A+B(I-D)^{-1}C\bigr)<1.
}
\]

The certificate lift is constructive and rational.  Clearing denominators gives
a finite strict integer certificate.  For zero-self auxiliary rows this becomes
`A+BC`; the selector formula `alpha+beta T<1` is its one-dimensional case.

Certified thin, fixed-interface or support modules may therefore be eliminated
before the final difficult core is searched.  Uncertified reused-support modules
remain explicit and are not silently removed.

## Label-preserving balanced/CRT assembly

Collision, local-line, factor and fixed-interface provenance must remain in the
exact state.  Premature label merging can turn an acyclic transfer into an
artificial self-loop or cycle.

After retaining those labels, contract exact strongly connected components.  If
every recurrent block `D_i` has

\[
D_iv_i\le v_i-s_i,
\qquad s_i>0,
\]

reverse-topological rational scaling glues all finite transfer collateral into
one global vector `Av<v`.  Clearing denominators gives an independently
checkable integer certificate.

The current recurrent schema contains only:

1. combined return-selector assignment blocks;
2. strong, singleton and overlap line-clean overflow classes;
3. critical-selector classes not eliminated by capacity slack;
4. exact reused-support blocks;
5. repeated fixed-interface/thin orbit rows not yet certified and eliminated;
6. genuinely recurrent collision/local-line states preserving every label.

## Genuine current frontier

1. **Return class geometry.**  Prove host-uniform score caps and small source/
   target covers for the exact classes of `h_T`, giving one shared dual below one.
2. **Line-clean overflow classes.**  Put inherited capacities inside the exact or
   universal budgets and certify the finitely many classes surviving the overflow
   threshold.
3. **Selector capacities.**  Combine exact rook numerators, thin census caps and
   conserved rank mass; eliminate every class with positive denominator slack and
   certify the survivors.
4. **Canonical geometric offspring.**  Enumerate genuinely new offspring on the
   45 side-four and 124 side-five hosts, certify their orbit matrices and eliminate
   certified auxiliary tables by the exact resolvent.
5. **Final labelled quotient.**  Certify reused-support and remaining collision/
   local-line SCCs, publish one strict integer quotient certificate and apply the
   proved CRT gluing protocol.

## Corrections retained

- Sequential two-layer rematching may reoccupy an old first-layer cell.
- Historical target lines, stars, selector episodes and trace loads are not
  simultaneous families.
- One edge return may serve several neutralisations in one absence run.
- Removing one essential edge gives Hall deficiency exactly one.
- Aggressive batch deletion is branch-local.
- Differently masked leaf unions are not automatically one matching host.
- Conditioning on one support edge does not fix the other layer labels.
- Finite scheduler termination is not potential improvement.
- Old response edges and old subsets create no new collateral.
- Standard-grid classifications do not transfer to scattered residual
  coordinates.
- Geometric capacities and displacement stocks use `W_omega`, not `d`.
- Strict transfers and fresh resources are off-diagonal only when the chosen
  policy executes the corresponding handoff or monotone payment.
- A loaded response witnessing a large expectation need not itself be the
  minimum response.
- Uniform line-clean floors may be too weak for individual hosts; exact component
  ratios remain available.
- First restoration/support/signature labels are finite, but repeated labels are
  genuine recurrent currency.
- Rooted trace and prime-field batching do not add historical destroyed loads.
- Superlevel matching numbers at different thresholds need not be attained by
  one response; they form an upper certificate, not an exact assignment identity.
- Symmetry normalization may rename labels but may not discard collision,
  local-line, owner or CRT provenance.
- Rank-mass conservation is within one current response law; it does not add
  historical prescription families.
- Auxiliary resolvent elimination applies only after the auxiliary block has a
  genuine strict certificate.

## Bottom line

There is no complete proof.  Through **CMR1709**, the broad recurrent fronts are
reduced to class-supported return assignment covers, exact and universal
line-clean capacity slacks with overflow localization, selector denominator
slacks, a complete matching-level rank-one/two/three census through side five,
exact prescription rank-mass capacities, certified auxiliary resolvents and a
label-preserving CRT certificate protocol.

The remaining obstruction is numerical and geometric: fill these certificate
surfaces with host-uniform class bounds, certify every surviving labelled block,
and publish the final strict integer quotient.
