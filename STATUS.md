# Status and honesty ledger

**Last updated:** 28 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open. This repository does **not** contain a complete proof.

The authoritative theorem ledger reaches **CMR2573**. CMR2390 onward is recorded in
`proofs/composite-modulus-theorem-index-live-continuation-8.md`.

## Current exact finite endpoint

The branch now contains synchronized executable work banks through T15:

| Target | Exact finite surface | Mathematical status |
|---|---|---|
| T01 | literal source statements, UTF-8 hashes and sealed source artifacts | genuine source statements remain unproved/unpopulated |
| T02 | cases, clauses, axes, exclusions and global-parent applications | genuine recurrence exhaustiveness remains open |
| T03 | open/populated/proved slot and candidate payloads | actual complete population remains absent |
| T04 | skeleton-derived recurrent-block and interface assembly | actual complete assembly remains absent |
| T05 | exact finite geometry and selector arithmetic | arbitrary-`n` coverage remains open |
| T06 | exact slot scores, candidate sets and deterministic winners | score theorems and intended policy remain open |
| T07 | exact fate, state and transition subjects, claims and support DAGs | semantic truth remains open |
| T08 | one active row per exact T06 global-parent application | genuine simultaneous-row exhaustiveness remains open |
| T09 | literal destroyed-resource universes, overlap graph and scopes | physical resource exhaustiveness remains open |
| T10 | exact routed-credit subjects, assignments and injectivity | route and child-state semantics remain open |
| T11 | exact common-row bridges, primitive weights, recurrent closure and strict margins | genuine recurrent-block semantics remain open |
| T12 | exact acyclic auxiliary substitution and selected-response stability | expansion truth and target-state meaning remain open |
| T13 | T04-derived local states, T07-supported global classes and spanning proof trees | genuine cross-block state identity remains open |
| T14 | T11/T13-rooted scale equations, primitive multipliers and component weights | external scale semantics and intercomponent scaling remain open |
| T15 | T04-derived interface rows, primitive intercomponent multipliers and exact final-exit dispositions | genuine interface exhaustiveness and rank descent remain open |

Every final checker permanently reports:

```text
all_n_proved_by_checker = 0
```

## Canonical endpoints

```text
scripts/check_prime_power_geometry_selector_frontier_v2.py
scripts/check_prime_power_auxiliary_semantics_frontier_v2.py
scripts/check_prime_power_state_equivalence_frontier.py
scripts/check_prime_power_component_scale_frontier.py
scripts/check_prime_power_interface_exhaustiveness_frontier.py
```

## Exact T15 interface-row frontier

CMR2566--CMR2573 remove the independently supplied row, component-multiplier and rank tables accepted by
the older interface checker.

The exact proof root is now:

```text
T04_BLOCK_INTERFACE_POPULATION
T07_FATE_TRANSITION_STATE
T12_AUXILIARY_SEMANTICS
T14_COMPONENT_SCALES
    -> T15_INTERFACE_EXHAUSTIVENESS
```

The semantic obligation has the matching immediate support:

```text
FATE_TRANSITION_STATE_SEMANTICS
AUXILIARY_EXPANSIONS_SEMANTIC
COMPONENT_SCALE_SEMANTIC
    -> INTERFACE_RETURN_ROWS_EXHAUSTIVE
```

The corrected root changes the shared obligation and target-definition digests. Concrete downstream
certificates generated against the previous definitions must be regenerated.

### Exact row census

Every T04 `interface-row` unit contributes exactly one open/proved T15 record. Its identity binds the
T04 unit, row kind, parent, operation slot and population payload. Missing, duplicate, reordered or
independently supplied rows are rejected.

### Intercomponent weights

T15 gives every T14 component one positive integer multiplier. The complete multiplier vector has gcd
one. For every global state it reconstructs the final weight

\[
\widehat W_g=m_{C(g)}W_g.
\]

The multiplier bank is sealed by one `global-interface-component-scale-proof` with exact T14 support.
This does not prove the relative-scale statements mathematically true.

### Row semantics and arithmetic

Every proved row binds:

- its exact T04 population payload and field digests;
- the exact T07 state and transition claim bank of its slot;
- exact T13 parent and target classes;
- exact T14 component and final weights;
- an integer fixed offset and positive target multiplicities; and
- row statements and evidence.

The checker reconstructs every weighted load and margin. Negative margins are rejected. Positive rows
are `strict`. Zero-margin rows remain `critical-unranked` and are passed to T16; T15 does not accept a
free rank witness.

### Final-exit coverage

The final nonrecurrent exit census is reconstructed from T11 and the selected rows after complete T12
auxiliary elimination. Every T11 auxiliary exit must occur in the T12 closure, and every T11
nonauxiliary exit must remain visible after elimination.

Each final exit receives exactly one disposition:

```text
interface-row
terminal-sink
```

A row disposition must name a proved T15 row with the exact global source and sufficient target
multiplicity. A terminal disposition is permitted only for an exact T13 sink class.

### Typed sealing

Every proved row requires one `interface-row-semantic-proof` supported by the exact T04, T07, T12, T13,
T14 and interface-scale artifacts. The aggregate `interface-exhaustiveness-proof` synchronizes both
`INTERFACE_RETURN_ROWS_EXHAUSTIVE` and `T15_INTERFACE_EXHAUSTIVENESS`.

Exact ancestry, arithmetic and coverage do not prove the row statements or interface-exhaustiveness
theorem true.

## Genuine current frontier

The next executable semantic layer is **T16 global-rank well-foundedness**.

T16 must:

1. derive its complete critical-edge census from T15 `critical-unranked` rows;
2. reject an independently supplied support graph or rank table;
3. bind every critical edge to exact T15 row and target records;
4. prove every critical edge strictly decreases one exact rank;
5. prove the rank domain genuinely well founded under the external recurrence semantics;
6. retain all strict T15 rows as already decreasing by weight; and
7. synchronize `GLOBAL_RANK_WELL_FOUNDED` and `T16_GLOBAL_RANK` through typed noncircular artifacts.

After T16, the remaining exact fronts are:

- T17 global-state predicates;
- T18 final row theorems and fixed-offset interpretation;
- T19 global-family exhaustiveness;
- T20/T21 closure of all `232 + 20 = 252` exceptional chambers;
- T22--T43 final premises, typed support, induction handoff, dossier audit and reviewed root implication.

## Concrete mathematical work still required

1. Transcribe and prove every genuine source statement.
2. Prove the supplied recurrence genuinely exhaustive.
3. Populate all actual T03/T04 objects.
4. Prove T05 covers every relevant geometry for arbitrary `n`.
5. Prove every T07 statement and T06 score theorem.
6. Prove genuine T08 simultaneous-row completeness, T09 physical resource exhaustiveness and T10
   routed-credit semantics.
7. Prove every T11 block and T12 expansion has its intended external meaning.
8. Prove every T13 identity, T14 scale equation and T15 interface theorem semantically correct.
9. Prove T16 rank, T17 predicates and T18 final rows.
10. Prove global-family exhaustiveness and all 252 exceptional chambers.
11. Complete and review every final premise and handoff assertion.
12. Prove the ordinary implication from the reviewed quotient and handoff to `D(n)=2n`.

## Corrections retained

- A source locator, hash or sealed evidence record does not prove a theorem true.
- Exact recurrence identity does not prove recurrence exhaustiveness.
- Literal T03/T04 data do not prove their intended semantics.
- Finite T05 arithmetic does not prove arbitrary-`n` coverage.
- A T06 integer score remains an external theorem.
- T08/T09/T10 documentary identity does not prove simultaneous, physical or route semantics.
- Exact T11 closure and T12 substitution remain relative to supplied semantic claims.
- A T13 spanning tree does not prove state equivalence.
- T14 rational consistency does not prove external scale semantics.
- T15 exact exit coverage does not prove the interface family mathematically exhaustive.
- A zero-margin T15 row is not terminating until T16 proves genuine rank descent.
- Typed artifacts and readiness flags are documentary metadata.
- Syntax compilation and isolated helper tests are not a dependency-backed certificate suite.

## Bottom line

There is no complete proof. Through **CMR2573**, the repository has an exact finite documentary stack
from source statements through interface-row identity, intercomponent scaling and final-exit coverage.

The unresolved center remains the genuine mathematics: real source and recurrence theorems, actual
population, arbitrary-`n` coverage, truth of every semantic claim, interface exhaustiveness, genuine
rank well-foundedness, all exceptional chambers and the final implication to all `n`.
