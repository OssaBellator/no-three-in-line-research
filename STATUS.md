# Status and honesty ledger

**Last updated:** 27 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open. This repository does **not** contain a complete proof.

The authoritative theorem ledger is split across six files: the first five reach
CMR1997 and `proofs/composite-modulus-theorem-index-live-continuation-6.md` continues
from CMR1998 through CMR2021.

## Structural endpoint

Every genuinely new physical triple has one absolute last-entering response-edge
owner. Owner, provenance, collision, local-line, interface, root, thin and CRT labels
remain until exact equivalence or honest componentwise domination is proved.
After exact SCC contraction,

\[
\rho(A)=\max_i\rho(A_{ii}).
\]

Finite response, finite resource use and structural descent are not potential
improvement. Every recurrent labelled block still needs a strict numerical
certificate. Certified auxiliary modules eliminate through

\[
A+B(I-D)^{-1}C
\]

when `rho(D)<1`; reverse-topological scaling and denominator clearing then produce a
global integer certificate.

## Canonical raw-host layer

The corrected first geometric layer has 86 side-four and 654 side-five raw hosts.
Every host has a stable ID determined by its side and canonical partial-matching
deletion set. The exact catalogue reconstructs every forbidden edge, response,
rank-three response count, denominator `Z`, numerator `A_3`, slack `S_3`, histogram
and record digest. It contains 740 unique IDs and 9,260 responses, with digest

\[
\texttt{f333bc7dda6fc5aa0de25336f641d4d444b601be0ff27ab59c9853361f6d1d92}.
\]

These IDs are primary keys, not geometric or semantic quotients.

## Uniform and deterministic rank-three worklists

The exact uniform split remains

| class | side four | side five | total |
|---|---:|---:|---:|
| `S_3>0` | 53 | 598 | 651 |
| `S_3=0` | 6 | 38 | 44 |
| `S_3<0` | 27 | 18 | 45 |

For a non-strict uniform host the minimum independent numerator correction is

\[
c_{\min}=1-S_3.
\]

The 89-host distribution is `[[1,44],[2,14],[3,18],[4,12],[5,1]]`, total 179.

For deterministic response choice,

\[
m_3(H)=\min_Q\psi_3(Q)
\]

has distribution

\[
729\text{ at }0,\qquad9\text{ at }1,\qquad2\text{ at }4.
\]

Every side-five host, every uniform-strict host, every critical host and 34 of 45
excess hosts has a zero-rank-three response. Thus 78 of 89 uniform-exceptional hosts
are rank-three-free under exact deterministic selection.

## Exact eleven-host blocker classification

The undeleted side-four base host has six responses: four rank-three-free, one with
one triple and one with four. Positive minimum occurs exactly when the deletion set
hits all four zero-response edge sets while leaving a response.

The exact inclusion-minimal blockers are

\[
\{(0,2),(2,0)\},\qquad
\{(0,2),(3,1)\},\qquad
\{(1,3),(3,1)\}.
\]

Their canonical supersets with a surviving response are exactly the eleven hard-core
hosts. Nine select the line `x-y-1=0` and have one rank-three occurrence. Two have one
available response, the four-point line `x+y-3=0`, and have four occurrences.

The selected-response occurrence-deletion burden is

\[
\boxed{17=9\cdot1+2\cdot4.}
\]

Independent uniform correction costs 44 units. The 27-unit difference compares
response policies; it is not a theorem-preserving replacement of uniform response.

## Canonical owner/provenance fibre linkage

A linked fibre must reconstruct its raw host from the source allowed-edge set, cite
the matching host ID and record digest, reproduce every response and the denominator,
declare a canonical-selector or explicit declared-response policy, and carry the
ordered nonempty labels

\[
(\text{provenance},\text{collision},\text{local-line},\text{interface},
\text{root},\text{thin},\text{CRT}).
\]

The checker fingerprints the full source, labels, response list, fibre and linkage.
It proves stable identity and presence only; it does not prove label semantics or that
the parent transition executes.

Its deterministic suite validates 300 linked fibres containing 3,815 response records
and 4,096 primitive witnesses, split evenly between canonical and declared policies,
and rejects twelve corruptions.

## Exact scalar accounting

For true pre-response points `P`, removed set `R`, surviving background `B`, and
response `Q`, the direct checker proves

\[
\Delta\Psi(Q)=\Psi(B\cup Q)-\Psi(P)
=W_1(Q)+W_2(Q)+W_3(Q)-|\mathcal D(P,R)|.
\]

For deleted-load capacity `K`, pool remainder `U_pool`, coefficient score `B(Q)` and
domination surplus `E_+(Q)`, the exact gap is

\[
\boxed{(B(Q)-U_{\rm pool})-\Delta\Psi(Q)=(K-d(Q))+E_+(Q).}
\]

There is no hidden third scalar loss term.

## Genuine current frontier

1. Wrap and validate every actual owner/provenance source with canonical fibre linkage.
2. Populate actual point, removal, background, entry-order and transition data.
3. Run complete background-dependent response censuses where the raw selector is not
   the full minimizer.
4. Prove one correction or sharper routing on each of the nine one-triple blockers.
5. Prove four units or change the operation/state/routing on each of the two
   four-triple one-response blockers.
6. Close the other 78 uniform-exceptional rows using their rank-three-free responses
   plus full background and labelled terms.
7. Complete direct/pool audits, fate semantics, labelled SCC certificates, auxiliary
   elimination and the global integer quotient.

## Corrections retained

- Historical selectors, traces, target lines and destroyed loads are not one current
  simultaneous family.
- Canonical IDs and signatures are not geometric or semantic quotients.
- Fibre linkage proves identity and label presence, not semantic correctness.
- A zero-rank-three response does not prove the complete row strict.
- A raw rank-three minimizer need not minimize the background-dependent delta.
- Deterministic and uniform response are different policies.
- Silent witness deletion is invalid.
- Pool reuse applies only to the unlabelled scalar potential after response selection.
- A literal delta requires the actual point, removal and insertion data.
- A negative scalar delta does not automatically certify a labelled SCC.
- Evidence identifiers do not prove domination, transfer or auxiliary subcriticality.
- Passing source, linkage, blocker, correction, selector, direct, pool and assignment
  checkers proves only their stated finite equalities.

## Bottom line

There is no complete proof. Through **CMR2021**, the finite path includes stable keys
for all 740 raw hosts, exact uniform and deterministic worklists, an exact transversal
classification of the eleven-host core, its 17-unit selected correction burden,
canonical owner/provenance fibre linkage, exact direct/pool accounting and the final
integer assignment machinery.

The unresolved obstruction is to populate and semantically verify the real linked
fibres, execute the required corrections and transitions, certify every recurrent
labelled block and publish one strict denominator-cleared global CRT quotient.
