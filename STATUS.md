# Status and honesty ledger

**Last updated:** 28 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open. This repository does **not** contain a complete proof.

The authoritative theorem ledger reaches **CMR2565**. CMR2390 onward is recorded in
`proofs/composite-modulus-theorem-index-live-continuation-8.md`.

## Current exact finite endpoint

The branch now contains synchronized executable work banks through T14:

| Target | Exact finite surface | Mathematical status |
|---|---|---|
| T01 | literal source statements, UTF-8 hashes and sealed source artifacts | genuine source statements remain unproved/unpopulated |
| T02 | cases, clauses, axes, exclusions and global-parent applications | genuine recurrence exhaustiveness remains open |
| T03 | open/populated/proved slot and candidate payloads | actual complete population remains absent |
| T04 | skeleton-derived recurrent-block and interface assembly | actual complete assembly remains absent |
| T05 | exact finite geometry and selector arithmetic | arbitrary-`n` coverage remains open |
| T06 | exact slot scores, parent candidate sets and deterministic winners | score theorems and intended policy remain open |
| T07 | exact fate, state and transition subjects, claims and support DAGs | semantic truth remains open |
| T08 | one active row per exact T06 global-parent application | genuine simultaneous-row exhaustiveness remains open |
| T09 | literal destroyed-resource universes, overlap graph and scopes | physical resource exhaustiveness remains open |
| T10 | exact routed-credit subjects, assignments and injectivity | route and child-state semantics remain open |
| T11 | exact common-row bridges, primitive weights, recurrent closure and strict margins | genuine recurrent-block semantics remain open |
| T12 | exact acyclic auxiliary substitution and selected-response stability | expansion truth and target-state meaning remain open |
| T13 | T04-derived local states, T07-supported global classes and spanning proof trees | genuine cross-block state identity remains open |
| T14 | T11/T13-rooted scale equations, primitive multipliers and component weights | external scale semantics and intercomponent scaling remain open |

Every final checker permanently reports:

```text
all_n_proved_by_checker = 0
```

## Canonical corrected endpoints

Two earlier publication paths have canonical corrected adapters:

```text
scripts/check_prime_power_geometry_selector_frontier_v2.py
scripts/check_prime_power_auxiliary_semantics_frontier_v2.py
```

The exact T13/T14 endpoints are:

```text
scripts/check_prime_power_state_equivalence_frontier.py
scripts/check_prime_power_component_scale_frontier.py
```

## Exact T13 state-equivalence frontier

CMR2550--CMR2557 remove the parallel integer-block/state population accepted by the older cross-block
identification stack.

T13 derives every recurrent block and local state from T04. Every local state has exactly one
local-to-global link containing:

```text
block_id
unit_id
local_state_id
local_state_subject_sha256
global_state_id
support_state_claim_ids
identity_statement
evidence
```

The claim support must be nonempty and belong to the exact T07 state-claim bank of slots used by the
same block.

The link bank derives every global class. Class members must have identical `(role, stratum, owner)`
cores, and at most one member may come from each block. Every class, including a singleton, receives one
open/proved status record.

A proved class has exactly `member_count - 1` pairwise edges forming a connected tree, one canonical
root-to-member path for every member and one typed artifact:

```text
global-state-equivalence-class-proof
```

The aggregate bank synchronizes `CROSS_BLOCK_STATE_IDENTITY_SEMANTIC` and
`T13_STATE_EQUIVALENCE` through `state-equivalence-proof` artifacts.

This proves exact documentary coverage and support. It does not prove any asserted state equality true.

## Exact T14 component-scale frontier

CMR2558--CMR2565 remove the independently supplied integer-block scale family accepted by the older
weight-synchronization checker.

T14 requires T11 and T13 to share the same T07 root. The T13 block set must equal the exact T11
common-weight block set, and every T13 local state must have its exact T11 primitive weight.

Shared T13 classes derive the block-overlap components and equations

\[
\alpha_b w_b(s)=\alpha_c w_c(t).
\]

The checker:

1. propagates exact rational scales from the lexicographically first block in each component;
2. rejects every inconsistent repeated path or cycle;
3. clears all denominators;
4. divides the componentwise gcd;
5. publishes one primitive positive integer multiplier for every block;
6. publishes one exact component weight for every global state; and
7. preserves a positive scaled minimum margin for every T11 block.

Every proved component has one semantic certificate and one typed artifact:

```text
component-scale-synchronization-proof
```

Its exact support contains every T13 class artifact and every T11 common-weight and closure artifact in
the component. The aggregate bank synchronizes `COMPONENT_SCALE_SEMANTIC` and
`T14_COMPONENT_SCALES` through `component-scale-proof` artifacts.

Disconnected components remain independently normalized. T14 does not invent their relative scale.

## Genuine current frontier

The next executable semantic layer is **T15 interface-row exhaustiveness**.

T15 must:

1. derive the complete return/interface/off-diagonal row census from exact T04 units;
2. bind every row to its exact T07 state and transition claims;
3. bind every endpoint state to the exact T13 class;
4. use exact T14 component weights and expose every cross-component relative-scale requirement;
5. reject missing, duplicated and wrongly typed interface rows;
6. prove that every nonrecurrent exit published by T11/T12 is represented; and
7. synchronize `INTERFACE_RETURN_ROWS_EXHAUSTIVE` with `T15_INTERFACE_EXHAUSTIVENESS` through typed,
   noncircular artifacts.

After T15, the remaining exact fronts are:

- T16 global-rank well-foundedness;
- T17 global-state predicates;
- T18 final row theorems and fixed-offset interpretation;
- T19 global-family exhaustiveness;
- T20/T21 closure of all `232 + 20 = 252` exceptional chambers;
- T22--T43 final premises, typed support, induction handoff, dossier audit and reviewed root implication.

## Concrete mathematical work still required

1. Transcribe and prove every genuine source statement.
2. Prove the supplied recurrence clauses and exclusions genuinely exhaustive.
3. Populate all actual T03/T04 objects.
4. Prove T05 covers every relevant geometry for arbitrary `n`.
5. Prove every T07 statement and T06 score theorem.
6. Prove genuine T08 simultaneous-row completeness, T09 physical resource exhaustiveness and T10
   routed-credit semantics.
7. Prove every T11 block and T12 auxiliary expansion has its intended external meaning.
8. Prove every T13 state identification and every T14 scale equation semantically correct.
9. Prove the complete T15 interface family and all T16--T18 semantic theorems.
10. Prove global-family exhaustiveness and all 252 exceptional chambers.
11. Complete and review every final premise and handoff assertion.
12. Prove the ordinary implication from the reviewed quotient and induction handoff to `D(n)=2n`.

## Corrections retained

- A source locator or hash does not prove a statement true.
- A sealed evidence record does not prove its statement logically sufficient.
- Exact global-parent-to-slot identity does not prove recurrence exhaustiveness.
- Literal T03/T04 data do not prove their intended semantics.
- Finite T05 arithmetic does not prove arbitrary-`n` coverage.
- T05 closure must be read through the corrected v2 endpoint.
- A T06 integer score remains an external theorem requiring proof.
- Application-derived T08 rows remain relative to the supplied recurrence skeleton.
- Coordinate-canonical T09 scopes do not prove physical independence.
- T10 route linkage and injectivity do not prove routed-credit meaning.
- Exact T11 closure remains relative to supplied T07/T10 semantics.
- Exact T12 substitution does not prove expansion truth.
- A T13 spanning evidence tree does not prove state equivalence.
- T14 rational consistency does not prove external scale semantics.
- Componentwise normalization does not choose relative scales between disconnected components.
- Strict-or-ranked arithmetic does not prove interface exhaustiveness or termination semantics.
- A chamber disposition record does not verify its proof.
- Typed artifacts and readiness flags are documentary metadata.
- Syntax compilation and isolated helper tests are not a dependency-backed certificate suite.

## Bottom line

There is no complete proof. Through **CMR2565**, the repository has an exact finite documentary stack
from source statements through cross-block state classes and componentwise weight synchronization.

The unresolved center remains the genuine mathematics: real source and recurrence theorems, actual
population, arbitrary-`n` coverage, truth of every semantic claim, complete interface and rank theorems,
all exceptional chambers and the final implication to all `n`.