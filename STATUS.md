# Status and honesty ledger

**Last updated:** 28 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open. This repository does **not** contain a complete proof.

The authoritative theorem ledger reaches **CMR2581**. CMR2390 onward is recorded in
`proofs/composite-modulus-theorem-index-live-continuation-8.md`.

## Current exact finite endpoint

The branch now contains synchronized executable work banks through T16:

| Target | Exact finite surface | Mathematical status |
|---|---|---|
| T01 | literal source statements, UTF-8 hashes and sealed source artifacts | genuine source statements remain unproved/unpopulated |
| T02 | cases, clauses, axes, exclusions and global-parent applications | genuine recurrence exhaustiveness remains open |
| T03 | open/populated/proved slot and candidate payloads | actual complete population remains absent |
| T04 | skeleton-derived recurrent-block and interface assembly | actual complete assembly remains absent |
| T05 | exact finite geometry and selector arithmetic | arbitrary-`n` coverage remains open |
| T06 | exact slot scores, candidate sets and deterministic winners | score theorems and intended policy remain open |
| T07 | exact fate, state and transition subjects, claims and support DAGs | semantic truth remains open |
| T08 | one active row per exact T06 global-parent application | genuine simultaneous-row exhaustiveness remains open |
| T09 | literal destroyed-resource universes, overlap graph and scopes | physical resource exhaustiveness remains open |
| T10 | exact routed-credit subjects, assignments and injectivity | route and child-state semantics remain open |
| T11 | exact common-row bridges, primitive weights, recurrent closure and strict margins | genuine recurrent-block semantics remain open |
| T12 | exact acyclic auxiliary substitution and selected-response stability | expansion truth and target-state meaning remain open |
| T13 | T04-derived local states, T07-supported global classes and spanning proof trees | genuine cross-block state identity remains open |
| T14 | T11/T13-rooted scale equations, primitive multipliers and component weights | external scale semantics and intercomponent scaling remain open |
| T15 | T04-derived interface rows, primitive intercomponent multipliers and exact final-exit dispositions | genuine interface exhaustiveness remains open |
| T16 | T15-derived critical edges, explicit rank domains, exact endpoint ranks and graph audit | genuine rank meaning and external well-foundedness remain open |

Every final checker permanently reports:

```text
all_n_proved_by_checker = 0
```

## Canonical endpoints

```text
scripts/check_prime_power_geometry_selector_frontier_v2.py
scripts/check_prime_power_auxiliary_semantics_frontier_v2.py
scripts/check_prime_power_state_equivalence_frontier.py
scripts/check_prime_power_component_scale_frontier.py
scripts/check_prime_power_interface_exhaustiveness_frontier.py
scripts/check_prime_power_global_rank_frontier.py
```

## Exact T16 global-rank frontier

CMR2574--CMR2581 remove the independently supplied support graph and rank table accepted by the older
interface/support-condensation stack.

The exact dependency root remains:

```text
T15_INTERFACE_EXHAUSTIVENESS
    -> T16_GLOBAL_RANK
```

and the semantic obligation is:

```text
INTERFACE_RETURN_ROWS_EXHAUSTIVE
    -> GLOBAL_RANK_WELL_FOUNDED
```

### Exact critical-edge census

Every T15 row classified `critical-unranked` contributes one critical-edge subject per positive target
multiplicity. The subject binds the exact T15 row semantic digest, target semantic digest, T15 row
artifact, endpoint T13 class artifacts, multiplicity and endpoint final weights.

Missing, duplicate, reordered or independently supplied critical edges are rejected. Strict T15 rows
need no T16 edge because their row margin is already positive.

### Explicit well-founded domain

A nonempty critical-edge bank requires one open/proved domain of kind:

```text
nonnegative-integer
lexicographic-nonnegative-integers
```

The lexicographic domain has a fixed finite coordinate count and nonnegative integer coordinates. A proved
domain requires one `rank-domain-well-foundedness-proof` artifact. The finite state table alone is not an
external proof of well-foundedness.

If T15 contains no critical row, T16 closes vacuously without an artificial domain or rank table.

### Exact state ranks

The state census is the sorted union of all critical-edge endpoints. Every state record binds its exact
T13 class artifact and the single rank domain.

A proved state requires one `global-state-rank-proof` artifact supported by the domain artifact and its
T13 class artifact. Open states have null rank and verification fields.

### Exact edge descent

Every proved edge binds the exact endpoint rank records and verifies

```text
rank(target) < rank(parent)
```

under the chosen domain. One `critical-edge-rank-descent-proof` must cite the exact T15 row artifact and
both endpoint rank artifacts.

### Complete graph audit

When every rank and edge is proved, T16 reconstructs the critical graph, rejects self-loops and directed
cycles, publishes a deterministic topological order and computes the exact longest critical path. Strict
rank descent forces every critical strongly connected component to be a singleton.

The graph calculation is finite consistency arithmetic. It does not prove that the rank has the intended
recurrence meaning.

### Typed sealing

The aggregate `rank-well-foundedness-proof` synchronizes both:

```text
GLOBAL_RANK_WELL_FOUNDED
T16_GLOBAL_RANK
```

through a noncircular bank containing the T15 proof-bank digest, exact edge subjects, domain/state/edge
records and artifacts, and the complete graph audit.

## Genuine current frontier

The next executable semantic layers are **T17 global-state predicates** and **T18 final row theorems**.

The older `check_prime_power_global_quotient_semantic_refinement.py` is not an acceptable final endpoint
because it reads a separately assembled global-family certificate, accepts opaque predicate/theorem
locators and digests, and has no typed ancestry from the exact T13--T16 banks.

T17 must:

1. derive its complete global-state census from T13 rather than an older identification package;
2. bind every predicate to the exact T13 class core and all representative local states;
3. bind rank-sensitive predicates to the exact T16 state-rank record when relevant;
4. use open/proved records and typed `global-state-predicate-proof` artifacts; and
5. prove representative agreement and the external mathematical meaning of every predicate.

T18 must:

1. derive its complete final-row census from exact T11 recurrent rows and T15 interface rows;
2. bind strict/critical classification to exact T11/T15/T16 data;
3. bind fixed offsets to exact T05/T07/T10/T12 semantic sources;
4. reconstruct parent and target T17 predicates with exact multiplicities;
5. use typed `global-row-theorem-proof` artifacts with complete immediate support; and
6. synchronize `T18_ROW_THEOREMS` without importing an independently supplied quotient family.

After T17/T18, the remaining exact fronts are T19 global-family exhaustiveness, T20/T21 closure of all
`232 + 20 = 252` exceptional chambers, and T22--T43 final premises, typed support, induction handoff,
dossier audit and reviewed root implication.

## Concrete mathematical work still required

1. Transcribe and prove every genuine source statement.
2. Prove the supplied recurrence genuinely exhaustive.
3. Populate all actual T03/T04 objects.
4. Prove T05 covers every relevant geometry for arbitrary `n`.
5. Prove every T07 statement and T06 score theorem.
6. Prove genuine T08 simultaneous-row completeness, T09 physical resource exhaustiveness and T10
   routed-credit semantics.
7. Prove every T11 block and T12 expansion has its intended external meaning.
8. Prove every T13 identity, T14 scale equation and T15 interface theorem semantically correct.
9. Prove the T16 domain theorem, state ranks and edge descents have their intended recurrence meaning.
10. Prove T17 predicates and T18 final rows.
11. Prove global-family exhaustiveness and all 252 exceptional chambers.
12. Complete and review every final premise and handoff assertion.
13. Prove the ordinary implication from the reviewed quotient and handoff to `D(n)=2n`.

## Corrections retained

- A source locator, hash or sealed evidence record does not prove a theorem true.
- Exact recurrence identity does not prove recurrence exhaustiveness.
- Literal T03/T04 data do not prove their intended semantics.
- Finite T05 arithmetic does not prove arbitrary-`n` coverage.
- A T06 integer score remains an external theorem.
- T08/T09/T10 documentary identity does not prove simultaneous, physical or route semantics.
- Exact T11 closure and T12 substitution remain relative to supplied semantic claims.
- A T13 spanning tree does not prove state equivalence.
- T14 rational consistency does not prove external scale semantics.
- T15 exact exit coverage does not prove the interface family mathematically exhaustive.
- T16 integer or lexicographic descent does not prove the rank has its claimed external meaning.
- Typed artifacts and readiness flags are documentary metadata.
- Syntax compilation and isolated helper tests are not a dependency-backed certificate suite.

## Bottom line

There is no complete proof. Through **CMR2581**, the repository has an exact finite documentary stack
from source statements through interface rows and a T15-derived critical-rank graph.

The unresolved center remains the genuine mathematics: real source and recurrence theorems, actual
population, arbitrary-`n` coverage, truth of every semantic claim, interface exhaustiveness, external
rank well-foundedness, state predicates, final row theorems, all exceptional chambers and the final
implication to all `n`.
