# Status and honesty ledger

**Last updated:** 27 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open.  This repository does **not** contain a complete proof.

The authoritative collision-free theorem ledger is split across:

- `proofs/composite-modulus-theorem-index-live.md` through CMR747;
- `proofs/composite-modulus-theorem-index-live-continuation.md` through CMR869;
- `proofs/composite-modulus-theorem-index-live-continuation-2.md` through CMR1197;
- `proofs/composite-modulus-theorem-index-live-continuation-3.md` through CMR1629;
- `proofs/composite-modulus-theorem-index-live-continuation-4.md` through CMR1829;
- `proofs/composite-modulus-theorem-index-live-continuation-5.md` from CMR1830 through CMR1957.

Matching side, prime-power envelope side and inherited coordinate span remain
separate parameters.

## Structural endpoint

Every genuinely new physical triple has one absolute last-entering response-edge
owner.  All owner, collision, local-line, interface, root, thin and CRT labels
remain until exact equivalence or honest componentwise domination is proved.

After exact SCC contraction,

\[
\rho(A)=\max_i\rho(A_{ii}).
\]

Finite response, finite resource use and structural descent are not themselves
potential improvement.  Every recurrent labelled block still requires a strict
numerical certificate.

Already-certified auxiliary modules eliminate through

\[
A+B(I-D)^{-1}C
\]

when `rho(D)<1`.  Reverse-topological rational scaling then glues certified blocks,
and denominator clearing produces one global integer certificate.

## Corrected geometric layer through side five

The corrected first geometric layer contains exactly

\[
86+654=740
\]

coordinate-labelled raw hosts before owner/provenance refinement.  Every response
matching is enumerated and all 89,664 nonaxis host-line capacities and exact
response-averaged line moments are known.

For every host,

\[
z_r(G,\ell)=\sum_{Q\in\operatorname{PM}(G)}\binom{|Q\cap\ell|}{r},
\qquad r=1,2,3.
\]

The exact rank-three split is:

| class | side four | side five | total |
|---|---:|---:|---:|
| `S_3>0` | 53 | 598 | 651 |
| `S_3=0` | 6 | 38 | 44 |
| `S_3<0` | 27 | 18 | 45 |

On a strict host, after exact rank-three payment,

\[
R_G(h)=\sum_\ell\left[\binom{h_\ell}{2}z_1(G,\ell)+h_\ell z_2(G,\ell)\right]
\]

must satisfy

\[
R_G(h)\le S_3(G)-1.
\]

## Primitive witnesses, owners and coefficient linkage

The raw geometric source reconstructs every primitive witness before aggregation:

- rank one: one response edge and one collinear background pair;
- rank two: one compatible response pair and one collinear background point;
- rank three: one collinear response triple.

A strict response-edge order gives

\[
\operatorname{own}(w)=\max_\prec P(w).
\]

Every witness has one fate: retained, deleted, transferred or dominated.  The
owner/fate checker prevents omissions and preserves recurrent owner support.  The
source exports one labelled integer coefficient table, and the geometric bundle
requires direct equality with that export and denominator `|PM(G)|`.

## Destroyed-current-triple cancellation

Let `P` be the exact point set before the response operation and `R` the removed
subset.  The exact current triples destroyed by removal are

\[
\mathcal D(P,R)=\{T\in\binom P3:T\text{ collinear and }T\cap R\ne\varnothing\}.
\]

The surviving ordered point list must equal the owner/fate source background.

### Global injection

The original cancellation checker reserves a different destroyed triple for every
deleted witness.  It is valid but conservative.

### Static conflict coloring

Two deleted witnesses conflict when one response can contain both.  A destroyed
triple may be shared by one independent set of this exact conflict graph.  A proper
`k`-coloring leaves

\[
U_{\rm col}=|\mathcal D(P,R)|-k.
\]

A clique of size `k` certifies static optimality.

### Exact scalar response pool

For each response define

\[
d(Q)=\#\{w\in W_{\rm del}:P(w)\subseteq Q\},
\qquad
K=\max_Qd(Q).
\]

Because destroyed triples are indistinguishable units of the scalar real-triple
potential, the exact response-pool remainder is

\[
\boxed{U_{\rm pool}=|\mathcal D(P,R)|-K.}
\]

The reservation hierarchy is

\[
\boxed{K\le\chi(\Gamma_{\rm del})\le\#W_{\rm del}.}
\]

Thus the pool is never weaker than static coloring or global injection.  The
checker contains an actual side-five geometric source with nine deleted witnesses,
`K=2` and static chromatic number `3`.

Every response satisfies

\[
\boxed{
N_{\rm raw}(Q)-|\mathcal D(P,R)|
\le
B(Q)-U_{\rm pool}.
}
\]

Put

\[
A_B=\sum_QB(Q),
\qquad
M_B=\max_QB(Q),
\qquad
Z=|\operatorname{PM}(G)|.
\]

Then

\[
A_B<ZU_{\rm pool}
\]

proves at least one improving response, while

\[
M_B<U_{\rm pool}
\]

proves every response improves.  In nested assignment currency it is sufficient
that

\[
6l_1+3l_2+l_3<6ZU_{\rm pool}.
\]

## Executable cancellation gains through CMR1957

The conflict-colored checker validates 300 systems containing 657 deleted
witnesses.  It used 363 credits, safely reused 294, retained 300 unused credits,
certified 213 optimal static colorings and checked 1,923 response inequalities.

The colored coefficient-bundle checker validates 300 systems containing 1,180
deletions.  Static reuse saved 556 credits over injection, gained 6,816 average
slack units and produced 100 average-strict and 100 uniform-strict cases unavailable
to the injective certificate.

The response-pool checker validates 300 systems containing 1,317 deletions.  Its
exact pool capacity was 420 versus static coloring capacity 520.  It saved 100
additional credits beyond coloring, 897 beyond injection, retained 400 unused pool
credits and produced 100 average-strict cases unavailable to the supplied coloring.

These regression systems establish certificate correctness.  They do not show that
the real 740 host fibres already satisfy the strict inequalities.

## Label-weighted integer assignment certificate

After source geometry, fate semantics, cancellation and coefficient linkage are
verified, the downstream manifest stores all labelled coefficients, complete
contracted dual families, one unified outer dual and positive row slacks.  Passing
every recurrent row proves

\[
AX<X
\]

for the declared exact or honest componentwise upper table.

## Genuine current frontier

1. Generate actual pre-response point sets, removed subsets, surviving backgrounds
   and strict entry orders for all 740 raw host fibres.
2. Compute each exact deleted-load histogram, response-pool capacity `K` and unused
   destruction credit `U_pool`.
3. Use static conflict coloring only when fixed witness-level credit provenance is
   needed; use `U_pool` for scalar improvement.
4. Prove all remaining domination and transfer evidence and attach the complete
   owner, collision, local-line, interface, root, thin and CRT state semantics.
5. On the 651 strict hosts, combine `S_3-1` and `U_pool` with exact line, return and
   selector coefficients.
6. Resolve the 89 critical/excess hosts using corrected fate maps, nonuniform child
   weights, finer states or certified off-diagonal/auxiliary transfers.
7. Export accepted pool-certified bundles, solve all recurrent assignment rows,
   eliminate certified auxiliaries and publish the global integer quotient before
   CRT gluing.

## Corrections retained

- Historical targets, selectors, traces and destroyed loads are not one simultaneous
  current family.
- Finite scheduling and first-use termination are not potential improvement.
- Rank-mass conservation counts prescriptions, not geometric multiplicities.
- Matching normalization does not quotient Euclidean geometry.
- Silent witness deletion is invalid.
- A fixed destroyed credit may be reused only across response-incompatible deleted
  witnesses.
- The scalar pool reassigns credits after the response is selected; it does not
  provide labelled witness-level payment.
- Unused destruction credit is valid only for the actual pre-response point set and
  actual removed subset.
- A transfer-role string is not proof that a transition executes.
- A deletion evidence string is not a proof unless a rule-specific checker accepts
  it.
- Passing source, cancellation and coefficient checkers proves their stated finite
  equalities only.
- Auxiliary resolvent elimination requires an already-proved strict certificate.
- Failure of a coarse upper model does not prove the exact row supercritical.

## Bottom line

There is no complete proof.  Through **CMR1957**, the finite path now includes exact
response geometry, rank-three budgets, primitive-witness reconstruction, absolute
owners, total fate maps, exact coefficient handoff, destroyed-current-triple
reconstruction, static conflict-compatible credit reuse, the exact scalar
response-pool optimum and the final integer assignment checker.

The unresolved obstruction is to instantiate these objects on the real 740 host
fibres, prove all remaining semantic fate obligations, certify every recurrent
block and publish the strict global integer quotient.
