# Status and honesty ledger

**Last updated:** 27 July 2026

## External status

The classical no-three-in-line conjecture

\[
D(n)=2n
\]

remains open. This repository does **not** contain a complete proof.

The authoritative collision-free theorem ledger is split across:

- `proofs/composite-modulus-theorem-index-live.md` through CMR747;
- `proofs/composite-modulus-theorem-index-live-continuation.md` through CMR869;
- `proofs/composite-modulus-theorem-index-live-continuation-2.md` through CMR1197;
- `proofs/composite-modulus-theorem-index-live-continuation-3.md` through CMR1629;
- `proofs/composite-modulus-theorem-index-live-continuation-4.md` through CMR1829;
- `proofs/composite-modulus-theorem-index-live-continuation-5.md` from CMR1830 through CMR1933.

Matching side, prime-power envelope side and inherited coordinate span remain
separate parameters.

## Structural endpoint

Every genuinely new physical triple has one absolute last-entering response-edge
owner. All owner, collision, local-line, interface, root, thin and CRT labels remain
until exact equivalence or honest componentwise domination is proved.

After exact SCC contraction,

\[
\rho(A)=\max_i\rho(A_{ii}).
\]

Finite response, finite resource use and structural descent are not themselves
potential improvement. Every recurrent labelled block still needs a strict
numerical certificate.

Already-certified auxiliary modules eliminate through

\[
A+B(I-D)^{-1}C
\]

when `rho(D)<1`. Reverse-topological rational scaling then glues certified blocks,
and denominator clearing produces one global integer certificate.

## Corrected geometric layer through side five

The corrected first geometric layer contains exactly

\[
86+654=740
\]

coordinate-labelled raw hosts before owner/provenance refinement. For every host:

- every response matching is enumerated;
- the exact response denominator is known;
- every nonaxis line has exact occupancy capacity and response-averaged moments;
- the exact rank-three numerator and slack are known.

Every response has

\[
\Psi(Q)\in\{0,1,2,4\}.
\]

The exact rank-three split is:

| class | side four | side five | total |
|---|---:|---:|---:|
| strict `S_3>0` | 53 | 598 | 651 |
| critical `S_3=0` | 6 | 38 | 44 |
| excess `S_3<0` | 27 | 18 | 45 |

## Exact response-averaged geometry

For every host and nonaxis line,

\[
z_r(G,\ell)
=
\sum_{Q\in\operatorname{PM}(G)}
\binom{|Q\cap\ell|}{r},
\qquad r=1,2,3.
\]

For actual background line loads `h_ell`, the exact uniform-response numerator is

\[
A_G(B)
=
\sum_\ell
\left[
 \binom{h_\ell}{2}z_1(G,\ell)
 +h_\ell z_2(G,\ell)
 +z_3(G,\ell)
\right].
\]

The identities

\[
\sum_\ell z_2(G,\ell)=Z(G)\binom d2,
\qquad
\sum_\ell z_3(G,\ell)=A_3(G)
\]

fix the total rank-two and rank-three moments.

On a rank-three-strict host, the remaining background-dependent geometric numerator
is strict whenever

\[
\sum_\ell
\left[
 \binom{h_\ell}{2}z_1(G,\ell)
 +h_\ell z_2(G,\ell)
\right]
\le S_3(G)-1.
\]

## Primitive witnesses, owners and fate maps

The raw source reconstructs every primitive witness:

- rank one: one response edge and one collinear background pair;
- rank two: one compatible response pair and one collinear background point;
- rank three: one collinear response triple.

A strict entry order gives

\[
\operatorname{own}(w)=\max_\prec P(w).
\]

Every witness appears exactly once with one fate:

1. retained in a recurrent child with the same owner;
2. deleted with evidence;
3. transferred to a lower-stratum off-diagonal or auxiliary child;
4. dominated by an explicit positive upper multiplicity in the same owner class.

`check_geometric_owner_fate_manifest.py` validates witness completeness, owners,
fate syntax and the labelled integer export. It does not prove a deletion merely
because an evidence string is present.

## Exact source-to-assignment linkage

The source exports one table

\[
c^{(r)}_j(P).
\]

`check_geometric_assignment_bundle.py` requires the downstream edge, pair and triple
table to equal that export exactly and requires the exact uniform denominator

\[
D=|\operatorname{PM}(G)|.
\]

There is no unchecked aggregation or copying step between primitive geometry and
the assignment LP.

## Destroyed-current-triple cancellation

Let `P` be the exact point set before the response operation and `R` the subset
removed before insertion. The current collinear triples destroyed by removal are
exactly

\[
\mathcal D(P,R)
=
\{T\in\binom P3:T\text{ collinear and }T\cap R\ne\varnothing\}.
\]

`check_geometric_destroyed_triple_cancellation.py` requires:

- the surviving ordered point list to equal the source background;
- one unique evidence identifier per deleted witness;
- one actual destroyed triple per deletion;
- injective use of destroyed-triple credits.

If `C` is the reserved credit set and

\[
U=|\mathcal D(P,R)|-|C|,
\]

then every response satisfies

\[
\boxed{
N_{\rm raw}(Q)-|\mathcal D(P,R)|
\le
B(Q)-U,
}
\]

where `B(Q)` is the accepted nondeleted/dominated export score.

This is a genuine rule-specific deletion proof for one-for-one potential
cancellation. A policy-specific verifier must still establish that the supplied
pre-response point set and removed subset are the operation actually executed.

## Exact cancellation slack

Put

\[
A_B=\sum_QB(Q),
\qquad
M_B=\max_QB(Q),
\qquad
Z=|\operatorname{PM}(G)|.
\]

The exact slacks are

\[
\Sigma_{\rm avg}=ZU-A_B,
\qquad
\Sigma_{\rm all}=U-M_B.
\]

- `Sigma_avg>0` proves that at least one response strictly lowers the real-triple
  potential.
- `Sigma_all>0` proves that every response strictly lowers it.

Any proved numerator upper bound `L` may replace `A_B`. In nested assignment
currency it is enough that

\[
\boxed{6l_1+3l_2+l_3<6ZU.}
\]

Thus unused destroyed-current-triple credits now use the same exact integer
currency as the existing rook, line-clean and nested-assignment certificates.

## Label-weighted integer certificate

After source, fate semantics, cancellation and coefficient linkage are verified,
the downstream integer manifest stores all labelled coefficients, complete
contracted dual families, one unified outer dual and positive row slacks. Passing
every recurrent row proves

\[
AX<X
\]

for the declared exact or honest componentwise upper table.

## Genuine current frontier

1. Generate actual pre-response point sets, removed subsets, surviving backgrounds
   and strict entry orders for all 740 raw hosts.
2. Use injective destroyed-triple cancellation for each deletion fate that is truly
   paid by current potential destruction.
3. Prove all remaining deletion, domination and transfer evidence with
   rule-specific transition or auxiliary-module verifiers.
4. Attach the full owner, collision, local-line, interface, root, thin and CRT state
   semantics to every nondeleted witness.
5. On the 651 rank-three-strict hosts, combine `S_3-1` and unused destruction credit
   `U` with exact line, return and selector coefficients.
6. Resolve the 89 critical/excess hosts using corrected fate maps, nonuniform child
   weights, finer states or certified off-diagonal/auxiliary transfers.
7. Export accepted cancellation-certified bundles, solve all recurrent assignment
   rows, eliminate certified auxiliaries and publish the global integer quotient
   before CRT gluing.

## Corrections retained

- Historical targets, selectors, traces and destroyed loads are not one simultaneous
  current family.
- Finite scheduling and first-use termination are not potential improvement.
- Rank-mass conservation counts prescriptions, not geometric multiplicities.
- Matching normalization does not quotient Euclidean geometry.
- Silent witness deletion is invalid.
- A transfer-role string is not proof that a transition executes.
- A deletion evidence string is not a proof unless a rule-specific checker accepts
  it.
- Destroyed triples may not be reused as cancellation credits.
- Unused destruction credit is valid only for the actual pre-response point set and
  actual removed subset.
- Passing the source and coefficient checkers proves completeness and arithmetic
  linkage, not unverified state semantics.
- Auxiliary resolvent elimination requires an already-proved strict certificate.
- Failure of a coarse upper model does not prove the exact row supercritical.

## Bottom line

There is no complete proof. Through **CMR1933**, the finite data path now includes
exact response geometry, rank-three budgets, primitive-witness reconstruction,
absolute owners, total fate maps, exact coefficient handoff, mechanically checked
destroyed-triple cancellation, exact residual destruction slacks and the final
integer assignment checker.

The unresolved obstruction is to instantiate these objects on the real 740-host
state fibres, prove all remaining semantic fate obligations, certify every
recurrent block and publish the strict global integer quotient.
